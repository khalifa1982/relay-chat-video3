- **Image downscale + thumbnails on upload** (frontend-speed; Messages.tsx:512/1278) — first pick next session; biggest remaining mobile-data win (canvas→webp cap 2048px + 512px thumb with explicit width/height; server columns already exist).
- **Party lines: dialable 6-digit room numbers** (unique-features; relay.ts:971/:124) — strongest deferred feature; touches the invite path of the signaling protocol, deserves its own session with protocol tests.
- **New-message dialog contact picker + directory.lookup preview** (ux-clicks; Messages.tsx:1491) — reuse GroupCallScreen's row pattern; the one number-entry surface with no lookup preview.
- **Knock: silent DND-friendly "are you free?" with auto-connect on knock-back** (unique-features; v2events.ts:25).
- **Walkie-talkie hold-to-talk auto-playing voice blips** (unique-features; Messages.tsx:589 + SSE `ptt` flag).
- **File beam: in-call P2P file transfer over a second data channel** (unique-features; relayClient.ts:4536, SFU fallback to normal upload).
- **Presenter laser pointer on screen share** (unique-features; generalize broadcastChat into typed frames at relayClient.ts:4543/4529).
- **Voice-intro ring: 5s "why I'm calling" clip on the caller card** (unique-features; relay.ts:932) — distinct from voicemail (pre-answer screening) but needs an invite-envelope change.
- **MessageList React.memo + composer extraction** (frontend-speed; Messages.tsx:886) — kills per-keystroke re-render of ~100 bubbles.
- **QueryClient default staleTime ~15s** (frontend-speed; main.tsx:13) — verify per-screen behavior since it's a global default.
- **RelayEngine context value useMemo** (frontend-speed; RelayEngine.tsx:205).
- **Profile reorg into 3 headed groups + adjacent Sign-in-PIN/App-lock with differentiators** (ux-clicks; Profile.tsx:283).
- **History filter tabs write the URL as source of truth** (ux-clicks; History.tsx:248) — fixes refresh snapping back to ?filter=missed.
- **Semantic accent-token system replacing scattered hexes** (ui-design; AppShell.tsx:30, Dialer.tsx:532/579, GroupCallScreen.tsx:232, Profile.tsx:255).
- **44px touch targets in the conversation UI** (ui-design; Messages.tsx:1213, header/composer buttons, extended hit area on the ⋮ menu).
- **Shared formatPin() + PinBadge component** — the 6-digit number currently renders four different ways (AppShell.tsx:63, Dialer, History.tsx:431/479, Contacts.tsx:395); make it the brand signature.
- **Chat-bubble theme tokens** (ui-design; Messages.tsx:954 + the duplicate search-results renderer at :837-860) — white-alpha borders are invisible in light theme.
- **Mobile in-call control bar: 5 fixed controls + "More" bottom sheet, hangup pinned** (ui-design; relayAssets.ts:650) — medium effort inside the legacy call CSS/JS, do alongside a device pass.
- **sendBeacon/keepalive go-offline** (bug-sweep; PresenceManager.tsx:32 + a tiny POST /api/v2/offline route) — presence LEDs stale up to 2 min after tab close; reaper already bounds it.