# CLAUDE.md — Briefing for Claude (and other LLM agents)

> Read this file first whenever you join this repo. It tells you what RELAY is, how it's built, what's already working, what's been deliberately chosen, and what to be careful about. Keeping this file accurate is part of your job as a contributor.

---

## TL;DR

**RELAY** is a self-hosted, browser-based voice / video / chat caller wrapped in a phone-app shell. Visitors pick a display name (or sign in), get a persistent 6-digit number, and can dial each other (peer-to-peer WebRTC mesh up to 6 participants), send SMS-style messages with attachments, and manage contacts. Hosted on **AWS** at `https://your-chat.io` (2× EC2 Mumbai + ALB + ElastiCache Redis + S3; auto-deploys on every push to `main` via `.github/workflows/deploy.yml`). The former Manus/`.org` deployment was **emptied and retired by the owner on 2026-07-21** — `.io` is the ONLY live deployment; do not target `.org` or the Manus Publish flow.

Stack: **React 19 + Vite + Tailwind 4 + Express 4 + tRPC 11 + Drizzle ORM (MySQL) + Manus OAuth + Server-Sent Events signaling + native WebRTC + MediaPipe Tasks Vision**. Deployed as a single Node.js process on Cloud Run.

**Current version: v2.99.75** (v2.99.75 — **CHOOSE YOUR OWN 6-DIGIT NUMBER** (owner: *"my personal number is 235680. Can you change it to 777777 and make sure whoever added me in his contact book or has any communications and messages makes sure they appear with the new number and keep the old data, such as calls or messages, but only updates to 777777"*). **HALF OF THAT ALREADY WORKED, said plainly rather than quietly re-fixed**: renumbering has propagated since v2.99.54, and `numberContinuity.test.ts` MACHINE-CHECKS it — it scans `drizzle/schema.ts` for every `varchar(…, {length: 6})` and FAILS THE BUILD if any number-bearing column has no declared strategy in `NUMBER_BEARING_COLUMNS`. All five are declared: `identities.number` moves, `contacts.number` is rewritten (so everyone who saved you keeps reaching you AND a block placed on the old number FOLLOWS you), `conference_participants.number` is rewritten scoped by identityId, `conference_history.dialedNumber` is resolved LIVE at read time, and `party_lines.number` is never touched because a line is not a person; the History roster JSON is likewise resolved by identityId, which is separately pinned. Everything else — messages, threads, call history, statuses, attachments, presence, push subscriptions — references the identity by numeric id, so it follows the person for free with nothing to migrate. **WHAT DID NOT EXIST was any way to pick a SPECIFIC number**: `regenerateNumber` only ever handed out a random one. **THE CONSTRAINT THAT SHAPED THE ENTIRE CHANGE**, and it is a good one: `guestUpgrade.test.ts` pins that the codebase contains exactly **ONE** writer of `identities.number`. Propagation is the whole difficulty of renumbering and a parallel implementation is precisely how History's copies came to rot before v2.99.54 — so this is a `desiredNumber` PARAMETER on the existing writer, never a second function, and that pin makes any other shape unshippable (mutation-proven: a parallel writer that skips propagation fails the suite). The chosen number resolves to `newNumber` BEFORE the transaction, so the body it shares with a random allocation is byte-identical and inherits every copy. **TAKING A NAMED NUMBER SAFELY**: `normalizeDesiredNumber` accepts the grouping people actually type ("777 777", "777-777" — the app displays numbers that way, so refusing it would be rude) but strips ONLY spacing and grouping, never every non-digit, because `\D`-stripping would silently read "7a7b7c7d7e7f" as 777777 and turn a typo into a successful renumber of somebody's identity; it honours the same `RESERVED_PREFIXES` a random allocation does, or the reserved range would be reserved against the allocator and not against people; and it fails closed on a non-string. Availability is checked against BOTH number tables and then RESERVED in the shared ledger, which is what closes the cross-table NEW-vs-NEW race against a party line minted in the same instant. It spends the same **global mint budget**, because the drain backstop must not be sidesteppable by naming numbers instead of asking for them. Choosing the number you already hold is a **no-op, not an error**, so a double-tap or a retry after a dropped response is harmless rather than reporting "taken" about the caller's own number. **THE SUBTLE BUG I HAD TO REASON OUT AND THEN PIN**: the failure path must NOT hand the reservation back on a pre-flight refusal. `taken` means somebody ELSE holds it — possibly an allocation that has reserved the number but not yet inserted its row — so releasing there would un-reserve a stranger's in-flight number and let two people end up with it. The early return therefore precedes the release, and the ordering is mutation-verified rather than assumed; a genuine post-reservation failure DOES release, and `releaseUnusedNumberReservation` re-checks the number is absent from both tables so even then it cannot un-reserve a bound one. A lost race surfaces as `taken` (errno 1062) rather than a 500, and the OLD number is never released — the ledger is monotonic on purpose, or a number somebody kept written down would later connect them to a stranger. **REGISTERED ACCOUNTS ONLY, as policy not as a limitation**: a chosen number is first-come and permanent, and a guest identity is session-scoped, so a guest claim would squat a memorable number and then strand it the moment the browser closed. **FLAGGED HONESTLY: chosen numbers are FIRST-COME.** Nothing reserves 777777 for anyone, so the owner should claim it promptly; making it admin-only was considered and rejected because `users.role = "admin"` is granted by hand via SQL and may not be set on their account, which would have shipped a feature they could not use. UI: Profile now offers **"Choose my number"** beside **"Random number"** (the old button, renamed and behaviourally untouched — a test pins it still calls the no-argument path). The dialog deliberately does NOT close on submit, because the number may be taken and closing before the server answers would hide the one message telling the person to pick another; refusals are the SERVER's own named messages, since a typo and a collision need different things from the reader. A text input with `inputMode="numeric"` rather than `type="number"`, which brings spinners, accepts "1e5" and drops a leading zero, and `dir="ltr"` so an RTL locale cannot reorder the digits being typed. `server/chooseNumber.test.ts` (30); **all 12 tripwires verified by MUTATION** from byte-exact backups, source confirmed byte-identical afterwards. **A RECURRING WEAKNESS OF MY OWN FINALLY GIVEN A FIX RATHER THAN A FOURTH PATCH**: for the fourth release running, a "this pattern is gone" assertion matched a COMMENT explaining why the pattern is gone — here `type="number"` inside the comment justifying its absence — so it passed on prose rather than behaviour, which is worse than no assertion. Every `not.toMatch` against a source file now goes through a `codeOnly()` helper that strips comment lines. **NO OPS WORK AND NO SCHEMA CHANGE**: this reuses the existing transaction, ledger and migrator entirely. 2308 tests.) (v2.99.74 — **DELIVERY RECEIPTS, A REAL MESSAGE MENU, AND THE VOICE BAR THAT STILL DID NOT MOVE.** **(1) ONE TICK AND TWO TICKS WERE THE SAME STATE** (owner: *"it shows you what check if it's delivered. I mean the other user is online and he received, but he didn't open it. It should show second check mark beside that. If he [read] it, it will turn both check marks into blue colour… and any type of message either voice text video whatever"*). `messages.status` has carried a `delivered` value since the schema was written and **NOTHING EVER WROTE IT**, so the UI's "✓✓ if read else ✓" was a two-state display of a three-state fact: sent and delivered rendered identically. This adds the missing transition. **THE RECIPIENT REPORTS IT, NOT THE SERVER** — the tempting version marks a message delivered when the server fans its SSE event, but an open stream proves a socket exists, not that the message reached an app that has it, and it would MISS the case the second tick exists for: somebody who was offline when it was sent and opens the app later without opening the thread. So the report is driven off the RECIPIENT's thread list, mounted in `AppShell` rather than in `Messages` for exactly that reason — opening the app is enough, opening the thread is not required. **DEDUPED ON THE THREAD'S NEWEST MESSAGE TIME**, which is load-bearing rather than an optimisation: that list refetches every 15s, so an undeduped report is a write over the conversation's messages every fifteen seconds forever, for a fact that has not changed — worse than the missing feature. A newer `lastMessageAt` is precisely the signal that something new arrived; the watermark never moves backwards (two tabs, or a stale cached list, must not re-fire); the claim is taken BEFORE the request, because the live `message` event and the refetch it triggers land in the same tick and would otherwise each fire their own write; a genuine failure UN-claims, because a blip must not cost the sender their tick until the next message happens to arrive; and one sweep is capped at 12, so opening the app on a large backlog is not a write storm (the remainder goes on the next sweep, proven by test rather than asserted). **THE RECEIPT CANNOT WALK BACKWARDS**: `markThreadDelivered` promotes ONLY from `sent`, so a late report cannot turn two blue ticks grey again, and excluding `failed` leaves a real failure visible instead of claiming it arrived. Membership-scoped and never delivering to your own messages — the S6 finding's shape, in a second place. **IN A GROUP, delivered means AT LEAST ONE member has it**, not all: the row is shared, so the first member to report flips it for the sender's view — not a shortcut introduced here, since `markThreadRead` has always worked exactly that way, so the second tick inherits the semantics the third already had rather than inventing a different rule for the same row (per-recipient receipts need a per-participant table). The read transition already accepted `delivered` as well as `sent` before this release, which is the one thing that would have silently broken the whole chain — a message that got its second tick could never have gone blue — now pinned so a future narrowing has to come back and think about it. **READING BACKFILLS THE DELIVERED TIME** (`COALESCE`), or the info panel would show a message read at 10:05 that was never delivered, which is not a thing that can happen and reads as a bug in the panel. **A BUG CAUGHT BEFORE SHIPPING, worth naming because it would have been invisible in testing**: the first cut cast the new event through `as unknown as` to avoid touching the union — but `KNOWN_V2_EVENT_KINDS` gates the RECEIVE side of the Redis bus, so an undeclared kind is delivered locally and **silently DROPPED whenever the sender is on the other instance**, which on a two-instance fleet is most of the time. Single-instance dev would have looked perfect. `delivered` is now a declared member of both, and a test asserts the cast is absent. **(2) THE ⋮ MENU** (owner: *"reply or put forward or delete or info"*): Reply / Forward / Copy / Info, plus Unsend on your own. **Forward RE-SENDS rather than re-pointing the row**, so the target thread gets its own message with its own receipts — which is what makes a forward behave like a send; the attachment rides by id and `messages.send` re-checks the sender may use it, so this cannot smuggle media the forwarder could not already see. Forward is **WITHHELD from a self-destructing message** in both directions rather than offered-then-refused: copying a view-once message into a second permanent thread breaks the exact promise it was sent under, and a menu item that cannot do its job should not be there (the action still refuses, as a backstop, not as the UI). A still-LOCKED expiring message shows **no menu at all** — the QA H2 guard already there, which Forward and Info inherit by living inside it. **INFO** lists Sent / Delivered / Read, to the SECOND and always naming the date — deliberately not the bubble's `formatTime`, which drops today's date and rounds to the minute: those three times are frequently inside the same minute, so a minute-precision panel shows three identical values and answers nothing. A time nobody recorded shows an honest **em dash**, never a guess, because every message predating this release has none. **(3) THE VOICE BAR STILL DID NOT MOVE** (owner, second report on the same control). v2.99.73 fixed the probe that DESTROYED playback and threaded the stored duration through the ordinary bubble — and **missed the second render path**: the revealed-expiring `content()` helper never passed `durationMs`, so a view-once voice note had `dur = 0`, and since the fill is `cur / dur` the bar was pinned at zero however well playback was going. Fixed. Two more holes closed in the same pass: making the probe safe by deferring it until PAUSED meant it never ran during a first play, so a note with no stored length still sat still for its entire first play — it now probes on MOUNT (and only when nothing is stored, so the normal case costs no request at all; the cost lands on pre-v2.96 notes as one `preload="metadata"` header fetch) — and the rAF clock now picks a duration up mid-playback if the engine settles it late, without ever overwriting one already trusted. `client/src/app/deliveryReceipts.test.ts` (39): the reporting RULE is tested behaviourally, because a source pin cannot tell you whether a 15-second poll writes every 15 seconds. **ALL 24 tripwires verified by MUTATION** (reverted from byte-exact backups, aborting on a missing target, source confirmed byte-identical afterwards) — including the original screenshot bug, the receipt-walks-backwards case, and the bus-allowlist drop. One of my own assertions was thrown out during that run rather than counted: a check that the old hand-drawn "✓✓" was gone matched two COMMENTS describing the history, so it passed or failed on prose — it now strips comment lines and checks CODE. Two redundancies removed while pinning: `Receipt` owned its own mine/status guard AND had an outer one at the call site (two guards for one rule is how a receipt ends up rendering in one place and not the other), and its non-`mine` colour branch was unreachable behind its own early return. **NOT DONE, said plainly**: "delete" for a message somebody else sent. Unsend removes a message for everyone and is rightly ours-only; a per-recipient "delete for me" needs a real per-participant tombstone touching `messages.list`, thread previews, search and the stored unread counters — a localStorage version would be a lie that reappears on their other device. It is a feature, not a polish item, so it is flagged rather than faked. 2278 tests.) (v2.99.73 — **VOICE PLAYBACK THAT MOVES, RECORDING YOU CAN SEE AND UNDO, DATED MESSAGES, AND INSTANT STATS.** **(0) "MAKE IT LIVE, NOT AFTER 30 SECONDS"**: v2.99.71's 2s push was close but still tick-bound, so every online/offline TRANSITION now pokes the feed directly and the figure moves in ~150ms. The poke is COALESCED and that is load-bearing — presence writes arrive in bursts (a heartbeat sweep, a call ending, the reaper) and one read per event would be worse than the polling this replaced, so 50 simultaneous sign-ins cost ONE read; a poke only re-reads `onlineNow`, never the full-table counts, and is a no-op with no audience. The notify lives INSIDE `markOnline`/`markOffline` rather than at their four call sites (forgetting one is the class of bug this repo keeps re-learning) and is registered as a HOOK because `statsFeed` already imports `v2db` and a second edge would be a cycle; a mere heartbeat from someone already online deliberately does NOT poke, or every open tab costs a read every 30s. **(1) THE VOICE PLAYER'S CONTROL NEVER MOVED** (owner: *"the sound is played, but the control doesn't show that it's moving… it only stays there like it's not played"*) — TWO real bugs, both visible as a scrubber pinned at 0:00 beside "· · ·". **The duration probe DESTROYED playback**: MediaRecorder blobs report `duration === Infinity` until seeked past the end, and the workaround ran from `loadedmetadata`, which fires just AFTER the click that started playback — so pressing play seeked to `Number.MAX_SAFE_INTEGER`, clamped to the end, fired `ended` and reset the clock to 0. It now never runs while playing and uses the `1e101` form the codebase's own `readMediaDurationMs` already uses (MAX_SAFE_INTEGER is refused outright by several engines, which is why the total stayed "· · ·" too). **The duration was already known and went unread**: every voice note this app records stores its real length in `attachments.durationMs` and `messages.list` already hands the whole attachment row to the client, so seeding from it means the common case needs NO probe and is seekable before the first play. The clock is also rAF-driven now, since `timeupdate` fires ~4Hz — enough to look motionless on a short note. **(2) RECORDING SHOWED NOTHING AND COULD ONLY BE SENT** (owner: *"it just turned red, and there is no wave when you talk… you need to click on the red to send, or there's no choice to delete the voice, or you can pause"*): Stop was the ONLY exit and it also sent, so a misfire had no way out but send-then-unsend. The composer row now becomes **discard · live waveform · clock · pause/resume · send**. THE WAVE IS REAL — RMS off a WebAudio analyser tapped from the SAME MediaStream the recorder encodes, so the bars move because the mic is hearing you; a decorative animation would look identical while telling you nothing, which is the complaint. RMS not peak (a peak meter pins to the top on any transient); NOT connected to `ac.destination` (feedback loop); degrades to 0 rather than failing the recording. **Paused time is EXCLUDED from the duration**, or a note paused a minute claims a minute of audio it does not have; the UI reads the paused state BACK rather than assuming, because an engine without `MediaRecorder.pause` leaves it running. Discard calls `cancel()`, which resolves `done` null, so the note is genuinely gone. The 30 bars + clock are written IMPERATIVELY from one ~20Hz rAF loop — a state update per frame would re-render the whole thread 60x/sec, the v2.99.67 mistake. **(3) NO DATE ON A MESSAGE**: the TIME was already there, the DATE was not — the thread's day separator only covers messages BELOW it, so everything above carried a bare "12:09 PM" with nothing saying which day (the owner's screenshot shows three of them above a "Today" divider). Today stays time-only; older names the day; another year names the year, same rule as `formatLastSeen`. `voiceAndStamps.test.ts` (20) + statsFeed 14 -> 19; ALL 12 messaging tripwires and 4/5 stats tripwires verified by MUTATION. THREE OF MY OWN TEST WEAKNESSES were found by that run and fixed rather than counted as passes: the rAF assertion matched the self-re-arm inside `tick()` so deleting the kick-off stayed green; the timestamp test asserted the formatters, leaving `return time;` — the very bug — passing; and a new poke test claimed in a comment to slow the tick and did not, so a coincidental tick would have satisfied it. ONE mutation did NOT bite and is recorded rather than hidden: `pokeStatsFeed`'s audience check is redundant because `refresh()` guards it too. TWO pre-existing v2.96 pins REWRITTEN to the stronger invariant — one froze the exact player prop list, the other pinned `MAX_SAFE_INTEGER` as the CURE, i.e. it pinned the bug. 2238 tests.) (v2.99.72 — **THE FIVE LIVE FIGURES ARE PUSHED, SO THEY MOVE WHILE YOU WATCH** (owner: *"the statistics of number of users, active users, messages, calls — the one on the main page and on the login page — it should be dynamic. While I'm seeing the page, if somebody logs in, it will automatically update. No need for me to refresh the page. These numbers are read from the database, for all five."*). WHAT WAS ALREADY TRUE, said plainly rather than quietly re-fixed: all five WERE already live database reads (`getPublicStats` runs six `COUNT(*)`s with no cache) and both surfaces already refreshed without a reload — so the ask was not broken, it was SLOW and it scaled badly. WHAT WAS ACTUALLY WRONG: the landing page polled every **30s** with `refetchOnWindowFocus: false`, so a visitor could sit looking at figures half a minute stale AND returning to the tab did not refresh them — exactly the "I have to refresh" experience reported; the sign-in screen polled at 15s; and polling scales the wrong way, since EVERY viewer independently ran six `COUNT(*)`s, one over `messages`, the largest table in the schema. THE FIX: one shared computation per instance pushed to every viewer over SSE (`server/statsFeed.ts`, `GET /api/stats/stream`), so ten thousand people watching the landing page cost what one costs. Public and unauthenticated by design, exactly like the `stats.public` procedure it mirrors — the landing page has no identity — and the payload is aggregate counts and nothing else (a test asserts the frame's exact key set: never a name, a number or an identity). **TWO CADENCES**, because one would have been indefensible: `onlineNow` is the figure the owner named, it is the one that moves when somebody signs in, and it is CHEAP (`presence` is small and carries `presence_isOnline_idx`, so it is an index scan) — new `getOnlineCount()` refreshes it every **2s**, while the other four barely move and are expensive, so they refresh every **20s**; recomputing a `COUNT(*)` over `messages` every two seconds to watch a number that changes hourly would be waste no user could ever see. **NOTHING RUNS WHEN NOBODY IS WATCHING** — the timers start on the first subscriber and stop on the last, so an idle instance does ZERO database work, which is the property the old per-visitor polling had and would have been easy to lose. Frames are sent **only on change**, so a quiet network costs one heartbeat comment every 25s. IT DEGRADES RATHER THAN MISLEADS: `getOnlineCount` returns **null, never 0**, and the feed then holds the previous snapshot, because "0 people online" is a visible claim on the front page and a query blip must not be allowed to make it; a refused client is answered as a STREAM carrying a `retry:` directive rather than a bare JSON 429, since an EventSource ignores the body and reconnects on its own schedule — without it the refused clients ARE the reconnect storm. THE CLIENT KEEPS A BACKSTOP POLL deliberately: a proxy can hold an event-stream open while buffering it, which is indistinguishable from a quiet network from the browser's side, so `client/src/app/useLiveStats.ts` polls at 15s until the stream proves itself and at 120s after — never zero — and does NOT hand-roll a reconnect loop on top of EventSource's own (that is how you get two overlapping streams per tab). BOTH SURFACES now read the SAME hook, so neither can be fresher than the other; the old 30s-vs-15s split was exactly that kind of silent drift. `server/statsFeed.test.ts` (14) drives the REAL Express route over a REAL socket, because a source pin cannot tell you whether a stream delivers a second frame when a number moves and that is the entire feature — proven: immediate seed, push on change, silence when unchanged, TWO viewers served from ONE read, timers tracking the audience, and a failed read holding the last good value. All 8 tripwires verified by MUTATION. TWO HARNESS BUGS OF MY OWN found and fixed rather than reported as results (a 1200ms wait for a 2000ms tick, and a module-scoped snapshot that made a correct corrective frame look like noise) — cadences are now env-overridable, clamped so a bad value cannot make a busy loop, and the run was repeated three times to confirm it is not flaky. TWO PRE-EXISTING PINS REWRITTEN to the stronger invariant rather than relaxed, both having asserted the polling this release removed. The ES5 `Set`-iteration trap (TS2802) bit again in `broadcast()` and was caught by `pnpm check` — `pnpm build` uses esbuild and does not typecheck, which is exactly how it reached CI in v2.99.49. NO OPS WORK: no env var, no schema change, no new dependency. 2238 tests.) (v2.99.71 — THE TURN HEALTH CHECKER DISAGREED WITH THE SERVER ABOUT WHAT IS ADVERTISED. The owner finished the TURN work; read back from the LIVE `/api/relay/ice` rather than taken on trust, the fleet now advertises **hostnames with TLS on 443** — `turn:{turn,turn2}.your-chat.io:3478?transport=udp` + `:3478?transport=tcp` + `turns:...:443?transport=tcp`, 6 URLs over 2 relays. Hostnames because a public TLS cert cannot be issued for an IP; `turns:` on 443 because that is indistinguishable from HTTPS and passes the DPI that was dropping plaintext TURN; and the plaintext `turn:...:443` form is ABSENT, which is the observable proof that v2.99.65's `TURN_TCP_ALT_PORT=off` is live. That closes the last owner-only item from v2.99.63–65. IN THE SAME BREATH, A BUG IN MY OWN TOOLING: `scripts/turn-check.mjs` never learned the `off` rule — `+(process.env.TURN_TCP_ALT_PORT || "443")` is **NaN** when the value is `off`, so the checker probed a third endpoint per relay that the fleet does not advertise and can never answer; every future health check would have reported 2 permanently DOWN endpoints, and a false failure is exactly what hides a real one. THE REAL FIX IS THE TEST, because this is the v2.99.50 class — two gates disagreeing about one rule, in different files AND different languages (the checker is plain `.mjs` run by bare `node` on an EC2 instance from the release tar; `iceServers()` is TypeScript in the server bundle), so no string check would catch the NEXT divergence: `deriveTurnEndpoints`/`endpointToUrl` are exported and `server/turnCheckParity.test.ts` compares their ACTUAL OUTPUT against `iceServers()` over 7 env shapes including the live one, asserting the sets are EQUAL (mutation-verified: reinstating the unconditional push fails 3 of 9). The script's main body is now behind a real run-as-main guard so importing it is inert — without it the import ran a health check and `process.exit(0)`, killing the test runner. LIVE EVIDENCE from the in-VPC run: every REAL endpoint allocates — `turns:turn.your-chat.io:443` OK relayed port 58067 and `turns:turn2.your-chat.io:443` OK 65319, plus udp/tcp 3478 on both — so TLS-on-443 works on BOTH relays and, because the check is an authenticated Allocate rather than a ping, both relays provably share one `static-auth-secret`; that closes the v2.99.63 item with measurement. The SAME run printed the bug this release fixes, verbatim: `turn:turn.your-chat.io:NaN tcp DOWN connect: Port should be >= 0 and < 65536. Received type number (NaN).` on both relays, yielding `TURN_CHECK_EXIT=1` and "2/8 endpoint(s) NOT usable" — a wholly FALSE failure. Note the workflow step ECHOES the script's exit code rather than propagating it (a status report must not mask the rest of the fleet report), so the step going green is never evidence of relay health — the per-endpoint lines are. 2198 tests.) (v2.99.70 — **THE GROUP GRID READS AS A LIVE CALL, AND THE ORPHAN RECOVERY CAN ACTUALLY BE RUN.** **(1) THE GROUP-CALL TILES** (owner, asked TWICE: *"make it animated like people is talk… their lips is moving"*). SAID PLAINLY FIRST: lips cannot move on a still image. The ten tiles are stock PHOTOGRAPHS of people on video calls — laptop and monitor bezels in frame, faces at wildly different scales and positions — so there is no normalised mouth region to animate; a hardcoded mouth overlay would land on a houseplant in one tile and a keyboard in another. Real lip movement needs real footage, which is an asset decision. THE ACTUAL BUG, and it was not the missing lips: **nothing correlated.** Four tiles carried hardcoded speaking times (`lpSpkA2`/`lpSpkO2` at -15s/-10s/-5s) on a DIFFERENT stagger from the ring sweep (-2s per tile), so the ring lit one face while another face's bars bounced — 20s of motion that belonged to nobody, which is exactly why it read as decoration. Now ONE schedule per tile drives the ring, the level meter, the nod and the chip's speaking dot, so the grid reads as a single conversation moving around the room. **A MUTED PERSON IS NEVER THE SPEAKER**: the first cut derived the schedule from the raw index and the screenshot showed the bug at once — the ring lit ZAIN and DANA, who wear mute badges. New `speakingTurns()` gives a turn only to UNMUTED tiles (ordinal, not index), and 8 speakers over the 20s loop is 2.5s each, filling it exactly so there is no dead gap either; a muted tile keeps its live feed but gets no ring, nod, meter or dot. **WHAT MAKES A PHOTOGRAPH READ AS A FEED**: new `lpLive`, sub-pixel non-periodic drift on every tile — a real video tile is never perfectly still and photographs have no drift at all, which is why the grid read as fixed images however much the chrome moved. It rides a WRAPPER, not the img, because the img already runs Ken-Burns and two transform animations on ONE element do not compose (the later declaration wins); ten non-harmonic timings so no two tiles drift in lockstep. **THE BARS ARE VOICE-SHAPED**: the old `lpEq` was one smooth 1.1s sine on every bar, which reads as a loading spinner — speech is bursty and the bars must disagree, so four bars get four uneven envelopes (`lpVox1/2/3`) and four different periods; `lpTalk` became a nod plus a scale, because a pure scale reads as a camera zoom. **COST, MEASURED** (this is the page that made a phone hot two releases ago, so it is measured rather than asserted) — emulated 390px phone at 4x CPU throttle, before -> after: **repainting animations 4 -> 0** (the removed `lpSpkA2` animated `box-shadow`, which repaints every frame), compositor-only animations 50 -> 84, phone layout and tile size identical. Repaint is the expensive one, so this wins on the metric that matters; layer count went UP and that is the deliberate trade, stated rather than hidden. No JS added, and it stays inside the existing `prefers-reduced-motion` kill switch. **TEN PEOPLE NO LONGER RENDER AS EIGHT PLUS TWO**: `auto-fit/minmax(130px,1fr)` resolved to EIGHT columns on the 1138px card, so the 10-up rendered 8 + 2 with a large empty block — which is what the owner's screenshot shows. Now an explicit 5x2, stepping 4 / 3 / 2 so no breakpoint orphans a single tile, 2x5 on a phone with no horizontal overflow; the "GROUP CALL · 10 LIVE" chip also sat ON TOP of the first tile and now has its own band, confirmed by rect intersection — and the FIRST metric written for it was wrong (it compared the grid CONTAINER's box to the card's, but padding moves the tiles, not the container, so it could never have detected the fix). **(2) THE ORPHAN RECOVERY IS NOW RUNNABLE.** `scripts/recover-orphan-identity.mjs` has existed since v2.99.60 with no way to reach a live database — `DATABASE_URL` lives only in `/home/relay/.env` on the fleet. New `recover-identity` action in `aws-ops.yml` runs it over SSM on ONE app instance, sourcing the fleet env, so no production credential is ever copied onto a laptop and none is printed. DRY RUN IS THE DEFAULT (`recover_apply` false), because the script's one destructive act is deleting the account's current identity row — which it does only after proving that row empty across all seven identity-referencing tables. ONE instance, `--max-concurrency 1 --max-errors 0`: running a DB mutation on both boxes would in fact be harmless (the second pass finds the orphan already claimed and refuses), but "harmless because a guard catches it" is not a reason to fire a write twice. The verdict comes from the script's printed `RECOVER_EXIT=` marker rather than the SSM status, because a wrapper or pipeline can mask a non-zero exit (the v2.99.46 bug). INJECTION: `recover_number`/`recover_email` are free-text `workflow_dispatch` inputs reaching a command string executed on production EC2, the class this file has been bitten by TWICE — both get base64-on-runner / decode-on-instance, and it is **proven empirically**: the step's own CMDLINE construction was replayed with eight hostile values (quote+semicolon, double-quote break, `$( )`, backticks, `&&`, `|`, embedded newline) and executed with a stub `node`; in all 8 the value arrived as ONE literal argument and no payload ran. The FIRST harness reported all eight as pwned — a bug in the HARNESS, not the code: the stub echoes its arguments, so the payload's own text appeared in the output and a naive grep matched it; text in output is not evidence of execution, so the detector became a sentinel FILE that either exists or does not. `groupGridLive.test.ts` (19) + 8 `recover-identity` pins; TWO pre-existing pins REWRITTEN to the stronger invariant rather than relaxed — the v2.99.16 grid pin asserted `-i * 2` and the comma-combined `g.spk`, and both of those WERE the incoherence removed here, and the aws-ops options pin froze the exact action list, which has now broken twice on a legitimate addition while saying nothing about the property that matters (it is now a prefix match plus "verify stays first, no duplicates"). STILL OPEN: lip movement itself, which needs footage. 2190 tests.) (v2.99.69 — **ADOPT-AND-RETIRE: A GUEST CAN RECLAIM THE NUMBER A BROWSER CLOSE FORGOT.** The last structural data-loss path. v2.99.49 fixed the guest -> register transition and v2.99.54 welded the number to the person through a renumber; what stayed broken was simpler and far more common — **a guest closes their browser**. Both things that resolve a guest are session-scoped BY DESIGN (the device id in `sessionStorage`, the guest cookie a session cookie), so the number, contacts, threads, call history and statuses were stranded with NOTHING left in the browser able to name the row, and nothing reaps those rows either, so the data sat there intact and unreachable. THE DECISION THAT MAKES THE FIX SAFE: **automatic resolution is NOT loosened** — making the device id or the cookie durable would restore the previous guest AUTOMATICALLY, which on a SHARED browser drops the next person into someone else's account, and that session scoping was an explicit product decision rather than an oversight. So this adds a SECOND, DELIBERATE path instead of widening the first: a recovery key the browser keeps in `localStorage`, sent only when the person asks for their number back. Because recovery takes a tap, an explicit **sign-out** is enough to sever it (`forgetGuestRecovery()` on both sign-out paths), so the shared-browser property is preserved by the gesture that actually protects it; pinned that the device id is still sessionStorage and `guestCookieOptions` still adds no `maxAge`. THE KEY: new `server/guestRecovery.ts` (dependency-free) mints 32 bytes of CSPRNG as 64 hex, stored server-side ONLY as a SHA-256 hash in the new nullable `identities.recoveryHash` (+ `recoveryIssuedAt`), so a row read never yields something that can claim an identity — the same reason `push_subscriptions.claimHash` is a hash; plain sha256 is correct and a KDF would be wrong (256 bits of uniform randomness has no dictionary to stretch against, and a per-row salt would turn the by-hash lookup into a table scan). `normalizeRecoveryKey` fails CLOSED. Minted in the SAME insert that creates the identity, so there is no window in which a guest exists with no way back. EVERY EXISTING GUEST IS HEALED: a pre-release row has a NULL hash and would otherwise stay permanently unrecoverable, so `ensureGuestRecoveryKey` issues one on the row's NEXT visit from both of `startGuest`'s reuse branches — never overwriting an EXISTING hash (that would invalidate the copy the browser holds, turning a recoverable identity into a lost one), never touching a row with a `userId`, never throwing, verdict from `affectedRows`. THE SAFETY GATE, because this DELETES AN IDENTITY ROW: the row being retired must be **provably EMPTY** across all seven identity-referencing tables (`identityFootprint`), or adoption would just move the loss to the other row — there is deliberately **no override flag**, and an unreadable count is **-1, never 0**, because treating an unknown as empty is how you delete somebody's messages. The column list matches `scripts/recover-orphan-identity.mjs`: `contacts.ownerId` (NOT `ownerIdentityId`) and call history split into caller/callee, both wrong in that script's first draft and now forbidden by a test. THE RECOVERED IDENTITY KEEPS ITS OWN NUMBER — nothing in the adoption path writes `identities.number` (pinned), so `NUMBER_BEARING_COLUMNS` needs no new entry and nobody who saved the number is broken by its owner coming back. One transaction: retire FIRST (the per-account unique index blocks the claim while the old row lives), then claim, each statement re-stating its preconditions in the WHERE so a concurrent change LOSES instead of corrupting; a lost claim throws to roll the delete back rather than leaving the caller with no identity, reported as `race-lost`. A GUEST caller is rebound (device id + fresh guest token move over, so later requests resolve normally with no special case anywhere else); a REGISTERED caller has the row claimed by their account with the guest handles dropped; a visitor with NO identity skips the retire entirely, which is the PRIMARY path (reopen the browser, restore BEFORE typing a name). EMERGENT PROPERTY WORTH NAMING: this is also a **self-service fix for the owner's original bug** — if a registration ever fails to adopt the browser's guest row, that row stays `userId IS NULL` while the browser still holds its key, so the Profile card offers exactly the stranded identity and a registered caller can claim it without an operator touching the database. `identity.guestRecoveryPreview` (read-only) returns the number, name and footprint the prompt shows verbatim ("restore 601-586 · 14 contacts, 320 messages"), reporting `null` counts rather than a confident zero; `identity.adoptGuestRecovery` performs it. Both `directoryGate`-limited BEFORE any DB work (brute force is not the threat at 2^256 — an unmetered DB read is), both reject a malformed key without hashing it, and refusals are NAMED (`not-found` / `current-has-data` / `footprint-unknown` / `race-lost` / `unavailable`) because each has a different correct next step and `current-has-data` must never be silently resolved by discarding one side. A key can only name an UNCLAIMED row and is deliberately NOT gated on `guestExpiresAt` (that models the COOKIE's life, and this lookup exists for exactly the case where the cookie is gone); registration also nulls the hash as defence in depth. UI: `client/src/lib/guestRecovery.ts` (localStorage, NOT a cookie — a cookie is sent on every request, which is what would make recovery automatic again) and `client/src/app/GuestRestore.tsx`, used by BOTH surfaces so they cannot make different promises — the entry screen ABOVE the sign-in card (for a returning guest, restoring IS the primary action; typing a name mints a second identity and strands the first) and a Profile section for someone who typed a name before noticing, deliberately NOT on the `/i/<pin>` call-link screen, which is one focused field by design. **The card NEVER discards the stored key on a failure** — it is the only copy in existence and an empty lookup can mean a DB blip or a rate-limit, so forgetting is only ever an explicit "Not me" tap, pinned as exactly ONE `forgetGuestRecovery()` call that is not in the error path. Guest copy on the entry screen, in Profile and in the sign-out dialog corrected: a browser close no longer wipes the number, signing out still does, and the dialog says so. `server/guestRecovery.test.ts` (54); **all 10 tripwires verified by MUTATION**, each reverted from a byte-exact backup rather than git. TWO PRE-EXISTING PINS REWRITTEN TO THE STRONGER INVARIANT: the v2.99.49 mint-gate pin hardcoded the destructured names and broke when `recoveryKey` joined the statement — it now asserts the ADJACENCY plus that `createGuestIdentity` has exactly ONE call site; and the v2.99.43 M47 pin sliced to a bare `identities_user_unique` token, which this release mentioned in prose 1,000 lines earlier, putting the slice's end BEFORE its start and silently reducing it to `""` — a pin reading an empty string cannot fail for the reason it was written, so it now anchors on the migrator entry and asserts the slice is non-empty first. Both rewrites verified to still bite. 2190 tests.) (v2.99.68 — ROUND 11: THE SIGNALING ROOM REGISTRY SURVIVES THE LOSS OF THE LEADER. Clustered signaling elects ONE leader that owns the whole call registry — rooms, members, roles, the pin→room index — in memory; kill that process and every active call's room is gone FLEET-WIDE, so nothing can be renegotiated and a relayed call cannot recover. Verified absent before building (`relay:room:*` and `rejoin-recreate` were both 0 occurrences). **A — DURABLE SHADOW** (`server/roomStore.ts`): every room mutation marks the room dirty, a coalesced next-tick flush writes it (the `touchBusyState` discipline, so a handler crossing several funnels mid-transition writes ONCE, settled), plus a 15s sweep so a mutation site nobody marked still converges. `relay:room:<id>` is ONE hash — `e` (epoch) + `d` (signed JSON) — because a single atomic HSET means a reader can never see a half-written room; `relay:rooms` is a SET index so hydration is one SMEMBERS, not a SCAN. THE PIN→ROOM INDEX IS DERIVED, not stored as `relay:pinroom:<pin>` per the spec: a pin is in at most its ACTIVE and its HELD room and BOTH always contain it, so a `held` flag per member rebuilds both maps on the SAME atomic write, where separate keys would be a second write that can land out of order. FENCED: a lease can expire while its holder is alive and still believes it leads, and two leaders both writing would interleave two registries into one record — so every write carries a monotonic epoch (`INCR relay:leader:epoch` on winning) applied by a Lua CAS that refuses a LOWER one (`>`, not `>=`, so a leader can overwrite itself); losing the lease sets the epoch to 0, stopping writes at the source. HYDRATION GATES SERVING — a new leader mints, reads back, and only THEN processes signaling (a leader answering `accept` for a room it had not hydrated would tell the caller the call is gone); 5s timeout then it serves ANYWAY, fail open, because a missing room degrades to "dial again" while a wedged signaling layer means nobody can call at all. **THE BUG THAT WOULD HAVE MADE ALL OF IT A NO-OP**, found by reasoning through the behavioural test before writing it: a hydrated room has NO connected members by construction, which is exactly the "room of ghosts" shape `sendRejoinIfInRoom` exists to dissolve — the FIRST peer back would have called `leaveRoom` and deleted the very call hydration saved. New `roomMeta.hydratedAt` + `HYDRATED_GRACE_MS` (45s) holds it until its owners re-register, after which a ghost room behaves exactly as before. Records are HMAC'd with the fleet key (they cross the same trust boundary as bus envelopes and hydration feeds them into the live registry); `busSecret` is now EXPORTED from redisBus and imported rather than re-derived — a second copy of "which env var is the fleet secret" is what caused the v2.99.49 identity bug. **B — `rejoin-recreate`, REWRITTEN AROUND A SIGNED CAPABILITY.** The spec had the client send `{roomId, selfPin, selfRole, knownMembers}` and the server recreate-and-admit; that is an authorization hole, because room ids are relayed to every participant, so naming a stranger's roomId would admit you to their live call and `selfRole:"host"` would hand you kick/mute/admit over it — the exact class closed by v2.99.43 (M45) and v2.99.57 (R-GENPIN). The client is asked for ONE thing instead: a capability THIS FLEET minted when it admitted that pin to that room (`server/roomCapability.ts`, `<exp>.<role>.<hmac>`, 12h). Subject pin from the CONNECTION, role from INSIDE the signature, claimed member list not read at all — membership converges because every returning peer presents its own; a signed `host` capability takes a VACANT host seat and never displaces one, so concurrent recreators cannot fight over moderation; no fleet secret ⇒ the path does not exist rather than existing unauthenticated. **C — CLUSTER HYGIENE** (review findings 26/27, both confirmed real first): `makeRemoteSocket` returned only `{send, close}`, so `!target.socket.alive` short-circuited TRUE for every remote peer. A per-instance heartbeat (`relay:sig:hb:<leader>`, 5s, ONE frame per instance carrying its cid list) now drives a real `alive()` that FAILS OPEN twice over — unknown cid, and an instance that has never beaten at all (an older build mid-rollout) — because reporting a LIVE browser as dead sends its calls to the leave-a-message card; a home that misses 20s hands its browsers to the ORDINARY `cleanupRegistryConn` grace, not oblivion, since it may just be restarting. On a leader change each instance sends its local browsers `{type:"resync"}` → re-register → `rejoin` from the hydrated rooms (old clients ignore the unknown type). SECOND BUG CAUGHT BEFORE SHIPPING: the change hook is deliberately silent when the new leader is US, so the instance that TOOK OVER would have repaired every browser in the fleet except its own — it now resyncs them at the end of taking over, after hydration. The client keeps the capability beside `roomId`, refreshed by every room ack in ONE place so a future ack inherits recovery for free, persists it in the rejoin snapshot, and DROPS it on hang-up/destroy, which is what stops the repair path resurrecting a call the user ended; the recreate is ARMED (1.5s), not sent, so it only fires when the room is genuinely gone. `server/roundEleven.test.ts` (35, driving the real registry) + `server/roomStoreLive.test.ts` (7, against a REAL spawned redis-server — the fence is Lua and Lua runs inside Redis, so a string check of the script is not evidence it works; a lower-epoch write AND a lower-epoch DELETE are both proven refused). 10 tripwires verified by mutation, reverting from byte-exact backups and aborting on a missing target (never `git checkout --` — that discarded uncommitted work under test in v2.99.56 and produced three false PASSes); two of my first mutations were wrong rather than the tests, and one property turned out to be defended by three redundant gates — recorded rather than quietly counted as a pass. NO OPS REQUIRED: `RELAY_CLUSTER=1` + `REDIS_URL` are already live and every new key self-expires. 2108 tests.) (v2.99.67 — **THE PHONE-HEATING FIX, A VANISHING CONFERENCE TILE, THE WRAPPED DIAL PAD, AND THE MISSED-CALL BANNER.** **(1) THE LANDING PAGE MADE PHONES HOT** (owner: *"when I open this website from the phone, the phone is heating"*). MEASURED, not guessed — an emulated 390px phone at 4x CPU throttle against the real built bundle. The page ran TWO uncapped `requestAnimationFrame` loops (the fx loop and the WebGL scene), repainted a FULL-VIEWPORT canvas at device pixel ratio EVERY frame, rewrote SIX gradient/box-shadow style strings 20 times a second (`fc % 3` at 60fps — each pass re-parsing two 1100px `radial-gradient`s plus three box-shadows over most of the viewport), and did none of it conditionally on the tab being visible. FIXED: a frame budget (30fps desktop, 20fps on a low-power device), an early return when `document.hidden` (with the rAF re-armed BEFORE the return, or the loop would die on the first hidden frame), the chrome tint throttled by TIME rather than frame count (~4.5/s instead of 20/s, so the cost no longer tracks the frame rate and the slowly-drifting hue looks identical), the matrix canvas dropped from DPR 1.5 to 1.0 and from a column every 18px to every 26px on a phone, and THE WEBGL SCENE SKIPPED ENTIRELY on a low-power device (`innerWidth <= 820` OR `hardwareConcurrency <= 4` OR Data Saver). RESULT, before -> after on that emulated phone: canvas repaints **59.1/s -> 16.6/s**, backing store **585x1266 -> 390x844**, i.e. canvas fill **~43.7 Mpx/s -> ~5.5 Mpx/s (~8x less)**. HONEST LIMIT: the WebGL saving is asserted from the source gate, NOT measured — this headless environment runs `--disable-gpu` and reported no WebGL context in EITHER run, so the probe could not observe the scene either way. The `prefers-reduced-motion` path is untouched. **(2) A PARTICIPANT WHO TOOK ANOTHER CALL CAME BACK WITH NO TILE** (owner: *"on the conference call, somebody got a line, when he answer and he returned back, he disappeared… he keep hearing [him], but his profile is disappeared"*). ROOT CAUSE: `onPeerHold` restored a placeholder tile ONLY under `livekitEnabled`, so on the MESH the tile stayed gone; and the coming-BACK branch only stripped the `.on-hold` class, so if the tile had already left with the peer's transport there was nothing to un-hold. `addTile` could not help — it requires a live `peers[id]` entry, which is exactly what the teardown removed. New `ensurePlaceholderTile` makes a name-only tile on EITHER transport, marked `data-ph` so `addTile`/`addLkTile` REPLACE it instead of appending a second element with the same id; called on hold AND on return. **(3) THE LANDING DIAL PAD WRAPPED ON A PHONE** (owner screenshot: five digits on one line, the sixth below). Six digits joined by spaces is 11 characters; at 30px monospace with `.28em` letter-spacing that is ~290px against ~285px of inner card width at 390px — so it wrapped, and only on a phone, which is why it shipped. Now `white-space:nowrap` (structurally one line) plus `font:clamp(19px,6.2vw,30px)` and `letter-spacing:clamp(.13em,.6vw,.28em)`, whose maxima are the original values, so desktop is unchanged. **(4) THE MISSED-CALL BANNER IS GONE FROM THE MAIN SCREEN** (owner: *"don't show it on the main screen as a side banner from up to down. Show it only on the notification center on the top… and also on the history"*). `AwaySummaryToast` is no longer mounted (the component is kept, the import cleaned up). Nothing is lost: the bell still counts missed calls AND unread messages, still blinks for them, lists them in its panel, and routes to History / Messages — pull rather than push. **STILL OPEN:** the group-call tiles are STATIC PHOTOS and the owner has now twice asked for people who look like they are talking. Lips cannot move on a still image — that needs short looping video per tile, which is an asset decision, so it is flagged rather than faked. `client/src/app/ownerUiBatch3.test.ts` (17); two pre-existing pins REWRITTEN to the new intent rather than relaxed — the v2.97.1 hold pin now asserts the transport-agnostic restore in BOTH directions, and the v2.99.12 away-card pin now asserts the banner is unmounted while the bell keeps the count, the blink and the routes. 2066 tests.) (v2.99.66 — **OWNER UI BATCH FROM FIVE SCREENSHOTS.** **(1) LAST SEEN NOW CARRIES THE CLOCK.** `formatLastSeen` (shared/profileFields.ts) returned `last seen on Jul 23` for anything older than yesterday while the same-day and yesterday branches already included the time (owner: *"it shows you below last seen on this, but doesn't show you the time and the minutes"*). Every dated branch now ends `at H:MM AM`, and the YEAR is named only when it differs from now — "Jul 23" silently reads as this year, and being twelve months wrong without saying so is worse than one extra token. **(2a) ONE "+" REPLACES THE IMAGE AND PAPERCLIP BUTTONS** (owner: *"put the attachment and the image into one icon like you click a plus… it will give more space for the input box of chatting"*). The `+` always opens the existing attach menu, now carrying Record video (still gated on `videoRecorderSupported()`), Photo & video, and Attach file — so nothing was lost and the composer row gives ~44px back to the text field. NOTE the old handler opened the menu ONLY when a recorder existed and otherwise jumped straight to the library, which would have hidden "Attach file" entirely; it is now an unconditional toggle. **(2b) THE BELL AND MAGNIFIER MOVED INTO THE PEER PROFILE** (owner: *"for the search and for the notification, make it inside the profile of the person when you click on his name"*). As permanent header icons they squeezed the name to "Ibrahi…" and left the "last seen" line with nothing after it. Both are conversation-scoped, so `openPeerProfile(number, chat?)` now takes an optional `PeerProfileChatActions` ({onSearch, muted, onToggleMute, lastSeenText}); Messages passes it, and Contacts / History / the dialer open the same popup with none and are byte-identical. The popup also shows the FULL last-seen line with its clock, which the one-line header cannot fit. Only the close-search affordance stays inline, and only while search is open, because that one acts on what is on screen. **(3) LIVE COUNTERS ON THE SIGN-IN SCREEN** (owner: *"this is the live reads on the main website where I want also this one to be also on the login page… put it below the login and registration with the email, above the voice video chat"*). New `client/src/app/LiveStats.tsx` renders Registered / Guests served / Call parties / Messages / Online now from the same public aggregate `stats.public` endpoint, polled every 15s with the online figure pulsing. Placed exactly there. A new `messagesSent` COUNT(*) joins the four existing counters (wrapped in try/catch — `messages` is the largest table and a headline number must never stop the landing page rendering) and the landing page gains the same tile in EN and AR. Renders NOTHING rather than five zeros when the query has no data, since `getPublicStats` answers zeros on a dead DB and a wall of "0" on the sign-in screen reads as a broken product. **(4) THE AWAY AUTO-REPLY IS OPT-IN** (owner: *"you have it as a feature, but you should allow the user to enable and disable it. You don't enable it by default"*). It used to fire for everyone unconditionally. New `identities.autoReplyEnabled` (additive nullable via the boot migrator) — on the IDENTITY, not `users`, so a GUEST can set it and it travels with the person through registration and renumbering, consistent with v2.99.54. Only an explicit `true` enables it, so NULL turns the old behaviour off for everybody, which is the intended change. `autoReplyEnabledFor` FAILS CLOSED on any trouble (this posts a line in someone's name into a conversation they are not watching, so silence is always safer than guessing yes), and the pref is checked BEFORE the presence and dedupe reads so the common opted-out path costs one indexed lookup instead of three. Surfaced as a switch in the Messages header (`AutoReplyToggle`), optimistic with rollback so it cannot misreport its own state, plus `identity.setAutoReply` and an `autoReplyEnabled` field on whoami. **(5) CONTACTS PUT LAST SEEN BELOW THE PIN.** They shared one line, and with a 6-digit PIN plus "last seen 18h ago" there was never room — every row wrapped mid-phrase ("last seen" / "18h ago" on separate lines) and read as broken. Two short lines, the pin `dir="ltr"`-isolated so an RTL name cannot reorder it. `client/src/app/ownerUiBatch2.test.ts` (23) plus 2 new `formatLastSeen` cases; the v2.96.2 recorder pin was rewritten to the new menu shape rather than relaxed (it asserted the exact branch this batch deleted). 2049 tests.) (v2.99.65 — TURN :443 DIAGNOSED TO ITS TWO REMAINING OWNER-ONLY STEPS, plus the code change that unblocks the better fix. `turn-fix` (v2.99.63) ran and established: both relays are EC2 instances in this account (`i-0ccd35acc6940c5dc` = 13.232.119.83, `i-0cf65f64e50fa4e3d` = 13.204.23.58) and they SHARE ONE SECURITY GROUP, `sg-063532b4c358d1513` — which explains why :443 is dead on BOTH identically and means the network half is ONE rule, not two. It could not go further: `ec2:DescribeSecurityGroups` is denied to relay-github-deploy, and the relay hosts are NOT SSM-managed from this account, so coturn's listeners cannot be inspected or changed from CI. Both remaining steps are owner-only and the action now prints exactly which grant would let it finish. v2.99.64 fixed a bug of my own found by that first run: GitHub runs steps as `bash -e {0}`, so the DescribeSecurityGroups denial aborted the entire diagnostic before the listener check — the more informative half — ever ran; the step now sets `+e` explicitly and reports a denial as UNKNOWN rather than letting it read as 'not open', which would have been a finding this role was never able to make. NEW `TURN_TCP_ALT_PORT=off|none|0|false` suppresses ONLY the plaintext alt-port candidate, which is what makes TLS-on-443 possible: `turns:<host>:443` is indistinguishable from HTTPS and passes DPI, where plaintext TURN on 443 is precisely what DPI drops — and the two cannot share the port, so advertising both would point clients at a TLS listener in plaintext. Unset still means 443, so every existing deployment is unchanged. Also fixed the test helper: its KEYS list omitted TURN_TCP_ALT_PORT/TURN_TLS_PORT, so those leaked between cases and the assertions did not mean what they said. 2025 tests.) (v2.99.63 (v2.99.63 — `turn-fix`: find out WHY TCP/443 is dead on both TURN relays, and fix the part that is safe to automate. The v2.99.62 health check reported `turn:<host>:443?transport=tcp` DOWN on both relays while udp/3478, tcp/3478 and turns/5349 all allocate — which matters more than 2/8 suggests, because 443 is the candidate that exists for networks blocking UDP and non-standard ports: on exactly those networks the other three are also unusable, so those users get no relay at all. A timeout has two possible causes with different fixes (a security group dropping 443, or coturn not listening on it), so the action DIAGNOSES BY DEFAULT and `turn_apply` defaults to false — production networking is never changed on a guess. It reads TURN_HOSTS from the app fleet's own env (so it can never drift from what clients are told), resolves each to its EC2 instance BY PUBLIC IP rather than a tag (survives a rename), reports whether tcp/443 is permitted by each relay's security groups, and — if the relay hosts are SSM-managed — what is actually LISTENING on 443/3478/5349 plus their coturn listening-port lines. With `turn_apply=true` it opens tcp/443 on the relay security groups; that is the ONLY mutation it can make, gated behind the flag (pinned by a test asserting the gate precedes the write). It deliberately does NOT rewrite coturn's config: those hosts may not be managed by this role, and the better fix is TLS on 443 — `alt-tls-listening-port=443` + `TURN_TLS_PORT=443` — because TLS on 443 is indistinguishable from HTTPS and passes DPI, where plaintext TURN on 443 does not. The action prints that recipe when the security groups are already open. 2021 tests.) (v2.99.62 (v2.99.62 — TURN HEALTH IS NOW A REAL, IN-VPC CHECK, AND MULTI-TURN IS LIVE. `TURN_HOSTS=13.232.119.83,13.204.23.58` + `TURN_TLS=1` were applied to the fleet via two `env-set` runs; the live `/api/relay/ice` went 3 → 6 → **8 URLs** (both zones × udp / tcp:443 / tcp:3478 / turns:5349). `TURN_TLS` had never been set, so the `turns:` candidate was not advertised even for relay 1 despite the cert being live — found by reading the live output instead of trusting the config. NEW `scripts/turn-check.mjs`, wired into aws-ops.yml's `verify` and run ON an app instance over SSM. WHY THERE: the runner is outside the VPC, so a probe from it cannot distinguish 'relay down' from 'port blocked to me' — I hit exactly that trap first, reporting 0/3 endpoints dead, and the CONTROL disproved it (the known-alive public TURN server failed identically, answering `485454502f312e31` = ASCII "HTTP/1.1", i.e. this sandbox's proxy; and 1.1.1.1:53 / github.com:22 also time out, so :3478 and :5349 were never testable from here). The check does a REAL authenticated TURN Allocate, not a ping, because only that proves each relay's `static-auth-secret` matches `TURN_SECRET` — a mismatched relay answers STUN happily and then refuses every allocation, silently breaking calls for whoever is steered to it. It prints no secret, and releases each allocation with a zero-lifetime Refresh so a repeated health check cannot accumulate them. The STUN/TURN wire format (incl. the MESSAGE-INTEGRITY length quirk) was validated against a local fake coturn that independently RECOMPUTES the HMAC: correct-secret ⇒ Allocate success + relayed port; mismatched-secret ⇒ 'credentials REJECTED (err 401)' and exit 1; unconfigured ⇒ clean skip, exit 0. Real exit codes were re-measured WITHOUT a pipe after the first attempt reported `exit=0` through `grep` — the same pipeline-masks-status bug v2.99.46 exists to prevent. `deploy.yml` now ships `scripts/` in the release tar (it did not, so the instance had nothing to run), pinned in workflowPinning.test.ts — which also puts recover-orphan-identity.mjs where DATABASE_URL actually lives. 2021 tests.) (v2.99.61 (v2.99.61 — MULTI-RELAY TURN: the last wire in the symmetric two-zone build. The fleet now runs one coturn per AZ (13.232.119.83 in ap-south-1a, 13.204.23.58 in ap-south-1b, both TLS-live), but the server only ever told clients about ONE — the relay list is minted server-side at registration, so this was the single layer of the HA story infrastructure could not fix on its own: losing a zone took the relay path with it. New `TURN_HOSTS` (comma/whitespace list, precedence over `TURN_HOST`) advertises EVERY relay with every transport, all sharing one minted credential — correct because coturn `use-auth-secret` validates the HMAC against its own static-auth-secret, so any relay holding the same `TURN_SECRET` accepts it. Clients then gather relay candidates from both zones, so a zone loss leaves the peer already holding a candidate on the survivor rather than renegotiating blind. `TURN_TCP_HOST` is honoured ONLY for a single relay (the v2.92 two-L4-LB case); with a list it is ignored, because one global TCP override would point every relay's TCP candidates at ONE zone and quietly rebuild the single point of failure. Duplicates are deduped. The single-`TURN_HOST` output is byte-identical to before — opting in is the only thing that changes behaviour, pinned by a test asserting the exact legacy URL list. VERIFIED IN A REAL BROWSER before shipping: the resulting 11-entry ICE list (3 STUN + 4 URLs × 2 relays) is accepted by RTCPeerConnection and survives `getConfiguration()` intact — nothing silently truncated. `server/multiTurn.test.ts` (8); 4 of them verified to FAIL against the pre-change parser. 2021 tests.) (v2.99.60 (v2.99.60 — THE AFFINITY FIX VERIFIED LIVE + AN ORPHAN-IDENTITY RECOVERY TOOL. v2.99.59's proxy fix is CONFIRMED against production: the same probe that measured **12/24 POSTs returning 404** now returns **24/24 200s**. One consequence is recorded deliberately rather than discovered later — an instance that is not the home cannot distinguish 'homed elsewhere' from 'no such cid', so a DEAD cid now also gets 200 instead of 404. Nothing regresses (the client never acted on the 404; it retries blindly and the SSE close is what drives reconnect, and a dead channel's message was undeliverable either way) and it removes a cid-existence oracle, so the response is now uniform by design; the integration test asserts that shape so it stays a decision. Restoring the distinction would mean mirroring cid→home into Redis — ALB stickiness is the cheaper cure and stays the ops follow-up. ALSO: `scripts/recover-orphan-identity.mjs`, the recovery CLAUDE.md has described as 'designed but not yet built' since v2.99.54 — it moves a pre-v2.99.49 orphaned guest identity (the owner's 601-586) onto its account. DRY RUN BY DEFAULT; it refuses unless the orphan is genuinely unclaimed (`userId IS NULL`), the user exists, and the identity it would REPLACE is provably EMPTY across all seven identity-referencing tables — there is deliberately no override, because the only way this destroys data is by deleting a non-empty row. The write is one transaction whose statements re-check their own preconditions in the WHERE clause, so a concurrent change loses instead of corrupting, and re-running after success is a no-op. Two column names were WRONG in my first draft (`contacts.ownerId`, not `ownerIdentityId`; call_history splits into `callerIdentityId`/`calleeIdentityId`) — caught by checking the schema, and now caught automatically: a PREFLIGHT validates every table/column against information_schema before reading a single row, so a rename can never silently under-count the emptiness check. HONEST LIMIT: no MySQL was reachable here (docker client present, daemon not running), so only the argument/preflight guards were executed — the DB paths are unrun, which is exactly why the destructive half is opt-in behind --apply. 2013 tests.) (v2.99.59 (v2.99.59 — **HALF OF ALL SIGNALING MESSAGES WERE BEING DROPPED IN PRODUCTION.** Task #38 was a QA-sweep suspicion nobody had confirmed; it is REAL and I measured it against the live fleet: open one SSE stream, then POST 24 times for that same cid — **12 returned 404 `channel not found`**. The SSE stream and each `/api/relay/send` POST are separate HTTP requests, and the ALB round-robins them across the two instances with no stickiness, so the POST routinely lands on the box that holds no channel for that cid. `attachRelay`'s clustered POST path checked `localDelivery.has(cid)` and answered 404, dropping the message on the floor. `sendWS` retries 3× with backoff, so ~6% were lost OUTRIGHT and the survivors delayed up to ~3s — on the offer/answer/ICE path, which is call-fatal. This is very likely the substrate under a long tail of 'calls not ringing / dying seconds after dial' reports that earlier versions chased as client bugs. FIX: a misrouted POST is now PROXIED to the leader instead of refused (`clusterProxyInbound`). The proxying instance does not know the home and is not allowed to claim it — the frame carries `proxy:true` and the leader IGNORES the claimed `home`, routing the reply to the home it already recorded in `homeOf`. No recorded home ⇒ the frame is DROPPED rather than binding the cid to an instance with no stream, which would have black-holed every subsequent reply (that failure mode is its own test). The v2.99.49 anti-spoof rule (`f.home !== fromInstance` ⇒ drop) is untouched for normal frames, and a proxy frame is strictly safer because its claim is never read. `decodeInbound` normalises `proxy` to a boolean so a frame from a not-yet-updated instance is explicitly non-proxied during a rolling deploy. Pinned by an integration test that replays the exact production scenario — B's ACCEPT arriving as a POST on the wrong instance — and asserts the call still connects; **verified to FAIL against the pre-fix 404** before being kept. 2013 tests.) (v2.99.58 (v2.99.58 — PINNING NOW COVERS EVERY WORKFLOW, AND THE HAND-LIST WAS THE HOLE. `workflowPinning.test.ts` named deploy.yml + the two Android files, so `aws-ops.yml` sat OUTSIDE the rule — and it assumes the SAME production role as deploy.yml (`role-to-assume: …/relay-github-deploy`, `id-token: write`) while running an UNPINNED `aws-actions/configure-aws-credentials@v4`. A mutable tag was reaching the exact credentials the rule exists to protect; `ci.yml` (3 unpinned) was uncovered for the same reason. The covered set is now READ FROM DISK (`readdirSync` over `.github/workflows`), so a NEW workflow is covered the moment it lands instead of starting life silently exempt — verified by dropping a temp workflow with `@v4`/`@main` into the dir and watching the suite fail on it. All 5 workflows / 17 refs are now pinned with PRECISE version comments (`# v4.4.0`, not `# v4`). TWO MORE ANNOTATED TAGS found: `aws-actions/configure-aws-credentials` v4 (tag object `ff717079…` → commit `7474bc46…` = v4.3.1) and `pnpm/action-setup` v4 (tag object `f40ffcd9…` → commit `b906affc…` = v4.3.0) — so 3 of the 7 distinct actions use annotated tags and the `.object.sha` trap is the COMMON case, not the exception; both deploy.yml pins that already existed were verified to be correct dereferenced COMMITs. The annotated-tag guard now forbids all three tag objects. Also replaced a bad assertion of my own: the per-file `refs.length >= 4` floor was calibrated for the three big workflows and broke the moment aws-ops.yml (2 refs) and ci.yml (3) came into scope — an arbitrary floor was never the right guard, so it is now a PARSER CROSS-CHECK (the parser must see exactly the count an independent scan finds) plus one global floor. A stale `awsOps.test.ts` pin that asserted the FLOATING `@v4` was updated to assert the pinned form. 4/4 tripwires re-verified by mutation with backup-based reverts. 2011 tests.) (v2.99.57 (v2.99.57 — 20-EXPERT SECURITY SWEEP: 46 CONFIRMED FINDINGS, 20 FIXED IN FOUR BATCHES (owner: "get me 20 securies expert and scan the whole web apps for bugs or errors and fix it without my approvals"). 20 experts, one per vulnerability class/surface, each briefed with a recon map of the ~55 already-closed classes and 16 accepted residuals — without it the report would have been mostly noise, since the prior panel refuted 19 of 22 candidates purely because they were already fixed. 65 candidates raised, each judged by THREE independent skeptics with distinct lenses (reachability / is-it-still-true / write-the-exploit), all defaulting to REFUTED: 46 survived, 19 killed, 217 agents. **HIGHS FIXED.** **R-GENPIN (1,8,45)** — `genPin` excluded only `reg.clients`, so an anonymous register could be handed a real identity's number whose owner just had no live stream: multi-device ring fanned that victim's dials to the squatter, `deliverPendingRing` handed over a ring in flight, `sendRejoinIfInRoom` dropped them into a live call with the roster and a LiveKit token, and the ring card rendered the VICTIM's name/avatar/badge (the caller is resolved BY PIN). Fixed in two layers — `genPin` now excludes `pinRoom`/`heldRoom` and uses `crypto.randomInt` (v2.99.20 #9 missed this second minting site), and `pinIsAddressable` makes an unverified registration un-ringable/un-rejoinable; the rejoin gate is `verifiedClaim(pin) || pin === effectiveOwned`, because `verifiedClaim` alone would strand an ordinary reload whose `createContext` hiccupped. **Finding 2** — the S2 gate counts a HELD room as shared and is evaluated from the SENDER's side, but the relayed frame carried no room: a peer whose call you PARKED could hand-craft a `signal`, land in `onSignal` with no matching peer, and have `createPeer` built around your CURRENTLY live stream — mic AND camera, since `createPeer` flips `callIsGroup` which makes `consentOk` true. A total mutual-consent bypass. The server stamps the authorizing room; the client routes on it via a new pure `signalDisposition` that deliberately does NOT gate the ACTIVE-room case on a client roster (the server already proved membership, and a roster check would refuse legitimate mesh offers arriving before `peer-joined`); unstamped frames fail OPEN. **Finding 7** — the boot-URL guard required a digit right after `/i/` while App.tsx strips EVERY non-digit, so `/i/x555555` was invisible to the guard and one-click dialable: M48 for the THIRD time, a guard parsing differently from the code it guards. Now uses the consumer's own normalization, with a test asserting the two AGREE on every input. **Finding 4** — ReDoS: M42 bounded the same quadratic regex in `parseInboundAddress` only, and `normalizeEmail` runs it over the same headers on a 5MB route; the cap now lives IN the function so a later caller inherits it. **Findings 3/5+9/6** — three surfaces had a RATE limit but no OCCUPANCY limit (media streams, contacts, push fan-out), any of which OOMs the `instances: 1` 1GB process that owns every call. Fixed with an in-flight ceiling released from ONE idempotent handler, a HEADER-PHASE-only upstream timeout (an abort signal also kills body streaming), an IDLE stall watchdog (wall-clock would break `<video>` seeking), an insert-RANK contact cap that never refuses an UPDATE (a user at the ceiling must still be able to BLOCK someone), a `listContacts` limit EQUAL to the cap instead of pagination (the client sorts over the whole list, so a short page would silently HIDE contacts), oldest-first push eviction gated on the row being ours, and a fixed-size send pool. **Finding 38** — `MAX_STREAMS_PER_IP = 25` was documented as "far above any legitimate device count" and was not: per-IP, two streams per tab, so ~12 tabs across a CGNAT population exhausted it and lost calling entirely; raised 10x with the open-rate limiter left as the flood defence, plus an off-by-one where a tab refresh was refused by its own superseded stream. **MEDIUMS FIXED**: moderation reaching HELD members and acting on their OTHER call (kick called `leaveRoom`, which acts on the target's ACTIVE room — dropping an unrelated call while leaving them a member here); no host succession when the host's SSE is grace-reaped (M53 covered `leaveRoom` only); `status.feed` missing "I blocked them" so a blocked person's status TEXT leaked while its media 403'd; over-cap view-once media burned before the size check and reported as success; `GET /api/auth/verify` mutating, so a mail security gateway detonating the link verified the account before the recipient opened it — plus M29's THIRD claim site, now clearing a credential set before the address was proven. LOWs: `deleteMessage` made an atomic claim (concurrent unsends each decremented other participants' STORED unreadCount, corrupting it permanently) and `messages.typing` block-gated per recipient. Eight stale pins rewritten to the stronger invariant; 41 new pins verified to FAIL against the pre-change sources. REMAINING, told plainly rather than quietly dropped: the OTP-register family, the two clustered-mode findings, `regenerateIdentityNumber`'s out-of-transaction read, the mint budget as a fleet-wide onboarding kill switch, the inbound-email block gate, `messages.list` byte budget, sign-out not removing the push subscription, DND on contact-online pushes, the FCM re-bind claim, `aws-ops.yml` pinning, and `directoryGate` charging one token for a 100-number `presenceMany` — all enumerated in todo.md. 2000 tests.) (v2.99.56 — ACTION PINNING CLOSED: the last three floating `@v4` refs in the two ANDROID workflows are now commit SHAs, so `PENDING_SHA` in `server/workflowPinning.test.ts` is EMPTY and every action in all three credential-holding workflows (deploy.yml + android-apk.yml + native-rn.yml) is pinned. The v2.99.55 note said resolving these needed upstream repo reads this session couldn't do — that was true of the GitHub *API* (api.github.com returns 403 for anything outside this repo's scope, and there is no `gh` CLI here) but NOT of the git protocol: `git ls-remote https://github.com/<owner>/<repo> refs/tags/v4 'refs/tags/v4^{}'` is unscoped and answers the question BETTER, because one call returns both the ref and — for an annotated tag — the commit it dereferences to. Resolved: `actions/setup-java` v4 = v4.8.0 = `c1e323688fd81a25caa38c78aa6df2d33d3e20d9`; `actions/upload-artifact` v4 = v4.6.2 = `ea165f8d65b6e75b540449e92b4886f43607fa02`; `gradle/actions/setup-gradle` v4 = v4.4.3 = `ed408507eac070d1f99cc633dbcf757c94c7933a` (15 `uses:` lines across the two files). **THE ANNOTATED-TAG TRAP MATTERED**: gradle/actions v4 is an ANNOTATED tag, so its ref is a tag OBJECT (`0b6dd653ba04f4f93bf581ec31e66cbd7dcb644d`) and the commit is `ed40850…` — the suggested `gh api …/git/ref/tags/v4 --jq .object.sha` would have returned the tag object, putting a NON-COMMIT sha in `uses:`; a new test asserts that object sha appears in no workflow. The two pre-existing pins were verified against upstream at the same time: `actions/checkout@11d5960…` and `actions/setup-node@49933ea…` are both genuinely v4.4.0 and both are what v4 currently points at. PENDING_SHA is kept as an EMPTY mechanism (not deleted) so a future unresolvable action gets enumerated rather than exempting a whole file, plus a new guard that FAILS if anyone re-adds an entry. All five tripwires were re-verified by mutation (unpin one ⇒ fail, add a PENDING_SHA entry ⇒ fail, pin the tag object ⇒ fail, `@main` ⇒ fail, SHA with no `# tag` comment ⇒ fail); the FIRST verification run was invalid and was redone — it reverted mutations with `git checkout --`, which discarded the uncommitted work under test and made three cases report a false PASS, so the harness now reverts from byte-exact backup copies and aborts if a mutation target is missing. 1932 tests.) (v2.99.55 — STATUS AUDIENCE (EVERYONE / CONTACTS ONLY) + GRADLE SIGNS THE ANDROID RELEASE. Three owner asks. **(1) STATUS AUDIENCE**: two options, and the decision that carries the feature is that **the audience is a property of the POST, not of the poster** — new `statuses.audience` is stamped at insert from the new `identities.statusAudience` default, and every gate reads the ROW. Reading the owner's CURRENT preference instead would mean flipping your default to "everyone" retroactively republished every contacts-only story still inside its 24h window; per-post storage also gives the composer a free override that does not rewrite the standing default. `normalizeStatusAudience` FAILS CLOSED — anything that is not the literal `"everyone"` (NULL, `"EVERYONE"`, `" everyone"`, a future value) resolves to `contacts`, and the browser copy is pinned to agree with the server on every input. NULL means contacts on both columns, i.e. exactly the rule every existing row was posted under, so the migration is a no-op until someone opts in. Blocks outrank the audience (both `isNumberBlockedBy` checks run BEFORE the everyone short-circuit, pinned by index comparison). **"Everyone" is deliberately NOT a broadcast**: `getStatusAudienceIds` stays bounded to contacts + savers because its reverse is every identity in the database, so it is an AUTHORIZATION widening plus a PULL surface — new `status.forNumber`, used by the profile popup, which already holds the number. Without that surface the setting would have been authorized-but-invisible, since the feed is bounded; it answers an unknown number and an unauthorized one IDENTICALLY (empty, not an error) so it is no existence oracle, and is `statusGate`-limited before any DB work. The storage proxy still refuses an ANONYMOUS request even for an "everyone" status — v2.99.14 exists so a media URL cannot be opened outside the app, and relaxing it would hand back the shareable link that lockdown removed. `audience` is on the wire only for your OWN statuses (the v2.99.40 #4 lesson). UI: a per-post picker in the composer + a Profile → Status privacy radio group (guests included — statuses hang off the identity), both rendering from ONE module (`client/src/app/statusAudience.ts`), because v2.99.49 was a duplicated rule and duplicated COPY is worse: nothing fails when two screens promise different things. `statusAudience.test.ts` (27), with three mutations run to prove the pins bite. **(2) GRADLE SIGNS THE RELEASE, jarsigner only VERIFIES** — finishes v2.99.52. Adversarially reviewed first (SAFE_WITH_CHANGES), which caught: the plan would have turned CI red on commit one (a test pinned the exact substring being deleted); a ZERO-BYTE keystore satisfies `File.exists()` and is what an absent CI secret decodes to (`base64 -d` of empty exits 0 — verified), so an exists()-only guard would fail every secretless run — closed with `test -s` plus `isFile() && length() > 0`; silent-unsigned was a real regression versus jarsigner's loud failure, so `RELAY_REQUIRE_SIGNED` (set only when CI staged a keystore) throws naming the missing part; `RELAY_KEYSTORE_PATH` must be ABSOLUTE because `file()` resolves relative against the app MODULE dir; and the `RELAY-RN-release-aab-SIGNED` artifact NAME is the contract the README runbook depends on, so it is kept though the mechanism moved. Verification uses the right tool per format — `apksigner` cannot read an AAB, and `jarsigner -verify` can exit 0 on an unsigned archive, so the deterministic gate is `unzip -l | grep META-INF/*.RSA`. Real upside: Gradle signs the APK via apksigner (v1+v2+v3), so a signed run finally yields an INSTALLABLE release APK (jarsigner gives v1 only, which does not install on Android 11+ at targetSdk 36). **`android-apk.yml` is deliberately NOT converted**, against the system-wide rule: its TWA and Capacitor modules have no `signingConfig`, so removing jarsigner there would leave those releases unsigned with no signer anywhere. 8 → 16 tests, all 10 new pins confirmed to fail against the pre-change files. **(3) SHA-PINNED the two Android workflows** (they hold the keystore secrets). checkout + setup-node pinned; three actions remain on `@v4` because resolving a tag needs read access to those upstream repos, which this session does not have. Rather than exempt the files wholesale, `workflowPinning.test.ts` covers them with an explicit `PENDING_SHA` set of exactly those three — a fourth fails, a stale entry fails, a BRANCH ref fails regardless, and `deploy.yml` gets no exemption; three tripwires verified. 1930 tests.) (v2.99.54 — **IDENTITY CONTINUITY: DATA IS WELDED TO THE PERSON, NOT TO THEIR NUMBER** (owner, after the v2.99.49 data-loss fix: *"what I care is that it should not be repeated again to any other users... if you were a guest and you have data and you decide to register, you should not lose your data. It will move with you whenever you are moving. You can regenerate the number, but you will not lose your data. But your number is hooked to other contact lists — it will be updated automatically. So you need a mature system, no glitches"*). THE STRUCTURAL INSIGHT: RELAY names a person TWICE. The **identity row** is referenced by numeric id — contacts, messages, thread membership, call logs and statuses all point at it, so they follow the person through every transition with nothing to migrate. The **6-digit number** is what OTHER people store, and every place it is stored is a COPY THAT CAN ROT. Registration lost the owner's data because the upgrade could not find their identity row (v2.99.49); renumbering rotted History because the propagation guarantee lived inside one function that happened to know about `contacts`. Same root cause, two symptoms. **(1) NUMBER CONTINUITY.** `conference_history.dialedNumber` is what `History.tsx:721` uses for the call-back button, so after anyone regenerated their number that button dialled a number they no longer owned (dead, never a stranger — the reservation ledger never recycles); the same stale value was DISPLAYED as "PIN NNNNNN" as if it were fact, the presence dot looked it up and stuck grey forever, and the avatar was resolved BY NUMBER so a renumbered person silently lost their photo from everybody's History. `conference_participants.number` and the JSON roster froze every participant's number at call time. New `NUMBER_BEARING_COLUMNS` in `server/v2db.ts` declares EVERY schema column holding a 6-digit number and how it stays correct (`identity` = source of truth, `renumber` = rewritten in the transaction, `live` = never rewritten but resolved from the identity at read time, `not-a-person` = a party line's own number, which a person renumbering must never touch); `regenerateIdentityNumber` now also moves `conference_participants.number` scoped by `identityId`; and `calls.conferenceHistory` resolves the roster BY `identityId` (live number, name, avatar) and maps the frozen `dialedNumber` through the roster to a live call-back target — so renumbers that ALREADY happened come out correct with no backfill. `server/numberContinuity.test.ts` reads `drizzle/schema.ts` and FAILS THE BUILD if any number-bearing column has no declared strategy (verified by planting a fake column and watching it fail, naming the column) — so the guarantee cannot rot the next time someone adds a table. **(2) THE AUDIT'S FINDINGS AGAINST MY OWN v2.99.49 FIX.** A 51-agent adversarial audit red-teamed it and three findings landed. **MY TESTS DID NOT TEST WHAT THEY CLAIMED**: the test named "the PIN / legacy sign-in path too" anchored with `indexOf("await ensureUserIdentity({")`, but that substring first occurs 17 characters INSIDE verifyOtp's own `const identity = await ensureUserIdentity({` — so it re-read verifyOtp and never looked at the PIN site 5,626 chars later. PROVEN by reverting the PIN path to the pre-fix cookie-only shape (the exact bug that cost the owner their data): all 20 tests still passed. A test that cannot fail when its bug returns is worse than no test, because it reports safety. Rewritten to ENUMERATE every `ensureUserIdentity` call per file, assert the COUNT as well as the contents, and — the guard that makes the per-file tests exhaustive rather than a hand-maintained list — walk every server source and assert the complete set of minting sites; each rewritten test was then verified to FAIL against its reintroduced bug. Also vacuous: "minting is the LAST resort" compared two `indexOf` results, and on pre-fix code the candidate loop was absent (-1) while allocate was at 959, so `-1 < 959` and it PASSED against the code it was written to reject (both indices are now required to exist first). **A FIFTH MINTING SITE WAS MISSED**: `/api/oauth/callback` still passed the cookie ONLY — the original bug shape on a still-mounted route — and was the worst of the five, because without `resolvedIdentityId` the stranded-guest warning could not fire either, so it stranded people SILENTLY. **THAT SITE ALSO DESTROYED THE RECOVERY EVIDENCE**: it ran `clearCookie` unconditionally, "regardless of whether the migration happened" — and the guest token is the only half of guest identity that survives a browser close, so dropping it after a failed claim converts a RECOVERABLE orphan into a permanent one. The cookie is now cleared only when the identity we ended up with really IS this browser's guest row (resolved BEFORE the claim, since a successful claim nulls the token), and a non-adoption warns instead of being silent; `isGuest` is deliberately NOT the test, because it is equally false for a freshly minted identity, i.e. for exactly the failure case. **THE CLAIM WAS UNGUARDED AGAINST THE PER-USER UNIQUE INDEX** while the mint path directly below it handles that race: an uncaught throw there surfaces as a 500 from `verifyOtp` AFTER `consumeOtp` has burned the code, so the user is told their code was already used and must request another for a registration that half-completed — the claim now resolves to the race winner, like the mint path. **(3) THE VIDEO-CONSENT PROMPT** (owner screenshot, mid-call after enabling the camera): the "X wants to start video" card ran off the RIGHT edge — name cut to "a Hasan", button to "Turn on vide" — while colliding with the Minimize/Fit chrome and the "Connected" status, reading washed-out over live video. ROOT CAUSE MEASURED IN HEADLESS CHROMIUM, not guessed: the card centred with `transform:translateX(-50%)` and entered with `animation:relayFade … both`, whose final keyframe is `transform:none` — fill-mode `both` makes that PERSIST, so the centring transform was wiped and the card's LEFT edge sat at exactly 50% of the viewport (390px: left 195, right 429 = 39px off-screen; 375px: 47px off; 320px: 74px off; 1280px desktop: just fits, which is WHY it shipped — broken only on phones). The computed transform read `matrix(1,0,0,1,0,0)`, the identity, which is the direct proof. Positioning no longer involves `transform` at all (`inset:0` + `margin:auto` + intrinsic size against `.relay-root`, which is `position:fixed;inset:0`) and the entrance animates OPACITY ONLY; it is opaque instead of translucent-over-blur, with the surroundings dimmed by a spread `box-shadow` (not hit-tested, so hang-up stays tappable). THIS TRAP WAS ALREADY FOUND AND FIXED ONCE for `.addpad`, whose comment spells it out verbatim, and never swept — so this was the same bug in a second place; `client/src/lib/videoAskCentring.test.ts` now guards the WHOLE stylesheet, failing the build if any element centred with `translateX(-50%)` applies `relayFade`, proven to fire against the pre-fix shape and proven not to flag `.call-waiting`/`.held-bar`, which restate the transform in their own `cwIn` keyframes. **(4) DEAD CODE AND DEPENDENCIES REMOVED** (owner: "delete whatever not ours and it's zero value"): 12 files, 4,292 lines, 6 packages, each verified unreferenced by a precise import check and by confirming no test touches it — the Manus template helpers `_core/dataApi|heartbeat|imageGeneration|llm|map|voiceTranscription`, `DashboardLayout` + its skeleton (only the former imported the latter; nothing imported either since v2.92 removed the OAuth sign-in it served), `Map.tsx`, `ComponentShowcase.tsx`, `data/landingCopy.ts` (superseded — `Home.tsx` carries its own copy and `scripts/gen-landing-copy.mjs` regenerates it), `shared/types.ts`; plus `ws` + `@types/ws` (POINTED: this architecture DELIBERATELY refuses WebSocket, since the gateway downgrades the upgrade — it was a dependency for the one thing the rules forbid), `@types/google.maps`, `framer-motion`, `add` (the `pnpm add add` typo) and `pnpm` as a devDependency (redundant against the `packageManager` pin that `ci.yml` itself calls "the single source of truth", and contradictory: `^10.15.1` vs the pinned `10.4.1`). DELIBERATELY KEPT: the unreferenced shadcn/ui primitives, because CLAUDE.md mandates shadcn for new UI and they tree-shake out of the bundle — pruning the sanctioned kit is an architectural call, flagged not taken. **STILL OPEN, told to the owner plainly**: their 601-586 identity is still orphaned (the audit confirmed it retains BOTH its guest token and device id, and the empty row has no device id, so it IS still resolvable — recovery is designed but not yet built); guest identity is SESSION-SCOPED by construction (both halves die on browser close), which is the architectural reason data does not yet "move with you" in every case; and the verification email hitting spam is mostly domain reputation (headers, multipart/alternative and copy were all audited CLEAN — my initial theory was wrong). 1879 tests.) (v2.99.53 — THE ANDROID WORKFLOW CATCHES UP WITH THE UNSIGNED RELEASE. v2.99.52's Gradle change WORKED — CI reported `BUILD SUCCESSFUL in 35m 16s` with assembleRelease, packageRelease and bundleRelease all green — but the job failed one step later, and the error is itself proof the fix took effect: `No files were found with the provided path: .../apk/release/app-release.apk`. AGP names the output `app-release-unsigned.apk` with no signing config and `app-release.apk` with one, so the workflow's hardcoded signed filename matched nothing and `if-no-files-found: error` failed the job. Fixed with a GLOB (`app-release*.apk`) that is correct both with and without a keystore, rather than swapping one hardcoded name for the other. ALSO corrected a now-false instruction that would have wasted someone's time on a device: that artifact's comment said "debug-keystore signed … Prefer THIS one for the QA-TEST-PLAN §F pass", but an UNSIGNED APK cannot be installed at all — §F must use `RELAY-RN-debug-apk` (side-by-side as `org.yourchat.relay.next`) or a build with `RELAY_KEYSTORE_*` set. `androidSigning.test.ts` grows to 8, pinning the glob and forbidding the stale claim. The AAB path is unchanged by signing and its upload never ran in the failed job, so the next run is what confirms it. 1848 tests.) (v2.99.52 — THE ANDROID RELEASE BUILD IS NEVER SIGNED WITH THE DEBUG KEY. `mobile/native/android/app/build.gradle` shipped the RN template default `release { signingConfig signingConfigs.debug }` — two problems, not one: a local/CI `assembleRelease` produced a DEBUG-SIGNED artifact that looks shippable, AND `native-rn.yml`'s `jarsigner` ADDS the upload signature rather than replacing the debug one, so the bundle handed to Play carried TWO signers (Play can reject that; the earlier "CI re-signs it so the store artifact is fine" was optimistic). Release now signs with a REAL keystore when configured (env vars as CI would pass them, or gradle properties locally) and is otherwise UNSIGNED — an unsigned artifact cannot be mistaken for a store build, and jarsigner then yields a single correct signature. Path+password+alias must ALL be present and the file must exist, so a half-configured environment gives a visible unsigned build rather than an invisible debug-signed one; no keystore or password is committed. `androidSigning.test.ts` (6) verified BOTH ways — passes on the fix, fails 4 of 6 against the pre-fix file. Source-pinned because there is no Android SDK in the unit env; the real Gradle build runs in `native-rn.yml`, which triggers on any push touching `mobile/native/**`, so the commit verifies itself. Deliberately NOT restructured further (letting Gradle sign from the same secrets and dropping jarsigner is cleaner but a bigger change to a store-release-only path — available on request). Also confirmed v2.99.51's SHA pins resolve in a real deploy run, which independently proves the owner's tightened OIDC trust policy still lets `main` deploy. 1846 tests.) (v2.99.51 — THE PRODUCTION-CREDENTIAL JOB NOW PINS ITS ACTIONS TO COMMIT SHAs. `deploy.yml` assumes the production deploy role via OIDC, so every action it runs holds credentials that can write the S3 release bucket and drive SSM on the live fleet — and all four sat on the MUTABLE major tag `@v4`, a pointer the action's owner can repoint at any commit with NO diff in this repo to review. Pinned to full SHAs (owner-supplied), tag retained as a trailing comment. `server/workflowPinning.test.ts` enforces it — every `uses:` in a credential-holding workflow must be a 40-hex SHA, never a branch or floating tag, and must carry its `# <tag>` comment; the tripwire was verified to FIRE by reverting the pins and watching it name all four offenders. Scoped to `deploy.yml` for now (`android-apk.yml`/`native-rn.yml` hold the Android keystore secrets and are the documented next step; a failing test there would block deploys rather than fix anything). The same test ALSO pins that the release tar still ships `ecosystem.config.cjs`, `patches/`, `shared/`, `drizzle/` and that the Manus-runtime strip survives — not a pinning concern, but `deploy.yml` is precisely the file someone hand-edits to change SHAs, and an OLDER revision of it is in circulation that drops those; each was added after a real per-server failure (no `ecosystem.config.cjs` ⇒ pm2 starts a stale path with no entry file; no `patches/` ⇒ the server's `pnpm install --frozen-lockfile` ENOENTs on the lockfile's patched-dependency reference), so a future edit that loses them fails the suite instead of the fleet. OPERATOR ITEM CLOSED by the owner: the deploy role's trust policy is scoped to `repo:khalifa1982/relay-chat-video3:ref:refs/heads/main` rather than `repo:…:*` (it uses `StringLike` where `StringEquals` would be stricter by construction — same behaviour today, but `StringLike` can quietly become permissive if the value is edited). 1840 tests.) (v2.99.50 — THE SERVE SET NOW MATCHES WHAT A PHONE HANDS BACK. Closes the one surviving item from the red-team pass over today's fixes (its panel refuted all 22 candidates — 19 were already fixed in v2.99.47/.48 while it ran, so the triggers no longer reproduced — and left one LOW kernel, which I re-confirmed against source myself). TWO GATES I WROTE TODAY DISAGREED: M38's `INLINE_SAFE_TYPE` in storageProxy serves only a small allowlist as itself and downgrades everything else to `application/octet-stream` + `Content-Disposition: attachment`, and v2.99.45's self-review checked that list against every type RELAY's own encoders PRODUCE — but the avatar and Status pickers upload the RAW `File` with no re-encode (`uploadBare`) and the upload door only tests `^image/`, so an iPhone `image/heic` or an Android `video/3gpp` stored fine and was then served as an opaque download. Fixed by widening the SERVE side (image/heic|heif|tiff|apng, video/x-m4v|3gpp|3gpp2|x-matroska|mpeg, audio/flac|wave|x-wav|3gpp) rather than narrowing the door, which would have REJECTED a legitimate photo — the exact class of regression this run has been undoing. Impact is honestly LOW (every avatar surface already falls back to an initials disc, `Content-Disposition` is ignored for `<img>`/`<video>` subresource loads, and HEIC doesn't decode in Chrome/Firefox anyway), but two gates disagreeing is a latent trap regardless. The load-bearing addition is a PROPERTY test: `INLINE_SAFE_TYPE` can never admit a markup or script media type (text/*, the XML family, XHTML, SVG, the JavaScript types, octet-stream), plus a structural check that the pattern never mentions the `text` top-level type — so the list is free to grow under the invariant M38 exists to hold. Also corrected a factual error from the parallel session's bulk renumbering: `repoHygiene.test.ts` claimed v2.99.49 shipped with conflict markers; that was v2.99.45. 1835 tests.) (v2.99.49 — **REGISTERING AS A GUEST NO LONGER CHANGES YOUR NUMBER OR ORPHANS YOUR DATA** (owner report with a two-screenshot comparison: they used RELAY as a guest on **601-586** — saved contacts, exchanged messages, made calls — registered with their email, verified the code, and landed on **737-582**, a brand-new number, with an empty account; verbatim: *"the data should be stored and attached to the old number, and the number will not change unless if I request to generate a new number"*). ROOT CAUSE — TWO FUNCTIONS DISAGREED ABOUT WHICH IDENTITY A BROWSER IS USING, and one of them allocates: `createContext` resolves a guest by guest cookie **OR** device id and documents that the DEVICE ID WINS when they disagree ("cookies are a hint, device id is the truth" — that rule exists precisely so a cleared / expired / ITP-dropped cookie doesn't cost a guest their number), while `ensureUserIdentity` — the upgrade that runs at registration — looked the guest up by **cookie token ONLY**. So for every browser whose live identity was device-resolved rather than cookie-resolved, the upgrade found nothing, fell through to its allocate-a-fresh-identity branch, and minted a new 6-digit number; the guest row (with the number, contacts, messages and call history hanging off its id) was left behind with `userId` still NULL and no way back to it. FIX (`server/v2db.ts`): `ensureUserIdentity` now takes `resolvedIdentityId` + `deviceId` and tries candidates in the RESOLVER'S OWN ORDER OF AUTHORITY — the identity the request already resolved (device-id-aware, so it agrees with every other request in that browser by construction), then the cookie token, then the device id — deduped through a `Set`, with each optional lookup individually try/caught so a transient DB hiccup can never be the reason someone gets a new number. Each claim is ONE conditional UPDATE gated `WHERE id = ? AND userId IS NULL`, verified by `affectedRows`, so it can only ever adopt an UNCLAIMED guest row and can never take an identity that belongs to another account (a refused candidate just moves to the next; `getIdentityByDeviceId` independently filters to `userId IS NULL` as a second guard). The claim sets `userId` / name / clears the cookie fields and **never touches `number`**, then re-reads by the SAME id — so the number, contacts, messages and history all simply carry over. Minting is now genuinely the last resort. ALSO — the same hint-blindness existed at **three other identity-minting call sites**, all fixed system-wide (repo rule): `createContext` itself mints on ANY request when a signed-in account has no identity yet (it now passes `deviceId`), `POST /api/auth/register` (the legacy password route, now passes `deviceIdFromRequest(req)`), and the PIN / legacy sign-in path in `v2routers.ts`. The header's shape rule moved into a new dependency-free `server/deviceIdHeader.ts` (`DEVICE_ID_HEADER`, `normalizeDeviceId`, `deviceIdFromRequest`) that `_core/context.ts` now delegates to — a second copy of that rule is what caused this bug, and `context.ts` ↔ `authLocal.ts` already import each other so a shared module was also the only cycle-free way to do it. The `existingByUser` early return now `console.warn`s when it leaves a guest identity unclaimed (re-registering an address that already has an account is the one case where a guest session really is stranded — it should not be silent). SECOND HALF OF THE OWNER'S ASK — *"even if I generate new number and my numbers and others people contact, it will automatically update"* — ALREADY SHIPPED and now pinned by this release's tests: `regenerateIdentityNumber` moves the identity and rewrites every contact row that saved the old number **inside one transaction** (all-or-nothing, so no contact is left dialling a number that is no longer that person), via `planRenumber`, which also drops a stale duplicate when the saver already had a row for the new number. A test asserts there is exactly ONE `.update(identities).set({ number: … })` in the whole codebase, so no future path can renumber someone silently. `server/guestUpgrade.test.ts` (20 — candidate order, the unclaimed-only gate, number/data preservation, all four call sites, behavioural `normalizeDeviceId` coverage incl. the repeated-header and injection-shaped inputs, and the regenerate-propagation pins). NOTE for the owner: their 601-586 row still exists with `userId` NULL — recovering it is a one-time DB operation (delete the empty 737-582 identity FIRST, because a unique index allows only one identity per user, then set 601-586's `userId` to their account), not something this code change does retroactively. ALSO IN v2.99.49 — THE FIVE ACCEPTED SECURITY RESIDUALS ARE CLOSED (owner: "close everything pending") + the desktop status composer. Each residual was accepted because the obvious fix broke something, so each was investigated against current source before any change — and THREE of my intended designs were corrected by that pass. **R1 PUSH ENDPOINT RE-BIND**: the upsert re-bound `identityId` keyed on the globally-unique `endpoint` alone, so anyone who learned a victim's endpoint could silently kill their notifications. My first design (bind to the device id) was REFUTED with evidence: that id is sessionStorage-scoped AND reset by `useSignOut`, so it would have refused 100% of legitimate account switches — the exact breakage the residual predicted. Shipped instead: a per-browser CLAIM secret in localStorage (deliberately NOT cleared on sign-out, because it identifies the browser profile rather than an identity), stored server-side only as a sha256 hash; the insert's ODKU is now a no-op and the re-bind is one conditional UPDATE with its ENTIRE gate in the WHERE (already-ours OR claim match OR a legacy claimHash-IS-NULL row on an encryption-keys match, which an endpoint-only attacker cannot produce and which stamps a claim so a row is legacy exactly once); the verdict comes from a RE-READ, not affectedRows; a refusal is a pure no-op; and the client SELF-HEALS by rotating to a fresh endpoint on `owned: false`, which is what makes a strict gate safe to ship. **R2 LEAKED NUMBER RESERVATIONS**: the ledger's MONOTONICITY is load-bearing (a handed-out number must never be recycled), so a "delete rows nothing references" reaper would have been wrong; shipped a `claimedAt` marker (no DEFAULT, or the reaper would be a permanent no-op) stamped at all four allocator sites, guarded release on the two reachable failure paths, and an hourly reaper needing FOUR independent conditions including a hardcoded EPOCH FLOOR that replaces the backfill I had planned (a NULL on a pre-release row means "unknown", not "leaked"). **R3 REDIS BUS AUTHENTICATION**: HMAC signing whose wire format stays a FLAT `{i,p}` object with the signature in EXTRA fields, because the old decoder ignores extras — any other shape would make every not-yet-updated instance parse-fail and drop 100% of real events during each rolling deploy; unsigned is accepted until `REDIS_BUS_STRICT=1`, with `/api/health` reporting `busAuth` counters so the flip happens on evidence, and the leader now also refuses a frame whose `home` differs from its publisher. **R4 PER-ACCOUNT PASSWORD LOCKOUT**: its OWN columns (sharing the PIN's would let a password brute-force lock out PIN sign-in — a cross-channel DoS), a slot claimed BEFORE the secret is tested so the cap bounds scrypt work, no dependence on SET emission order, three independent escape hatches, a uniform 401 so it is not an oracle, and deliberately NO alert email (an email-bomb primitive). **R5 UPLOAD ORDERING**: a pre-parse gate so a throttled — or wholly ANONYMOUS — request no longer costs 41MB of buffering, refusing with `Connection: close` rather than a socket destroy. ALSO the owner's desktop screenshot: the New-status composer overlapped the conversation and clipped its third tab to "L" because `position: fixed` is only viewport-relative while no ancestor establishes a containing block, and it renders inside the Messages column whose chrome is backdrop-blurred (a filter DOES establish one) — now PORTALLED to document.body, with a tab row that shrinks and truncates instead of clipping. `server/residualsClosed.test.ts` (38), each pinning the property AND the constraint it had to respect. 1783 tests.) (v2.99.48 — SELF-REVIEW ROUND 3: THE FIXES THAT ONLY LOOKED CLOSED. The remaining red-team clusters found the sharpest class yet, and the pattern repeated three times: **a fix keyed to something the ATTACKER controls, or a guard that parses differently from the code it guards, looks closed and is not.** **(M60, HIGH ×2 — M48's forced-call hole was STILL OPEN)** (a) `bootedWithDialTarget()` regex-tested the RAW search string while the Dialer reads `to` with `URLSearchParams`, which PERCENT-DECODES KEYS — so `?%74o=555555&video=1` was invisible to the guard and visible to the guarded code: one click, live mic AND camera to an attacker-chosen number. (b) `/i/<pin>` — the app's OWN share link — boots with an EMPTY search and only then redirects client-side to `/app/dialer?to=…`, reading as an in-app tap, so the documented invite URL stayed fully exploitable. Fixed by making the detector use the CONSUMER'S parser (`bootDialTarget()`) and capturing `BOOT_PATH` so an `/i/<pin>` arrival counts as an arrival; proven empirically in `bootUrl.test.ts` (the old regex is blind to `%74o=`, the new parser sees it). **(M60c, MED — regression)** `BOOT_SEARCH` is per-DOCUMENT, so after ONE arrival every later in-app call tap in that tab hit the prefill branch and one-tap calling stayed broken all session — the test is now per-NAVIGATION (`arrivedWithDialTarget(to)`). **(M60d, MED — regression)** of the three armed "tap to call" paths only the notification branch marked the intent; the sonner toast action did not (fixed), and the Web Push path structurally cannot (no `sessionStorage` in a service worker), so it honestly needs a confirming tap — the claim is corrected rather than papered over. **(M57, HIGH — incomplete closure of M40)** the enumeration oracle NEVER closed: the budget was keyed on `callerPin`, but an anonymous caller is handed a fresh random pin by `genPin` at register and the stream reopens ~1/s, so a new bucket was minted per loop and probing ran at ~60/s — a full 10^6 walk in HOURS, not the "three weeks" v2.99.45 claimed (a ~100× overestimate), and the reply carried the callee's NAME, making it name-harvesting. It also let an anonymous client drain a REAL user's bucket. Now the budget follows what the caller cannot re-mint — `verifiedPin` (from F1's cookie-resolved `__ownedNumber`) else the address (new server-only `__clientIp`, stripped from client input like `__ownedNumber`) — and only a verified caller is told the name. **(M58, HIGH — incomplete closure of M21)** the number-space ceiling was in the wrong place: `/api/auth/register` reaches the same permanent-number sink via `ensureUserIdentity` with NO mint budget (43,200 claims/day/IP, more than M21 advertised, inside a bare `catch {}`), and a per-endpoint gate can always be forgotten. The ceiling moved to `allocateSharedNumber` — the one funnel all four allocators share — as a global rolling budget (5,000/hour/instance; deliberately soft, a runaway-loop backstop not an authz boundary), plus a per-IP mint gate on the registration branch that actually creates an account. **(M21 retune #2, MED)** the v2.99.45 sizing rationale was wrong — guest identity is SESSION-scoped (sessionStorage device id + session cookie), so the same person spends a token every browser session and demand tracks sessions/day; 0.2/s sustained still hard-failed the 13th visitor per minute on a shared egress, so it is now ~1/s with the global budget protecting the real resource. **(M59, HIGH — incomplete closure of M23)** one reveal was bounded, the process was not: tRPC batching + the 60-burst `statusGate` allowed ~60 concurrent 30MB inline reads, each also holding a `Buffer.concat` copy, a ~40MB base64 string and the JSON body — an OOM restart of the `instances: 1`, `max_memory_restart: "1G"` process that owns the whole in-memory signaling registry and every SSE stream (ALB-pinned), dropping every call on the fleet. Added a process-wide slot + in-flight byte budget (2 concurrent / 60MB) reserved BEFORE the irreversible burn (over budget answers `{ok:false, retry:true}` with the message intact) and capped tRPC batches at 20 ahead of the middleware. **(M22 follow-up, LOW)** a refused reveal rendered an EMPTY bubble wearing the "View once" chip and latched unretryable for the thread session; only a reveal that returned content is cached now, everything else refetches so the row's own state shows the honest placeholder and transient failures stay retryable. New `bootUrl.test.ts` (11) + `selfReviewPass3.test.ts` (18); seven stale pins rewritten to the stronger invariants. 1775 tests.) (v2.99.47 — SELF-REVIEW ROUND 2: regressions and half-closures in MY OWN fixes. A second red-team pass over the 29 fixes shipped today, asking of each one how it made the app WORSE for a legitimate user or left its invariant only half-shut; eight items, each verified against source first. **(M37 re-fix, MED — the bypass I "closed" was still open)** `videoOfferedByUs` was a plain boolean cleared only in `hangUp`, but a call can be left WITHOUT hanging up — `switchCall` abandons an unanswered dial with a bare `leave`, hold/swap park the active call — so the offer survived into the NEXT call: victim taps Video call → attacker dials → victim taps "End call & answer" → one `video-accept` frame turns the victim's camera on. Re-fixed STRUCTURALLY by keying the offer to the ROOM it was made for (`videoOfferPending` + `videoOfferedForRoom`, bound ONLY by the `room` ack — the server's reply to our own invite, the one place a room is provably ours), so a flag set for one call cannot authorize another and future switch paths are safe by construction; `resetVideoConsent()` at switch/swap also stops `videoApproved` leaking (it disables the gate, so a live camera would publish to a new peer with no agreement). **(M53, MED — regression from M45)** History's "Join" became a DEAD END: M45's `room.has(conn.pin)` gate was right, but `hostPin` never moved when the host left, so the knock alert went to an absent host whose Approve tap hit the gate and returned SILENTLY — the knocker waited forever and nobody still in the call was ever asked. Fixed with HOST SUCCESSION in `leaveRoom` (co-host first, else the longest-standing CONNECTED member; ghosts skipped) + `role{host}` broadcast, which also restores mute/pin/kick for the rest of the call. **(M56)** the same failure made non-silent: `knock` refuses up front when no moderator is present in the room, and a refused approve/deny REPLIES — with a NEW `knockfail` code, not `forbidden`/`gone`, since those are in the client's FATAL sets and would have hung up the approver's own call. **(M54, MED — incomplete closure of v2.99.44 L1)** a group dial still hung 65s: the client drains outstanding invitees BY PIN and two replies in the async offline branch (`nonexistent`, the resolver-failure `.catch`) carried no `pin`, so a dial including one unregistered number never drained; both fixed and the stale three-site count rewritten to assert the CLASS invariant. **(M55, LOW — regression from M40)** the throttled dial claimed `offline`, which is voicemail-eligible — so a mistyped number offered "leave a voice message", recorded 60s, then failed with the recording lost; now `unavailable` (unreachable, never voicemail-eligible, still leaks nothing). **(M52, MED — regression from M38)** everyday `.xml`/`.js` attachments started 400ing: M38 widened the upload denylist, but the same change made the proxy downgrade every non-inline-safe type to an octet-stream attachment, so the widening was redundant while the Messages paperclip (no `accept` filter) began rejecting ordinary files — restored to the pre-M38 set, with the load-bearing guarantee left at the serving end. **(M49, LOW — regression from M36)** M36 splits claim-then-latch, and a death in between (pm2 restarts on every push, across the ~100ms scrypt verify) left `attempts=4, lockedAt=NULL` — refusing even the CORRECT PIN while `loginProbe` reported `locked:false`, parking the user on a pad no entry could satisfy with no lock notice and no alert email; new `pinSlotsSpent()` derives the state from BOTH fields (used by `loginProbe` + `pinStatus`) and the no-slot branch HEALS such a row into a real visible lock via a shared `latchLockAndAlert`. **(M50, MED — incomplete closure of M35)** `consumeOtp` was an unguarded UPDATE, so two verifies of the SAME code both reached `createOtpUser` and one email could own TWO rows (`users.email` has no unique index) — a later sign-in then lands on the orphan account, the exact diversion M35 prevents; now an atomic claim (`isNull(consumedAt)` + `affectedRows`) with `verifyOtp` refusing a lost race before creating anything, plus `ORDER BY id` in `findUserByEmailAny` so historical duplicates resolve stably. **(M51, MED — regression from M29+M35)** a user who registered a password, signed in by code before clicking the verify link, then re-registered was told "sign in instead" — pointing at a password login that 401s forever with no reset route anywhere; the 409 now names the way in ("signs in with an email code"). Residuals: no password-reset route (no client posts to `/api/auth/*`, so adding one is new attack surface on a dead surface), fresh guest identities can still reset the M40 dial budget (bounded per-IP by `guestMintGate`), and a call is no longer rejoinable by number once the DIALLED party leaves (the card disappears — honest degradation, not a hang). New `selfReviewPass2.test.ts` (11) + `selfReviewRelay.test.ts` (12 behavioural, incl. the exact M45 dead-end scenario); nine stale pins across seven files rewritten to the STRONGER invariants rather than relaxed. 1745 tests.) (v2.99.46 — PROCESS FIX: the gate that let a broken commit reach `main`. v2.99.45 was pushed with UNRESOLVED merge-conflict markers in three files (`shared/version.ts`, `client/src/app/updateChecker.test.ts`, `CLAUDE.md`) — typecheck, four test files and the production build all broken. The deploy workflow's build step failed, so `your-chat.io` kept serving v2.99.44 and no user was affected; the gate meant to stop it BEFORE the push is what failed. ROOT CAUSE: the pre-push command was `pnpm check 2>&1 | tail -2 && pnpm test … && pnpm build …` — a shell pipeline's exit status is the LAST command's, so `tail` (always 0) masked the failing typecheck and `&&` continued to the push. Any `gate | head/tail/grep && next` reads as a passing gate, so two durable guards replace "remember not to do that": (1) **`pnpm verify`** = `pnpm check && pnpm test && pnpm build` as ONE package script — no pipe to insert, so the gates cannot be decoupled from their exit codes by how the command is typed at the call site; (2) **`server/repoHygiene.test.ts`** — a tripwire failing the SUITE on a stray `<<<<<<<`/`>>>>>>>` line in any tracked text file (ts/tsx/js/json/md/css/html/yml/sql/sh/kt/java; node_modules, dist, build, coverage, mobile native dirs skipped), because the typecheck only covers compiled sources — CLAUDE.md's markers passed every existing gate — while the suite is the one gate every workflow runs. The tripwire was verified to actually FIRE by planting a marker file and watching it fail, then pass again once removed. `CLAUDE.md` itself needed real reconstruction: stacked rebases had left NESTED markers, i.e. two rival truncated changelog chains, so the v2.99.45 entry now sits ahead of the intact .44 → .43 → .42 → … chain instead of one side silently winning. Also corrected v2.99.45's test count (claimed 1706, actual 1717). 1721 tests.) (v2.99.45 — SELF-REVIEW: two availability REGRESSIONS in my own fixes. Red-teamed today's 29 shipped fixes for regressions rather than new vulnerabilities — they went out fast, several into the call path and auth, and most are guarded by source pins rather than behaviour; a security fix that breaks calling is worse than the bug it closed. **(1) M21 refinement — signup could be blocked on a shared network**: `guestMintGate` sat at the TOP of `startGuest`'s resolver, so it charged a token to cases (1)/(2), the paths that return an EXISTING identity and allocate NO number. Behind one shared address (carrier CGNAT, an office, a classroom, a conference) returning visitors whose cookie had been dropped drained the bucket and then blocked people who genuinely needed a new identity — a hard TOO_MANY_REQUESTS on the ONE screen that gets a person into the app. The gate now meters only the ALLOCATING branch (the protected resource is the finite number space, so meter the operation that spends it) and is resized 20/1-per-10s → 60/1-per-5s, absorbing a whole room signing up together while still bounding one host to ~17k numbers/day. Verified `startGuest` has exactly ONE call site (the "Enter as guest" submit), so it is not a per-pageview endpoint. **(2) M40 retune — group dials could lose missed-call records**: the offline-dial limiter was sized without accounting for GROUP DIALS, which fan one invite per invitee — a 9-person all-offline group dial spends 9 tokens at once, so 20/1-per-4s was exhausted by the SECOND such dial, and because the throttled path deliberately skips `onMissedCall`, everyone dialled after that silently lost their missed-call record, History row and notification. Retuned to 60/1-per-2s (~six full 9-person offline group dials back to back, 30/min sustained); enumeration still goes from "under two hours" to about three weeks. VERIFIED SOUND, no change needed: M38's inline-safe list covers every MIME type RELAY itself produces — enumerated the real candidates from `voiceNote.ts` (audio/webm, mp4, aac, ogg), `videoNote.ts` (video/mp4, webm), `emojiAvatar`/`animatedAvatar` (image/png, image/gif), the downscale path (jpeg/webp) and application/pdf, and evaluated each against the actual regex: all inline, none forced to download; M28's SQL guard cannot falsely deny an ordinary attachment because `JSON_EXTRACT(NULL, …)` returns NULL, so the meta-IS-NULL case — almost every message — satisfies its first clause; and M40's throttled reply uses code "offline", which the client classifies as a `reachErr` that PROMOTES the next invitee during a group-dial bootstrap, so a throttle degrades one invitee instead of collapsing the call. Two of my own pins rewritten to assert the better invariants. 1717 tests.) (v2.99.44 — THE LAST TWO DEFERRED FOLLOW-UPS, found by auditing every DEFERRED / "remaining" / "residual" line in the changelog against CURRENT SOURCE after the owner asked whether anything was still missing. Both had been flagged in their original batch and never revisited. **H8 — THE MISSED-CALL EMAIL HAD NO THROTTLE** (deferred in v2.99.22 as "needs a cooldown column"): the path was gated on the block check, `reason !== "cancelled"`, a linked email and the pref — then sent, unbounded, so a caller dialling you repeatedly produced one email per attempt (the same shape the offline-message nudge was just tightened out of, and a free amplifier aimed at whoever the caller chose). New `lastMissedCallEmailAt` column + `claimMissedCallEmail()`: ONE atomic conditional UPDATE keyed on the cooldown with the verdict from `affectedRows`, so two simultaneous misses can't both claim; 10-minute window; the push and the History record stay UNCONDITIONAL and run BEFORE the claim, so a throttled email never costs the user the record of the call; a failed send releases the claim by inspecting `sent.ok` rather than `.catch()`, which would be dead code (`sendEmail` never throws — the trap the v2.99.42 review caught). Deliberately a cooldown with NO daily cap unlike the message nudge: a missed call is a first-class event and suppressing the tenth could hide the one that mattered, while repeated dialling is what blocking is for (a blocked caller already reaches neither the push nor this email). **L1 — AN ALL-DECLINED GROUP DIAL HUNG FOR 65s** (deferred in v2.99.27): `inParkedCall()` is true for a group call, so a decline correctly does NOT tear the dial down — two of three declining must leave the third ringing — but nothing watched for the LAST one, so when everyone declined the caller sat on "Ringing…" until the no-answer backstop with nobody left to answer. New `groupDialOutstanding`, non-null ONLY while a FRESH group dial is unanswered (never for an add-person invite into an existing call); declines, busies and unreachable invitees each resolve one, and when the last resolves with nobody having answered the dial ends honestly. The server now also stamps `pin` on its reachability errors (additive — old clients ignore it), because a group dial rings several people and without knowing WHICH invitee an `error{offline}` concerns the caller cannot tell when the last has resolved; it is also the only thing that notices a later invitee going offline once the room exists. `server/deferredTail.test.ts` (11), incl. a re-pin of the v2.99.22 property that a PARTIAL decline still leaves the others ringing; four pre-existing pins updated. NOTHING KNOWN OUTSTANDING after this — the heavy-QA sweep, the Rounds 1–8 file and both deferrals are closed; what remains is documented accepted residuals, each a recorded decision. 1714 tests.) (v2.99.43 — HARDENING PASS 8: the verified-and-queued list. Five items confirmed during the class sweep but held back from the pass that shipped three HIGHs, because four sit in the CALL PATH and bundling them behind one bump was a bad trade against a green suite. One turned out WRONG on inspection and is recorded as a refutation. **(1) M45 — MODERATOR POWERS OUTLIVED MEMBERSHIP**: `knock-approve` gated only on `isModerator(meta, pin)`, but it takes `roomId` from the CLIENT and `roomMeta` outlives membership (the roster is add-only; nothing clears `hostPin`/`cohosts` on leave), so a FORMER host who hung up could name the old room and admit an outsider into a call they had left. It CHAINED with `kick`: `leaveRoom` only drops membership and never touched `meta.cohosts`, so a KICKED CO-HOST kept their role, could knock, and could then APPROVE THEMSELVES BACK IN — the kick was undoable by its own target. Both ends fixed: approve now also requires `room.has(conn.pin)` (membership rather than `rid === self.roomId`, because a host whose call is on HOLD is still in the member Set and must stay able to approve), and kick revokes cohost + any pending knock and broadcasts `role:null`. **(2) M46 — IN-CALL CHAT TRUSTED THE FRAME'S SELF-DECLARED SENDER**: `name` and `pin` came straight from the JSON, so any participant could publish `{name:"Alice", pin:"<alice's pin>"}` and have it render as Alice — avatar included, since the chip resolves the photo BY PIN. Both transports already know the real sender (the mesh has one data channel PER PEER, so `setupDC`'s pin is authenticated by the channel; LiveKit hands `DataReceived` the sending participant, whose identity is the server-minted token pin), so `receiveChatFrame` now prefers that proven identity and takes the display name from the roster. **(3) M47 — DUPLICATE IDENTITIES PER USER**, the long-standing "my number changes randomly / this device shows a different number" symptom: `ensureUserIdentity` is a check-then-insert with no unique constraint, so two concurrent sign-ins for one account could each mint an identity (two rows, two 6-digit numbers) — and `getIdentityByUserId` used a bare `.limit(1)` with NO ordering, so MySQL returned EITHER row per query and messages/contacts split across both. Layered: `orderBy(asc(id))` makes resolution DETERMINISTIC even where duplicates already exist, and a UNIQUE index on `identities.userId` stops new ones (NULL repeats under a MySQL unique index, so guests are unaffected; the migrator's per-item catch means an existing-duplicate deployment still boots). **(4) M48 — `?to=` PLACED A CALL WITH NO GESTURE**: the Dialer auto-dials the param so in-app "call" taps connect instantly, but it could not tell an in-app route change from someone ARRIVING on the URL, and mic permission persists per-origin — so for any regular user `/app/dialer?to=<attacker-pin>` turned ONE CLICK into a live outbound call to a number the attacker chose (camera too with `?video=1`). A route module cannot detect this itself, since `Dialer.tsx` is lazily loaded and its module scope first evaluates AT the navigation in question; new `client/src/lib/bootUrl.ts` is imported by `main.tsx` and captures the boot URL before any routing, so a `to=` there means the user arrived on it → prefill the pad instead of dialing. Verified the in-app paths are untouched (`/i/:pin` uses wouter's client-side `<Redirect>`; Contacts/Messages use `setLocation`), and the one legitimate full-page path — the "back online — tap to call them now" alert the user armed — keeps its single tap via a one-time same-origin sessionStorage intent marker a link cannot forge. **REFUTED, not fixed**: "/api/relay/send resolves identity before the rate check" — the limiter is `app.use` middleware registered at `_core/index.ts:141` and `attachRelay` runs at :206, and Express honors REGISTRATION order, so it already precedes the handler (which calls `createContext` only for `register`). Pinned as a refutation so it is not re-raised. New `hardeningPass7.test.ts` (24); two stale pins updated for the new shapes. 1662 tests.) (v2.99.42 — ROUNDS 1–8 GAPS (the owner uploaded `claudecodeinstructions.txt` — eight rounds of review items — with "after you finish all your tasks, complete and check this". Audited every item against current source: R1, R2B, R3, R4A/B, R5, R6, R7-GAP4 and R8-FIX1/2/3/4 were ALREADY SHIPPED and are pinned by their own releases' tests; FOUR were genuinely still open and are fixed here). **R2A — THE PRODUCTION SERVER BUNDLE REQUIRED FIVE devDependencies AT BOOT**: `server/_core/vite.ts` is dev-only (production calls `serveStatic`), but a top-level `import … from "vite"` plus a default-import of the root vite config are hoisted and evaluated when the module graph loads, so esbuild put `vite`, `@vitejs/plugin-react`, `@tailwindcss/vite`, `@builder.io/vite-plugin-jsx-loc` and `vite-plugin-manus-runtime` into `dist/index.js` as TOP-LEVEL imports — `node dist/index.js` loaded all five just to serve static files. Not breaking today (the deploy runs a full `pnpm install`), but the day anything installs `--prod` or prunes, production stops booting for dependencies it never uses. FIX: `await import("vite")` inside the function, and rather than importing the config module, hand vite the config file's PATH so vite loads and compiles it (exactly what `vite build` does) — which also makes the config single-sourced, replacing a spread-config + "there is no config file" pair that quietly bypassed vite's own resolution. VERIFIED against the real artifact (zero dev-only top-level imports) AND that dev still boots (react-refresh preamble, `/src/main.tsx` 200). **R7 GAP1 — A NEW MESSAGE NEVER WOKE AN OFFLINE RECIPIENT'S DEVICE**: a missed call has pushed since v2.83 and a voicemail since v2.88, but a plain message only fanned an SSE hint — which by definition reaches only a tab that is already connected, i.e. exactly the case where the user does NOT need telling; a phone with RELAY installed and closed stayed silent until its owner happened to open the app. FIX: `messages.send` now pushes every OFFLINE recipient (`kind:"message"`, tag `relay-msg-<conversationId>` so ten messages REPLACE instead of stacking ten notifications, `url` deep-links the thread), content-free by the owner's standing rule — the sender's name, never a word of the body; a voicemail keeps its own better-worded push (no double-notify). **R7 GAP2 — NO USER-FACING PUSH SWITCH**: new `users.pushEnabled` (additive + nullable via the boot migrator, NULL/true = on) enforced INSIDE `sendPushToIdentity` and BEFORE the subscription lookup, so every push kind and every call site obeys it — including ones added later, which a per-call-site check is exactly the kind of thing to forget. Reads NULL as on and FAILS OPEN on DB trouble so it can never silence a ringing call; exposed via `otpAuth.get/setNotificationPrefs` and a "Push notifications" toggle in Profile (the section is now "Notifications" and renders for ANY signed-in account — the two EMAIL rows stay gated on a linked address). **R7 GAP3 — THE OFFLINE-MESSAGE EMAIL IS NOW A LAST RESORT**: this is the only mail RELAY sends that the recipient did not ask for (someone else's message triggers it), so the failure mode is an annoyed user and an SES sending reputation you do not get back. Four rules on top of the existing pref + atomic claim: (1) SKIPPED entirely when the recipient has a push subscription — they just got the notification above, so the email is pure noise; (2) requires them to have actually been away ≥5 min (`OFFLINE_MESSAGE_EMAIL_MIN_AWAY_MS`) because presence flips offline the moment a tab hides, so a phone that locks for ten seconds mid-conversation would otherwise earn an email for a message its owner is already reading; (3) cooldown 15 min → 60 min, and the copy COALESCES by design ("you have messages waiting", never a per-message count), so a burst is one accurate email; (4) a hard ceiling of 3 per UTC day. The cap is enforced by the claim's WHERE against the PRE-UPDATE row, so two concurrent claims at CAP-1 cannot both win. A REAL BUG in this release's own new code was caught in pre-merge review and fixed before shipping: the counter originally reset itself inside the claim (`SET count = IF(day <=> today, count + 1, 1), day = today`), leaning on MySQL's left-to-right SET evaluation so the IF would read the OLD day — but the emitted order is NOT ours to choose, because drizzle's `buildUpdateSet` walks the table's COLUMNS object (SCHEMA DECLARATION order), not the object literal passed to `.set()`, and `messageEmailDay` is declared before `messageEmailsToday`. The day was therefore written FIRST, the IF always saw `today`, and the counter never reset: 3 emails on day one, then exactly ONE per day forever, with the counter growing unbounded — and the first version of the test asserted the object-literal order, so it proved nothing. The fix is STRUCTURAL rather than a reorder: an idempotent day-rollover UPDATE (race-safe, since concurrent rollovers write identical values) followed by a claim whose SET is a pure increment, correct under ANY emitted order; the test now pins that property plus a behavioural guard on drizzle's dialect so a library upgrade cannot silently reintroduce the dependency. A FAILED send now returns BOTH the cooldown and the day's budget slot (`GREATEST(... - 1, 0)` floors the counter). Plus ONE-CLICK UNSUBSCRIBE THAT NEEDS NO LOGIN: new `server/unsubscribe.ts` mints `<userId>.<hmac>` (signed with the inbound-email secret family, so no new env var) and `/api/email/unsubscribe` accepts GET (a human clicking) and POST (RFC 8058 one-click, where the mail client submits it); the mail carries `List-Unsubscribe` + `List-Unsubscribe-Post` headers AND a visible footer link. The token is a NARROW capability — it can only turn message email OFF, never on, and reaches no other setting, so a leaked link costs at most one channel the user re-enables in Profile — and deliberately does NOT expire (mail lives in inboxes for years; an unsubscribe link that has quietly gone stale is what gets a sender reported as spam). Fails CLOSED with no secret configured. Custom headers are threaded through `sendEmail` → `smtpSend` → `buildMimeMessage` behind a CR/LF + header-name guard (`extraHeaderLines`), so a header value can never inject another header or a body. A SECOND pre-merge finding: `/api/email/unsubscribe` MUTATED ON GET, and express answers HEAD from `app.get` — so a mail security gateway that fetches links found in mail to detonate them (Safe Links, Proofpoint, corporate AV) would have silently unsubscribed the recipient before they ever opened the message, which is exactly what RFC 8058's one-click POST exists to prevent. GET is now READ-ONLY (it verifies the token and renders a confirm page whose button POSTs — still no sign-in) and POST is the only writing path, leaving the RFC 8058 flow untouched; the form action is the only query value reaching the markup, is reached only after the token verified, and is escaped regardless, and the POST gets its own per-IP limiter + sweep. A 5-reviewer adversarial pass over this release's own diff then found FIVE MORE real defects in the new code, all fixed before merge: (a) PUSH OFF ⇒ NEITHER PUSH NOR EMAIL — nothing deletes the `push_subscriptions` row when the switch is turned off, so "has a subscription" was the wrong stand-in for "reachable" and push-off + message-email-on produced silence on both channels; new `pushReachable()` requires a live subscription AND the switch on. (b) THE FAILED-SEND ROLLBACK WAS DEAD CODE (pre-existing since v2.99.19): `sendEmail` never throws and resolves `{ok:false}`, so the `.catch()` never fired and a refused SES send kept the claim — it inspects `r.ok` now. (c) THE UNSUBSCRIBE URL NEVER REACHED text/plain, because the default `stripHtml(html)` deletes anchors together with their hrefs — a hand-written `messageWaitingText()` now carries both URLs. (d) MUTE AND DND WERE BYPASSED: both are per-DEVICE localStorage settings enforced in the page, which was enough only while every alert originated in an open tab — a push reaches the OS directly, and `conversationParticipants.mutedUntil` exists but is never written, so the server cannot know; fixed at the SERVICE WORKER (per-device, like the settings), with the page mirroring `{dnd, muted[]}` into Cache Storage (new `client/src/app/swPrefs.ts`) and `sw.js` parsing the conversation id off the `relay-msg-<id>` tag, failing OPEN and never letting a per-conversation mute suppress a call. (e) THE DEV SERVER STARTED SERVING `.git/**` AND `*.pem` — handing vite the config path made the file's `server.fs.deny` actually apply (the inline server block used to replace it), and `mergeWithDefaults` ASSIGNS arrays, so `["**/.*"]` replaced vite's four defaults; picomatch only matches a dotted LAST segment, so `.git/config` (credentials in its remote URL) and `key.pem` fell through — verified against picomatch directly and the list is now complete. Also `vite.config.ts` had fallen out of the tsc program (the static import was what pulled it in), so type errors there escaped both `pnpm check` and the build; it is in tsconfig `include` now. `server/roundsGaps.test.ts` (41 — including a real assertion against the BUILT `dist/index.js`, the SET order-independence property, a BEHAVIOURAL proof that drives the real express app and counts writes (GET → 0 + a form, HEAD → 0, POST → 1, tampered → 400 and 0), unsubscribe round-trip/tamper/no-secret, and header injection); three v2.99.13 pins in `emailNotifyPrefs.test.ts` updated to the tightened shapes. NO OPS REQUIRED. 1666 tests.) (v2.99.41 — HARDENING PASS 7: the class sweep COMPLETED (14/14 classes, 55 adversarial verdicts — 51 refuted, 4 upheld), and its last wave plus the panel's survivors are fixed here. **(1) ReDoS on the inbound-email webhook — the panel's highest-confidence new finding**: `parseInboundAddress` ran `/<([^>]+)>/` against an untrusted header value with NO length cap on a route that accepts 5MB of JSON. On input containing a `<` but no `>` the engine retries `[^>]+` from every `<` and gives back one character at a time — quadratic, ~10^13 steps for 5MB. Node is single-threaded and this process serves every SSE stream, signaling POST and API call, so ONE request stalls calls and messaging for EVERY user, and the webhook signature check is opt-in so it can be unauthenticated. Capped at 1024 bytes before the match (RFC 5321 caps an addr-spec at 320), which bounds n instead of relying on a cleverer pattern. **(2) `region` was still spliced raw into the SSM remote commands** — a GAP IN MY OWN EARLIER FIX: G11 base64'd `SES_EMAIL` and `DOMAIN` but missed `region`, the THIRD free-text `workflow_dispatch` input on the same path, which was still interpolated unescaped into all five command strings executed on production EC2. Same treatment applied; its other uses run on the runner via safe single-pass substitution and are deliberately untouched. **(3) sign-out never revoked the session ledger row** (upheld by the panel, traced end to end): v2.99.1 built a revocable session model and `createContext` gates every sid-bearing cookie on it, but `auth.logout` only cleared COOKIES — so the row stayed ACTIVE, the device kept appearing in the user's own Devices list as a live session, and the token itself stayed valid, so a copy recovered from a synced profile, a disk backup or a shared machine would still authenticate. "Log out" now means the credential stops working; wrapped so a DB hiccup can never prevent the cookies being cleared. **(4) the media-proxy limiter was too tight for shared egress** (an availability finding, not a vuln): 240 burst / 4-per-second is per-IP, and carrier CGNAT, an office or a café put many real users behind ONE address — on an image-heavy chat a few people scrolling together could exhaust it, and a throttled media request renders as a BROKEN IMAGE, the exact symptom this project has chased repeatedly. Raised to 600 / 20-per-second, which still caps a scraper two orders of magnitude below unlimited; the guard's real target is DB-CPU cost on the miss path, not enumeration (keys carry a random suffix and cannot be guessed). VERIFIED AND DOWNGRADED after independent checking: the Android `release { signingConfig signingConfigs.debug }` line is a real footgun but NOT a live compromise — `native-rn.yml` re-signs the AAB with a real keystore from `ANDROID_KEYSTORE_BASE64` after the build, so the store artifact is properly signed; recorded as an operator note rather than changed blind. LEFT TO THE OPERATOR (cannot be fixed from the repo, and guessing would break the deploy): the deploy OIDC role trusts `repo:…:*`, so a workflow on ANY branch can assume the production deploy role — that is an AWS IAM trust-policy edit; and `deploy.yml` pins third-party actions to mutable major tags in the job that holds production credentials — pinning needs verified SHAs. `hardeningPass6.test.ts` grows to 35, including a bounded-regex timing check and per-command assertions on the region fix. 1638 tests.) (v2.99.40 — HARDENING PASS 6: the class sweep's later-reporting classes. **(1) HIGH — the 4-try PIN lockout was bypassable by CONCURRENCY**: `attemptPinLogin` gated on `row.loginPinLockedAt` — a field from a snapshot the CALLER had already read — and then ran `verifyPassword` regardless of the row's LIVE state. So N simultaneous requests all saw an unlocked row, all passed the gate, and all got a PIN checked; the S1 fix had made the COUNTER race-free but never bounded how many VERIFICATIONS could occur, so the cap wasn't actually enforced per attempt and a burst could sweep much of the 10^4 space, held back only by the per-IP bucket. Inverted: every attempt must now WIN a slot from the DB first (`UPDATE … WHERE lockedAt IS NULL AND attempts <= cap`, verdict from `affectedRows`) and only a winner may verify — MySQL serializes it per row, so at most cap+1 verifications can ever happen between unlocks, regardless of concurrency or instance. The ladder is unchanged (the 4th try is still verified, so a correct 4th still succeeds; a wrong 4th latches the lock via its own `isNull`-guarded UPDATE, which also owns sending the alert email exactly once). The pure `judgePinAttempt` helper is test-only and now says loudly that it must never gate a login. **(2) HIGH — an unsolicited `video-accept` forced a peer's camera ON**: `onVideoAccept` checked nothing but `inCall`, so any call peer could send that frame and run `unlockApprovedVideo()` on the victim — a total bypass of the v2.81 mutual-consent protocol, whose whole promise is that a camera transmits only after BOTH sides agree; the victim's only notice was a cheerful "Video is on — both sides 🎥" toast, and on a party line one frame could do it to everyone. `videoReqT` alone was not a usable guard: a VIDEO DIAL answered with the Video button also replies `video-accept`, and there the caller consented implicitly by dialing (no request was ever sent), while `outgoingDial` is cleared at establishment though consent frames often arrive before the transport exists. New per-call `videoOfferedByUs` flag set at BOTH consent points and cleared by the existing per-call reset; an unsolicited accept is dropped SILENTLY so it reveals nothing. **(3) HIGH — attacker-chosen `Content-Type` served same-origin**: the storage proxy relays the stored (uploader-supplied) type verbatim, and `nosniff` means the DECLARED type is obeyed — so the upload denylist was the only defence, over an allowlist admitting `text/*` and `application/*` WHOLESALE. The entire XML family was missing (`text/xml`, `application/xml`, `text/xsl`, `application/xslt+xml` — XML carrying SVG/XHTML namespaces with a `<script>` executes in some browsers, the exact outcome `image/svg+xml` is blocked for), as were the other JavaScript media types. Fixed at BOTH layers: the denylist covers those families, AND the proxy now serves only an inline-safe set as itself, downgrading anything else to `application/octet-stream` + `Content-Disposition: attachment` — robust without enumerating every dangerous type, and matching how the client already presents attachments. **(4) `auth.me` shipped the caller's CREDENTIAL HASHES to the browser**: `getUserById` does an unprojected `select()`, so the handler serialized the entire `users` row — scrypt `passwordHash` AND `loginPinHash` — into every response, where it lives in the React Query cache, devtools/HAR captures and anything an extension can read. Self-only, so not cross-user disclosure, but it converts any read-only client foothold (an XSS like #1 of v2.99.37, a malicious extension) into offline cracking — and the PIN hash covers 10^4, which does not survive a trivial offline attack. Stripped as a denylist so no client-consumed field can vanish. **(5) the signaling OFFLINE dial was the last unthrottled number→identity oracle AND a third-party spam amplifier**: its replies differ by design ("<Name> is offline right now." vs "That number doesn't exist."), leaking existence and display name across the 10^6 space, and each pass also writes a History row and fires a missed-call push AND email — with no cooldown, unlike the offline-MESSAGE email. The tRPC resolvers were gated for exactly this (F5), but this path never was, and the signaling limiter is a ~200/s FLOOD guard a scraper stays under (full enumeration in under two hours). New per-caller-pin `offlineDialLimiter` (20 burst, ~1/4s) scoped ONLY to the offline branch — a dial to an ONLINE user never reaches it, so normal calling and group dials are untouched, which is why this avoids the previously-rejected idea of capping invites generally; the throttled reply is the GENERIC offline message and returns before resolving anything or recording a miss. **(6) `identity.regenerateNumber` had no throttle** — the M21 sibling: each call permanently claims another of ~980,000 numbers and the old one is never recycled, so one authenticated account could drain the shared space and break allocation for every future signup; now behind the same mint budget. New `hardeningPass6.test.ts` (22) + M36 coverage in `hardeningPass5.test.ts` incl. a simulation proving a 10,000-request burst yields exactly cap+1 verifications; 3 stale pins retargeted. 1582 tests.) (v2.99.39 — SHIPS THREE CHANGES, renumbered from v2.99.35–37 because a parallel session took those numbers on `main` while this branch was open. **(1) MESSAGES THREAD LIST REDESIGNED (owner brief + the Snapchat chat-list screenshot they supplied as the reference; verbatim: "redesign the message section this way … the icon, the name, showing typing when he's typing indicators. Down it shows you message. Put the PIN number. Put verified beside his name. If he has a status. I needed to redesign and make it very flexible for the eyes, NOT compact — the current design is compact. And no need to put message, dial, voice, this outside: if you go inside the message you will see it in the top bar so you can click and dial voice video, and no need to put the message because you're already in the message section. So act, think professional as perfect designer."). DESIGN PROCESS: three complete candidate rows were drafted and scored by three independent judges (visual craft / usability+a11y / robustness); the winner ("Quiet Two-Line", 87.5) shipped WITH every graft all three judges independently asked for — chiefly moving the timestamp to the RIGHT END OF LINE 1 (v2.99.33 had it on line 2, competing with the preview). THE ROW (`client/src/pages/app/Messages.tsx`, the whole v2.99.33 4-button block replaced): a 60px avatar as its OWN button (status ring → story viewer / profile, presence LED) + exactly TWO text lines inside ONE tap target — LINE 1 `NAME` at 19px (bold when unread) + a caption-less tier mark + the time pushed right (`ms-auto`); LINE 2 `[muted] PIN(NNN-NNN) · preview` — or "typing" with three pulsing dots — `· N new`. No dividers: separation is whitespace on a ~92px rhythm (`rounded-2xl mx-1.5 my-0.5 px-3 py-3.5`), which is the "not compact / flexible for the eyes" the owner asked for — the row is ~40% taller than v2.99.33's and the NAME now owns the full width instead of sharing it with an action cluster (which had squeezed it to "A…"). PER-ROW BUTTONS REMOVED per the owner: the message / voice / video `AccentCircle` cluster is gone from every thread row — nothing was lost, because the conversation's own top bar already carries voice + video (the dialer deep-links at `Messages.tsx`, re-pinned by the new test) and a "message" action inside Messages was always a no-op. The STATUS RING is untouched and still live (`PeerAvatar` draws gradient=unseen / subtle=seen and opens the viewer on tap) — that's the owner's "if he has a status". ROBUSTNESS (both were REAL bugs in earlier rows): NO fixed row height anywhere (a hard-coded 16px line clipped a badge in v2.99.36's Dialer — the row now only ever sets a MINIMUM), and the PIN + time carry `dir="ltr"` + `[unicode-bidi:isolate]` while the name/preview carry `dir="auto"`, so an Arabic (RTL) name can't reorder or absorb them; line 2 WRAPS (`flex-wrap gap-y-1`) rather than starving the preview on a narrow phone; the avatar button stays a SIBLING of the row button (nested buttons are invalid HTML); `aria-current` marks the open thread; the typing dots are `motion-safe:animate-pulse` (inert under prefers-reduced-motion). `client/src/pages/app/messagesRowRedesign.test.ts` (18 — the airy shape, every element the owner listed, the buttons' absence + the header still having voice/video, and the full robustness set); the `verifiedBadge` / `badgePinSurfaces` Messages pins were updated to the new row shape and the superseded v2.99.33 row describe in `ownerRowStatus.test.ts` replaced with a note (its status half is unchanged and still runs). 1514 tests. **(2) ALSO IN v2.99.39 — OWNER BATCH: CAMERA/MIC RELEASED WHEN A CALL ENDS + IN-CALL/DIALER UI. **(1) THE BIG ONE — "when I finish the call and I minimize the browser, the mic and the camera is still active, I cannot even have another call"**. A 5-dimension multi-agent audit (24 agents, every acquire/release path adversarially verified) found a CLUSTER of causes, all fixed: **(a) THE WEDGE (the actual reported symptom)** — the red End button is wired to `endActiveLine()`, which deliberately SKIPS `hangUp()` (the ONLY function that releases capture) whenever `heldRoomId` is set and waits for the server's `resumed`. `onResumed` never cleared `heldRoomId` (it was written for `swapCall`, which re-sets it itself), so after ONE end-and-resume `heldRoomId === roomId`; the NEXT End tap took the silent branch, the server's `promoteHeldRoom` found no held room and replied NOTHING, and the engine wedged: `inCall` stayed true, `localStream` + pipeline stayed captured (indicator lit), End became a permanent no-op, and a new dial was blocked (`programmaticDial` requires `!inCall`). FIX: `onResumed` clears a stale `heldRoomId`; `endActiveLine` arms a 4s fail-closed fallback (`END_ACTIVE_RESUME_MS`) that forces `dropHeld() + hangUp()` if no `resumed` arrives; the SERVER now always answers `end-active` (`error{nohold}` when it promotes nothing) and the client completes the hang-up on that reply. **(b) IDLE HOLD** — `primeMedia()` acquired camera+mic at login and deliberately KEPT them ("we warm the stream and keep it ready"), so the devices were captured the whole time the app was open with NO call, lighting the indicator and blocking every other tab/app. It now warms only the PERMISSION (acquire → immediately `stop()`); the grant persists so an in-call `ensureMedia()` still never prompts. **(c) ORPHANED ACQUISITIONS** — `ensureMedia`/`flipCamera`/`reacquireCameraForPublish`/`recoverDeadLocalTrack` all installed a stream AFTER an await with no staleness check, so a call ending mid-acquisition stranded a live capture nothing could stop. New monotonic `mediaGen` (bumped on every release) + `mediaStale(gen)`/`stopStream()`: each path captures the generation before awaiting and stops the fresh stream if it changed; `ensureMedia` also dedupes concurrent callers through one in-flight promise (concurrent calls used to double-acquire and orphan the loser). **(d) ONE RELEASE PATH** — new `releaseLocalMedia(reason)` (stops tracks + destroys the pipeline + clears the self-preview `srcObject`) is used by hang-up, engine destroy (which never destroyed the pipeline before), and a new backgrounded-while-idle sweep. **(e) VOICE-NOTE MIC** — `voiceNote.ts` only stopped its stream inside `rec.onstop`, so a MediaRecorder construct/start throw left the mic open with no handle; both are now guarded, and `Messages`/`VoicemailPrompt` cancel the recording if they unmount during the acquisition await or mid-record. **(2) IN-CALL UI (owner screenshot)**: the ⋯ **More** button, its overflow menu and the **Diagnostics** floater + overlay panel are REMOVED; **Record** survives promoted into the control bar as a normal labeled chip (still hidden unless recording is configured). The internal `diag()` event log is kept for console debugging; the `?` shortcut and all dead CSS are gone. **(3) DIALER (owner screenshot)**: the preview sub-line was a FIXED `h-4` (16px) row while `RoleBadge` stacks its caption (~22px), so the badge overflowed and collided with the keypad — it is now a two-line block (name + caption-less mark with the tier word INLINE on line 1, presence on line 2, new exported `roleLabel()`); tapping a key now plays a real **DTMF dual tone** (new `client/src/lib/dtmf.ts`, the genuine telephony frequency pairs, output-only so it can never hold the mic, also on hardware-keyboard digits, context closed on unmount); and the "Save … to contacts" pill is no longer clipped by the tab bar (own `shrink-0` row + the card can scroll as a safety valve). Tests: `client/src/lib/mediaRelease.test.ts` (15), `client/src/pages/app/dialerToneLayout.test.ts` (11), `callBarV2994.test.ts` rewritten for the removals; `verifiedBadge.test.ts` Dialer pin updated. 1499 tests. **(3) ALSO IN v2.99.39 — EMAIL VERIFICATION REACTIVATED: the `RELAY_OTP_REGISTER_BYPASS` STOPGAP IS REMOVED (owner: AWS approved SES production access — out of the ap-south-1 sandbox 2026-07-24, 50k/day @ 14/s). The v2.97.2 stopgap let `otpAuth.register` skip minting/emailing an OTP and sign the caller in immediately (email ownership unproven) while SES was sandboxed. Now that SES can deliver to any address, the bypass BRANCH + the `otpRegisterBypassEnabled()` reader are DELETED from `server/v2routers.ts` — `register` ALWAYS mints a code and `dispatchOtp`s it (via the built-in SMTP client → SES), and the account is only created/verified once the recipient enters the code in `verifyOtp`. So email ownership is proven at signup UNCONDITIONALLY — a stale `RELAY_OTP_REGISTER_BYPASS=1` still lingering in a server `.env` now has NO effect (the flag is no longer read). Client (`AuthPanel.submitRegister`): the `bypass:true` short-circuit that jumped to the setup step is gone — registration always advances to the code-entry stage. NOTHING ELSE about email changed: the missed-call email (`_core/index.ts onMissedCall`, pref `emailNotifyMissedCall`, default on) and the offline-message email (`v2MessagesRouter.send`, content-free `messageWaitingHtml`, pref `emailNotifyMessage`, 15-min cooldown) were ALREADY wired through the same `sendEmail`→SMTP→SES path and are unchanged. OPS to finish activation on the EC2 boxes: ensure the SES SMTP creds + a VERIFIED `EMAIL_FROM` are in `/home/relay/.env`, set `APP_URL=https://your-chat.io` (so the emails' "Open RELAY" links render), restart pm2. `server/otpRegisterBypass.test.ts` rewritten (6 pins: the flag is no longer read, register always mint/emails regardless of the old flag, AuthPanel has no bypass path); `deviceSessions.test.ts` session-count pin 3→2 (register no longer mints a session — verifyOtp/loginWithPin are the two sign-in paths). 1471 tests. NOTE on (3): the parallel session's v2.99.38 HARDENED the same bypass branch (M31, create-only) instead of removing it; deleting it supersedes that hardening and loses nothing — its companion M29 fix (`clearUnverifiedCredentials` before `markUserEmailVerified`) lives in `verifyOtp`, which is now the ONLY path that creates a registered user; `hardeningPass5.test.ts`'s M31 describe was rewritten to pin the stronger property and its M29 ordering pin now asserts EVERY claim site rather than a fixed count of two. ALSO FOLDED IN: the parallel session's v2.99.36 landing work found two REAL keypad-tone bugs that applied verbatim to this branch's app-Dialer `client/src/lib/dtmf.ts`, so both are fixed there too (repo rule: apply a fix system-wide) — notes were scheduled at `currentTime` in the SAME tick as the async `resume()`, so on iOS the start time had already elapsed and the tone was silent (it now resumes THEN schedules, via a `fire()` continuation, with a 5ms `LOOKAHEAD_S`), and the peak gain was inaudibly low at 0.085 (now 0.18, matching the landing pad and still under the ringtone's 0.28).) (v2.99.38 — HARDENING PASS 5 pt.2: the remaining class-sweep findings. **(1) HIGH — `RELAY_OTP_REGISTER_BYPASS` was unauthenticated account TAKEOVER, not just unproven signup**: the bypass branch of `otpAuth.register` (a `publicProcedure` whose whole input is a name + an email) called `findUserByEmailAny` and signed the caller in as WHATEVER IT RESOLVED — so with the flag on, anyone who knew a registered user's email got a full session as them, no code and no password. The accepted trade for this SES-outage stopgap was "email ownership isn't proven AT SIGNUP"; taking over EXISTING accounts was never part of it. The branch is now CREATE-ONLY: it refuses (CONFLICT) when the address already has an account and only ever mints a new one, which preserves the flag's actual purpose. Signing in remains `requestOtp`/`loginWithPin`, both of which still demand a real credential. **(2) HIGH — `BLOCKED_MIME` bypass via a multi-valued Content-Type**: `ALLOWED_MIME` and `BLOCKED_MIME` are both START-ANCHORED, so they only ever inspected the FIRST type in the client-supplied `?mime=` string — `image/png,text/html` passes ALLOWED (starts with `image/`) and misses BLOCKED (doesn't start with a blocked type). That value was stored and replayed verbatim as the `Content-Type` of a same-origin response, i.e. the stored-XSS shape `BLOCKED_MIME` exists to prevent, since header parsers disagree about which listed type wins. New exported `normalizeMimeType` reduces to a canonical `type/subtype` essence (params dropped, case-folded) and demands exactly one RFC-2045-token media type — no commas, no whitespace — applied at BOTH mimeType sources before any gate or storage write. **(3) account DIVERSION (the other half of M29)**: `/api/auth/register`'s `findLocalUserByEmail` matches only rows that HAVE a passwordHash, so it is blind to OAuth/otp accounts and inserted a SECOND `users` row for the same email. `findUserByEmailAny` ranks a `local` row ABOVE a legacy OAuth row, so the victim's email code then signed them into the attacker's empty row — and with the OAuth UI removed in v2.92, the email code is their ONLY way in, so their real account (number, contacts, history) became unreachable. New `findAnyUserByEmail` refuses registration when ANY row already holds the address; the unverified-local resend path is untouched. **(4) gzip amplification on `/api/v2/upload`**: body-parser inflates encoded request bodies by default and enforces `limit` against the DECOMPRESSED stream, so the 41MB ceiling held but the attacker's cost to reach it collapsed ~1000x (tens of KB of compressed zeros → 41MB buffered) — compounding the known ordering weakness that this route's rate limit runs INSIDE the handler, after buffering. `inflate: false` on both upload parsers; no client compresses an upload body, so it costs nothing. **(5) the storage-proxy rate limiter was never swept** — every other limiter in the codebase pairs itself with a periodic sweep, but this one shipped without one, so its per-IP Map grew for the process lifetime on the app's only fully anonymous, high-fan-out endpoint. `hardeningPass5.test.ts` grows to 59 (incl. behavioral `normalizeMimeType` coverage of the real bypass payloads). 1555 tests.) (v2.99.37 — HARDENING PASS 5: CLASS-BASED SECURITY SWEEP (owner: "make sure that you cover all type of security bugs... I want the system to be very perfect"). The four prior passes audited SURFACE BY SURFACE; this one audited by VULNERABILITY CLASS (injection, XSS, authz/IDOR, CSRF, SSRF, upload/path, crypto, race/TOCTOU, DoS/ReDoS, business-logic, info-disclosure, client-trust, deps, CI), which is what surfaced these. 10 fixed, every one verified against source before any change. **(1) HIGH — zero-click DOM XSS in the in-call chat** (`relayClient.ts`): a chat frame's `pin` arrives over the peer's DATA CHANNEL and was validated with nothing but `typeof d.pin === "string"`, then interpolated into the message row's `innerHTML` twice unescaped — once inside the double-quoted `data-pin="…"` ATTRIBUTE, and once through `fmtPin`, which returns a non-matching string UNCHANGED. A peer sending `pin: 'x"><img src=x onerror=…>'` broke out and executed on parse, with no click or hover, on our own origin — i.e. full session takeover (drive the authenticated API as the victim). Reachable by anyone who can share a call, INCLUDING a party line (joinable by number), so one frame could hit every participant. Fixed by validating to `/^\d{6}$/` (a pin is always six digits) rather than merely escaping — the same check `ensureChatAvatar` already did, which just ran AFTER the markup was written; `initials()` sinks on the chat chip, call tiles, and recents are escaped too. **(2) HIGH — account pre-hijacking → takeover**: the unauthenticated legacy route `POST /api/auth/register` creates a `loginMethod:"local"` user with an ATTACKER-chosen `passwordHash` and `emailVerified:false` for ANY email. When the real owner later signs in with an email code, `findUserByEmailAny` deliberately falls back to that `local` row (v2.92 compat), and `markUserEmailVerified` flipped it verified while LEAVING the attacker's password in place — after which `/api/auth/login` (password + verified) handed the attacker a session on the victim's account. New `clearUnverifiedCredentials(userId)` destroys any password/PIN on a still-UNVERIFIED row at the moment the address is proven, called before `markUserEmailVerified` at BOTH claim sites; scoped to unverified rows, so a legitimate local user who verified via their own link keeps their password. **(3) view-once lock bypass** — `getAttachmentForIdentity` authorized a non-uploader via ANY referencing message, including a still-LOCKED expiring one (its `attachmentId` is only nulled at burn). Since that one function backs `attachments.get` (sequential integer ids — just enumerate), `authorizeStorageKey`, AND `messages.send`'s ownership check, a recipient could read view-once media repeatedly WITHOUT burning it (sender never told it was seen) and even RE-ATTACH it to a new permanent message elsewhere, laundering content meant to vanish. Locked expiring messages no longer authorize; the uploader's early return keeps senders unaffected. **(4) MySQL left-to-right UPDATE assignment broke BOTH attempt ladders**: MySQL documents that a later SET assignment reads the value an earlier one just wrote, so the lock CASE saw `attempts` ALREADY incremented and the extra `+ 1` double-counted. The PIN row locked on the THIRD wrong entry, not the fourth — and the persisted count could then only reach 3 while the brute-force alert email requires exactly 4, so **that email was UNREACHABLE and an owner was NEVER told their account was under attack**. Same defect burned the email OTP a guess early. **(5) `startGuest` was an unauthenticated, unthrottled identity MINTER** — the only resource-creating public endpoint with no gate. Each call permanently claims one of ~980,000 six-digit numbers: `numberTaken` treats mere row existence as taken (ignores guest expiry), nothing deletes identities, and M20's reservation ledger is monotonic. Drained far enough, `allocateSharedNumber`'s 40-attempt search fails for EVERYONE and every new guest, registration, and party line dies with "could not allocate a unique 6-digit number" — a permanent, unauthenticated DoS on all onboarding. Added `guestMintGate`. **(6) `revealExpiring` buffered an unbounded body** — the guard was `Number(content-length ?? 0) <= CAP`, so a MISSING header yielded 0, PASSED, and went into `arrayBuffer()` with no ceiling (the later `buf.length` check was too late); base64 then added 33% inside a JSON response. Now a named `REVEAL_MAX_INLINE_BYTES` enforced against the STREAM, so a missing or lying header can't exceed it. **(7) view-once burn was not atomic** — both burn paths did read → check `consumedAt` in JS → write with an await between, so two concurrent reveals both returned the content (same lost-update class as the S1/S9 races). Now one conditional UPDATE guarded on `JSON_EXTRACT(meta,'$.consumedAt') IS NULL`, verdict from `affectedRows`. **(8) `avatarUrl` accepted arbitrary `http(s)://`** — a profile photo became a remote-fetch primitive aimed at other users, and it renders on the INCOMING-RING card, which appears with NO interaction: set an avatar to your own host, dial a victim, harvest their IP + User-Agent from a call they never answered. Same threat the status-background sanitizer already rejects `url()` for, and squarely against this app's no-tracing goal; restricted to our own storage path or `data:image/` (zero compat cost — clients only ever set it from our upload endpoint). **(9) `status.post` skipped its `keyInOwnerNamespace` gate for `kind:"text"`** while still persisting `mediaKey`. Since `authorizeStorageKey` resolves a `/status_` key via whichever ACTIVE status claims it, planting another user's key re-activated their EXPIRED or DELETED status media — readable again by the planter and re-exposed to the planter's audience, defeating the ephemerality guarantee. Gate now applies to any supplied key, and a text status never persists media. **(10) `tryReserveNumber` detected a duplicate key by ERROR-MESSAGE TEXT** in a helper that fails OPEN, so a driver upgrade or localized server would silently turn every lost race into "reservation won", reintroducing the cross-table number collision the ledger exists to prevent; now keys on errno 1062 / ER_DUP_ENTRY with the text sniff only as a fallback. VERIFIED CLEAN this pass (documented negatives): no SQL injection (every `sql` template parameterizes values and uses Drizzle column refs for identifiers); landing-page `innerHTML` escaping intact after the v2.99.35 React-19 rework (`escLp` covers all five chars, applied post-composition, all sinks text-context); secret comparisons consistently `timingSafeEqual` with length guards; CSRF genuinely defended by `SameSite=Lax` on every cookie (the `/api/v2/offline` beacon's cookie path can't fire cross-site, and its deviceId fallback needs a secret); `keyInOwnerNamespace` correctly anchored (the trailing slash defeats the 1-vs-12 prefix collision) and the absolute-URL avatar suffix-match fix unexploitable; `searchMessages` filters expiring content; `tabPresence` stores no secrets and fails safe. ACCEPTED RESIDUAL: `push.subscribe`'s upsert is keyed on the globally-unique `endpoint`, so knowing a victim's endpoint lets you re-bind it and silently kill their notifications — but no API ever returns an endpoint, the hijacker gains nothing readable (pushes are encrypted to their own keys), and the only correct fix is a proof-of-possession challenge; naively refusing re-binds would break the documented account-switch-on-same-device flow, silently killing notifications for real users. Prior-round residuals unchanged. `hardeningPass5.test.ts` (44, incl. a real arithmetic simulation of MySQL assignment order and the XSS payloads the pin guard must reject); 4 stale pins updated. 1540 tests.) (v2.99.36 — LANDING POLISH, three owner asks (screenshot + notes). **(1) ARABIC "OPEN APP" ICON MISSING**: the pill's copy carried a bare `↗` (U+2197). The v2.99.16 RTL rule forces `Noto Kufi Arabic` on EVERY element in Arabic and that face has no U+2197, so iOS fell through to the emoji font and drew a boxed ↗️ beside the Arabic label. FIX: the arrow is now an inline SVG (`ARROW_NE`) that renders identically in every language/font/platform, mirrored in RTL (`.lp-arrow{transform:scaleX(-1)}`) so it still points outward. A SECOND instance of the SAME defect was found while pinning the fix — the CALL button label also ended in `↗` and is NOT inside a `dir="ltr"` island, so in Arabic it rendered the identical emoji box; `setCallState` gained an `arrow` flag that keeps the label as `textContent` (localized copy) and appends the CONSTANT SVG via `insertAdjacentHTML` (no interpolation ⇒ no injection surface). ZERO raw U+2197 now reaches the DOM (verified on the live page: `document.body.innerText` contains none). **(2) ERASE KEY ON THE DIAL PAD** (owner: "make delete number icons so if you enter you can erase it"): an erase key now occupies the keypad's bottom-right cell, replacing `#` — pure decoration here, since the pad only accepts 0-9 for a 6-digit RELAY number and `#` did nothing but play a tone. It dims to .35 when there is nothing to erase, erases one digit per tap, and no-ops when empty. A REAL BUG was caught mid-build and fixed: the first implementation put the button beside the number display (absolute, `inset-inline-end`) — in English it covered the 6th placeholder dot and in ARABIC, where it mirrors to the leading edge, it landed ON TOP of the first digit (the display is too wide to share that row). Moving it into the grid removes the overlap by construction and gives a full 54px touch target. **(3) AUDIBLE KEY TONES**: DTMF existed but was effectively silent for two reasons, both fixed — the oscillators were scheduled at `ac.currentTime` in the SAME tick as the ASYNC `ac.resume()`, so on iOS the note's start time had already elapsed by the time the context ran (classic iOS Web Audio race), and the peak gain was 0.045 (≈ -27 dBFS). Now the context is unlocked on the first real gesture (1-sample silent buffer on `pointerdown`), a suspended context resumes and THEN schedules, every note starts at a +5ms lookahead so it can never be scheduled in the past, and the peak is 0.18; erase gets its own softer low tone (420/310Hz). HONEST LIMIT documented in-source: on iPhone the hardware mute switch silences Web Audio outright — no web page can override that. Verified headlessly on an emulated phone against the REAL built bundle: exactly 2 oscillators per keypress at the correct DTMF frequencies, peak 0.18, ZERO notes scheduled in the past, the iOS unlock buffer firing on first touch; the erase key overlapping neither the display nor any key in EITHER language; all 10 digit keys registering correctly in Arabic (a single earlier miss was proven a synthetic-tap artifact of the pad's 3D tilt — correct 5/5 by coordinate, by raw touchscreen, and 10/10 by direct dispatch). `server/v29936LandingPolish.test.ts` (15 tests). 1495 tests.) (v2.99.35 — LANDING PAGE STRUCTURALLY ALIVE (owner: "the dial pad… if you click, doesn't click. The numbers doesn't show… the Arabic tab is not active"). ROOT CAUSE traced live with an instrumented `Element.innerHTML` setter, not guessed: React 19 re-applies `dangerouslySetInnerHTML` whenever the `{__html}` OBJECT identity changes — even for a byte-identical string. `Home.tsx` passed an inline `{{__html: html}}` (fresh object every render), so the first unrelated re-render — the live-stats query resolving ~0.5s after mount — re-set innerHTML on the live DOM, rebuilding EVERY node and silently discarding all of the engine's listeners (keypad, CLEAR, DEMO, CALL, the AR/EN toggle) while the `[lang]`-keyed wiring effect saw no change and never re-ran; the pure-CSS watchdog then cleared the orphaned boot overlay, so visitors saw a normal load into a dead page (empirical timeline: engine wired t≈450ms, DOM replaced t≈565ms, same string hash / new object). This is also why the v2.99.24 "wire controls first" hardening didn't cure the symptom — the controls WERE wired; the nodes were thrown away afterward. FIX, three reinforcing layers: (1) the `{__html}` object is memoized (`dsih`) so its identity changes only when the markup truly does; (2) the wiring effect keys on `[lang, dsih]` so any future DOM swap re-wires in the same commit; (3) every dialer/lang click rides ONE DELEGATED listener on the stable host wrapper (never replaced — only its children are) instead of per-node listeners. The v2.99.15 live number-lookup + v2.99.21 RTL binding + v2.99.24 ordering all survive unchanged on top. POLISH found while verifying: `dialStatus` sat on "CHECKING NUMBER…" forever next to a RESOLVED `dialPreview` ("Sara · ONLINE") — the status line now hides while a resolved preview shows, and the fail-open (FALLBACK) paths flip it to "LINE READY" instead of a stale "checking". ALSO: `index.html` shipped a literal `%VITE_ANALYTICS_ENDPOINT%/umami` script whenever the env was unset (vite keeps unknown %VARS% verbatim) — every production load fetched the bogus URL, 400 + strict-MIME console error; tag removed, `main.tsx` injects analytics at runtime only when configured. Verified headlessly against the REAL built bundle (tRPC-batch-shaped lookup stubs): keypad digits render after the killer re-render, found-online armed / found-offline disarmed / party-line "JOIN CALL" / not-found disarmed / lookup-outage fail-open armed, EN→AR and AR→EN both flip dir + copy with the keypad live in RTL, CALL navigated into the `/i/<n>` name-only join flow; screenshots: EN-found ("Sara · ONLINE"), EN-notfound, AR-found. `server/v29935LandingAlive.test.ts` (9 tests) + the v2.99.24 pin updated to the delegated shape. 1480 tests.) (v2.99.34 — M11: SERVER-SIDE EPHEMERAL CONTENT GATING (the final heavy-QA-sweep item; owner: "finish the M11"). Previously `messages.list` returned the full `body` + `meta` + `attachment` (url + thumbUrl) for an UN-CONSUMED view-once/disappearing message, so a recipient reading the raw tRPC response (devtools) — or fetching `attachment.url` directly — saw the secret WITHOUT ever burning it; `consumeExpiringMessage` was the only thing that nulled the content and nothing forced it, so the "view once, then gone" guarantee was defeated with no tap (the casual Copy/Reply menu vectors were already shut in v2.99.23; this closes the raw-response vector). FIX (server): `list` now WITHHOLDS the content of a LOCKED message — `locked = isExpiring && !consumedAt && senderIdentityId !== me` → `body:null`, `attachment:null`, plus a `locked` flag to the client (the sender always sees their own; a consumed row is already null and renders "disappeared"). The ONLY path to the content is the new `revealExpiring` mutation → `revealExpiringMessage` (`server/v2db.ts`): same authorization as consume (participant, not the sender, not already consumed), it CAPTURES `body`+`attachmentId` then BURNS (nulls both + sets `consumedAt`, keeps the attachments row per F3), and the router INLINES the media as a `data:` URL (reads the object via `storageGetSignedUrl` server-side, ≤30MB, base64) so the immediate burn can't race a client fetch — the old client fetch-then-burn flow (`URL.createObjectURL` + a separate `consumeExpiring.mutate`) is RETIRED. Client (`Messages.tsx`): the tap-to-view card calls `revealExpiringMutation.mutateAsync` and renders `res.body`/`res.media.dataUrl` (a data URL survives the burn — no blob/revoke), and `burned` now keys on the server `locked` flag + `consumedAt` (empty content alone no longer reads as burned, so a withheld message correctly shows the tap-to-view card). `consumeExpiring` server endpoint is kept for back-compat. `server/m11ContentGating.test.ts` (8 pins); `qaSweepV29919.test.ts` #47 + `peerIdentityBatch.test.ts` v2.96 reveal pins updated to the M11 flow. THIS CLOSES THE HEAVY-QA SWEEP: all 37 confirmed findings addressed (33 fixed + the documented accepted residuals). 1471 tests.) (v2.99.33 — OWNER FEEDBACK (screenshot): MESSAGES ROW LAYOUT + STATUS VISIBILITY. **(1) THREAD ROWS CRAMPED — NAME TRUNCATED TO "A…"**: the DIRECT thread rows put the name + tier badge + PIN + time AND the 4 quick-action buttons (unread/message/voice/video) all on ONE horizontal line, so the action cluster stole ~150px and squeezed the display name to "A…"/"Khalifa …". FIX (`client/src/pages/app/Messages.tsx`): the row is now a vertical stack (`flex flex-col`) — LINE 1 is the full-width avatar + name/badge/PIN + time + preview/typing (name gets the whole width, no longer truncates in practice; the typing indicator is unchanged), and LINE 2 carries the quick-action circles on their OWN row (indented `pl-[52px]` past the avatar, `mt-1.5`, icons bumped to size-4), with the unread count shown as a "N unread" pill. **(2) STATUS "WHEN YOU POST IT, IT DOESN'T APPEAR ON ANYONE"**: status visibility was follower-only — `statusAudienceAuthorized(requester, owner)` passed ONLY when the REQUESTER had saved the OWNER, so a freshly-posted status was invisible to the poster's OWN contacts unless they'd saved the poster back (and the story-ring on `PeerAvatar`, driven by the same feed, never lit). FIX (either-direction, WhatsApp-style, `server/v2db.ts` + `server/v2routers.ts`): a status is now visible when EITHER side saved the other (minus a block in EITHER direction) — `statusAudienceAuthorized` returns true on `iSavedThem` OR `theySavedMe`; `getStatusAudienceIds` (realtime fan-out) unions the savers with the OWNER's own saved contacts so a post lights up live on the poster's contacts; and the `status.feed` candidate set adds `getIdentityIdsWhoSaved(me.number)` (people who saved me) so their statuses appear in my strip + as rings everywhere. Composer copy updated to "visible for 24h to your contacts and anyone who's saved you". `client/src/pages/app/ownerRowStatus.test.ts` (6 pins); `qaBatch7.test.ts` M16 + `server/status.test.ts` audience pins updated to the either-direction shape. NOTE: this widens status visibility (a deliberate owner call — numbers are directory-enumerable, so someone who saves you can see your ephemeral statuses; blocks still hide both ways). 1466 tests.) (v2.99.32 — HEAVY-QA SWEEP FIXES, BATCH 10 (presence). **M12 (MED) CLOSING ONE OF TWO TABS FLIPPED THE IDENTITY OFFLINE**: presence is ONE boolean per identity, but every tab runs its own `PresenceManager` — so closing one of two open tabs of the same account fired `pagehide → sendBeacon /api/v2/offline → markOffline`, blinking the user offline to every contact, and then the surviving tab's next 30s heartbeat re-marked online (`becameOnline=true`) → a FALSE "X is back online — tap to call them now" push/toast to every watcher. FIX: a browser-scoped last-tab ref-count (new `client/src/app/tabPresence.ts`) — each tab records `tabId→ts` in a per-identity localStorage map on every VISIBLE heartbeat (`touchTab`), and `onLeave` only sends the offline beacon when no OTHER tab of the identity is fresh (`otherTabsAlive`, freshness `TAB_FRESH_MS`=45s > the 30s beat); a real close/unmount `removeTab`s its slot first. A single tab still beacons instantly (no regression to v2.89 instant-offline); localStorage is per-browser so it coordinates all tabs regardless of which fleet instance each tab's SSE hit; FAILS SAFE — any storage error → treated as "no other tab" → beacon fires (today's behaviour) and the 2-min server reaper stays the backstop. Pure decision helpers (`anyOtherTabFresh`/`pruneTabs`) are unit-tested. **L4 (LOW) REAPER SPURIOUS-OFFLINE TOCTOU**: `reapStalePresence` SELECTed the stale victims, then UPDATEd `isOnline=false WHERE isOnline=true AND lastSeenAt<cutoff` — a victim that HEARTBEAT back online between the two isn't flipped by the UPDATE (its lastSeenAt is fresh again) yet was still returned to the caller, which fanned a spurious "went offline" for a user who is actually online. FIX: after the UPDATE, re-confirm each candidate is genuinely `isOnline=false` now (`inArray(identityId, ids) AND isOnline=false`) and return only those; a re-check error falls back to the captured victims (pre-fix behaviour). `client/src/app/qaBatch10.test.ts` (9 pins incl. behavioural ref-count logic); `v2offline.test.ts` pins updated to the onClose/onVisibility restructure. This CLOSES the QA sweep on the confirmed non-architectural findings: 32 of 37 fixed. REMAINING: M11 (server-side ephemeral content-gating redesign — a larger content-delivery change, flagged to the owner as its own effort) + a few accepted residuals (documented). 1460 tests.) (v2.99.31 — HEAVY-QA SWEEP FIXES, BATCH 9 (draft + auth edges). **M6 (MED) DRAFT LOST ON A FAST THREAD SWITCH**: `useDraft` (`client/src/app/draftStore.ts`) debounced the localStorage save 500ms, and its conversation-change effect cleanup `clearTimeout`'d the pending save WITHOUT flushing — so typing then switching threads (or unmounting) within 500ms silently dropped the draft, defeating the feature's whole "navigating away mid-draft doesn't lose it" purpose. FIX: a `flush()` (backed by `draftRef`/`convRef` so it writes the CURRENT text to the CORRECT conversation) runs in the `[conversationId]` cleanup instead of a bare clearTimeout, plus a `pagehide`/`visibilitychange` flush so a reload <500ms after typing also persists. **L2 (LOW) STALE "DECLINED" BOUNCED A RETRY**: `AuthPanel`'s new-device approval poll (`sessionApprovalStatus`) is a react-query with a stable key, so its cache survived the `enabled:false` toggle and remounts; after one attempt was DENIED on another device, retrying (a NEW pending session) had the waiting-stage effect served the CACHED `denied` immediately and bounced the user back to email with a false "declined" before the server ever evaluated the new session. FIX: `utils.otpAuth.sessionApprovalStatus.reset()` right before `setStage("waiting")`, so the effect only acts on a FRESH result. **L3 (LOW) DIGIT-CORRECTION BURNED OTP ATTEMPTS**: the code input auto-fires `verifyCode` the instant it hits 6 digits, and a wrong code left `code` populated (unlike the PIN path) — so deleting one digit and retyping re-fired and consumed one of the 5 server attempts PER single-character correction, exhausting a correct-but-fat-fingered user's budget. FIX: `setCode("")` in the wrong-code catch, so a correction is a fresh 6-digit entry = exactly one attempt. `client/src/app/qaBatch9.test.ts` (5 pins). DEFERRED to the next batch: M12 (closing one of two open tabs flips the shared identity offline) + the `reapStalePresence` SELECT-then-UPDATE spurious-offline TOCTOU — grouped as a dedicated presence batch since they touch the live presence-broadcast path and need multi-tab coordination. 1451 tests. QA-sweep progress: 30 of 37 fixed.) (v2.99.30 — HEAVY-QA SWEEP FIXES, BATCH 8 (number-allocation races). **M20 (MED) CROSS-TABLE NUMBER COLLISION**: identities and party_lines share ONE 6-digit number space, and each table has its OWN `UNIQUE` key on `number` — but MySQL cannot enforce uniqueness ACROSS two tables, so `allocateNumber`/`allocatePartyLineNumber` (a check-then-insert: `numberTaken` SELECTs both tables, then the CALLER inserts) let two concurrent allocations targeting DIFFERENT tables both pass the gate and claim the SAME fresh number — both inserts then succeed (different tables, neither unique key fires) and the collision permanently SHADOWS a person (a dial resolves the party line first) or unreachables a line. FIX (`server/v2db.ts`): a new shared `number_reservations` ledger (boot-migrator `CREATE TABLE IF NOT EXISTS`, `PRIMARY KEY (number)`); a new `allocateSharedNumber(db)` — used by BOTH allocators — runs `numberTaken` (pre-existing rows) THEN atomically `INSERT`s the candidate into the ledger via `tryReserveNumber` (dup-key → the loser retries a fresh candidate; ANY other error, incl. the table not existing on a first boot, FAILS OPEN → returns true → behaves EXACTLY as pre-ledger, so the per-table unique keys + numberTaken stay the backstop and the ledger can only ADD cross-table safety). The ledger is monotonic (never trimmed) so a handed-out number is never recycled — a deleted user's number can't later misroute to someone else. **L8 (LOW) PARTY-LINE CAP RACE**: `createPartyLine`'s per-owner cap was `owned.length >= MAX_PARTY_LINES_PER_OWNER` THEN insert (check-then-act), so two concurrent creates at count 9 both passed → an owner ended with 11. FIX: the fast pre-check stays (clean over-cap error in the common case), but the cap is now enforced deterministically AFTER the insert by the new row's id-RANK — `count(*) WHERE ownerIdentityId=? AND id <= insertId`; since id is monotonic and unique, concurrent racers get DISTINCT ranks, so exactly the rows ranked > MAX self-delete (each deletes only its OWN inserted id — no double-delete) and reject, converging to exactly MAX. `server/qaBatch8.test.ts` (5 pins); `partyLines.test.ts` allocator pins updated to the shared-allocator refactor. DEFERRED: rare leaked reservations on a post-reserve insert failure (900k space, cosmetic — no reaper). 1446 tests. QA-sweep progress: 27 of 37 fixed.) (v2.99.29 — HEAVY-QA SWEEP FIXES, BATCH 7 (status stories). **M15 (MED) VIDEO/AUDIO STATUS ALWAYS RAN 5s**: the story viewer's `itemMs` already honored `item.durationMs` for video/audio, but the COMPOSER never captured or sent a duration, so `durationMs` was always null and every media status auto-advanced at the flat 5s `DEFAULT_ITEM_MS` — a 30s video note flashed by in 5s while the `<video autoPlay>` kept playing under a finished progress bar. FIX (`client/src/pages/app/Status.tsx`): new `readMediaDurationMs(file)` loads the picked/recorded file into an off-DOM media element and reads `.duration` on `loadedmetadata` (with the canonical `currentTime = 1e101` seek nudge to coerce a finite value out of Infinity-duration WebM/MediaRecorder blobs, a 3s timeout + error/`onerror` fallback to null so a weird file never hangs the post); `submit()` passes `durationMs` for video/audio kinds (server already accepts it, clamped 0–10min; the viewer caps the slide at 60s). **M16 (MED) STATUS-AUDIENCE COPY WAS BACKWARDS**: the composer toast + strip hint said a status is "visible to your contacts for 24h" — but `statusAudienceAuthorized(requester, owner)` gates on the VIEWER having SAVED the owner, so a status is actually seen by people who saved YOUR number (a coherent, WhatsApp-style follower model; the feed you browse is the mirror — statuses of people YOU saved). Copy is the only thing that was wrong-direction. FIX: poster-side copy now reads "visible for 24h to anyone who has you in their contacts" (enforcement unchanged — NO visibility change, purely corrective labeling). **L5 (LOW) PRESS-HOLD RESTARTED THE STORY**: the left/right tap-zone `<button>`s overlay the viewer body, and their `onClick` fired on release even after a press-and-HOLD (which sets `paused`), so holding to pause then releasing navigated — on the first item `prev()` just re-ran `resetTimer()`, reading as "holding restarts it". FIX: a `pressStartRef` stamped on `onPointerDown` (`Date.now()`); the tap zones navigate only when the press was a quick tap (`< HOLD_MS` = 220ms), so a hold pauses/resumes with no navigation. `client/src/pages/app/qaBatch7.test.ts` (5 pins). DEFERRED: none new. 1441 tests. QA-sweep progress: 25 of 37 fixed.) (v2.99.28 — HEAVY-QA SWEEP FIXES, BATCH 6 (contacts/directory). **M18 (MED) BLOCKED-WATCHER BACK-ONLINE BYPASS**: `directory.watchOnline` (the v2.88 call-back alert) resolved a number→identity and armed a one-shot watch with NO block check, so a user the target had BLOCKED could still register to be told — with the target's display name AND a ready-to-dial link (the `watched_online` SSE toast + the `contact-online` push) — the instant the target came back online, a clean bypass of the block on the very "reach them when they're back" path blocking is meant to shut. FIX (`server/v2routers.ts`): after the self-check, `if (await isNumberBlockedBy(target.id, me.number)) throw NOT_FOUND` reusing the EXACT "isn't a RELAY user yet" message so the block is never revealed (mirrors the openThread/createGroup/call-invite block gates). **M14 (MED) FALSE "GUEST" BADGE ON A NON-USER**: `contacts.list` returned `role: "guest"` for a saved number that does NOT resolve to any identity (a made-up number, or one that never registered), so `roleFromFlags` rendered a blue "✓ Guest" seal on a non-RELAY entry — implying it's a real RELAY user. FIX: emit `role: null` (explicit) for the unresolved case → `roleFromFlags(null,…)` returns null → NO badge; a REAL identity with no admin/registered flag still defaults to "guest" (correct — they ARE a guest user). Client `ContactRow` `role` prop widened to `| null`. **M13 (MED) DELETING A BLOCKED CONTACT SILENTLY UNBLOCKS**: the block lives on the contact row (`contacts.blocked`, read by `isNumberBlockedBy`), so removing a blocked contact hard-deletes the row and silently drops the block — the person can call/message again with no notice. FIX (`client/src/pages/app/Contacts.tsx`): the "Remove contact?" confirmation now shows a warning when `deletingContact.blocked` ("removing them also unblocks them — they'll be able to call and message you again. Keep them blocked instead if you just want them out of sight."), turning a silent unblock into an informed choice (the sanctioned "warn" fix; a server tombstone was considered but would leave an invisible forever-blocked row, surprising the user who chose "remove"). `server/qaBatch6.test.ts` (6 pins); `verifiedBadge.test.ts` contacts-role pin updated to the null shape. 1436 tests. QA-sweep progress: 22 of 37 fixed.) (v2.99.27 — HEAVY-QA SWEEP FIXES, BATCH 5 (group-call invitee handling; all 9 HIGH findings shipped in v2.99.22–26, this is the MED/LOW tail). **M19 (MED) GROUP-PICKER OFF-BY-ONE**: the create-group-call picker used the TOTAL room cap (`engine.maxParticipants` = mesh 6 / SFU 10, which INCLUDES the caller) as the count of OTHERS to invite, so selecting the full cap rang one too many and the LAST acceptee hit a full room (`error{full}`). FIX: reserve the caller's slot — `MAX_PARTICIPANTS = max(1, engine.maxParticipants - 1)` in `GroupCallScreen.tsx` AND `cap = (livekitEnabled ? 10 : 6) - 1` in `programmaticGroupDial` (`relayClient.ts`). **L7 (LOW) PICKER ACCEPTED THE CALLER'S OWN NUMBER**: `toggle`/`addManual` didn't reject `engine.pin`, so the host could add themselves (silently dropped by the dial, but it burned an invitee slot at the cap); both now reject the own number. **M2 (MED) CALL-WAITING PARKED A DEAD DIAL ROOM**: answering a call-waiting invite DURING an UNANSWERED outgoing dial ran `parkActiveAsHeld()` on the empty dial room (no peer accepted) — nothing to resume, and the dialed party kept ringing into a room we'd left; Swap/Resume then entered a dead empty room. FIX (`switchCall`): when the current call is an unanswered outgoing dial (`outgoingDial && !establishedOnce`), CANCEL it (`leave` reaps the dial room + cancelPendingRings stops the outgoing ring) instead of parking it, then accept the incoming (no switch race — the leave targets the OLD dial room, the accept a DIFFERENT new room). `client/src/pages/app/qaBatch5.test.ts` (4 pins). DEFERRED: L1 (a group dial where EVERYONE declines hangs on "Ringing…" until the 65s backstop instead of failing instantly — needs outstanding-invitee tracking + real device testing). 1430 tests. QA-sweep progress: 19 of 37 fixed.) (v2.99.26 — HEAVY-QA SWEEP FIXES, BATCH 4 (storage-media). **H5 (HIGH) AVATAR LAUNDERING VIA AN ABSOLUTE URL**: `identity.updateProfile` ran the `keyInOwnerNamespace` ownership gate ONLY when `avatarUrl` was a RELATIVE `/manus-storage/…` path, but `isIdentityAvatarKey` (the `authorizeStorageKey` avatar-rescue) matches the key as a SUFFIX (`%/manus-storage/<key>`), so an ABSOLUTE `https://host/manus-storage/<victim-key>` avatarUrl slipped past the gate and, once stored, made the storage proxy serve ANOTHER user's private attachment (or burned view-once media) to anyone — the F2 gate's own comment wrongly assumed absolute URLs "never resolve through our proxy". FIX (`server/v2routers.ts`): validate the key after the LAST `/manus-storage/` (via `lastIndexOf`) so BOTH the relative and absolute shapes are gated; data:/external-CDN URLs (no `/manus-storage/`) are untouched. **M10 (MED) UNAUTHENTICATED STORAGE-PROXY DoS**: `/manus-storage/*` had no rate limit and each request could trigger an `identities` full-scan (the avatar rescue on an unindexed TEXT `avatarUrl`), so an anonymous loop over guessed `status_…`/`avatar_…` keys was an easy DB-CPU DoS. FIX (`server/_core/storageProxy.ts`): a per-IP token bucket (240 burst / ~4/s sustained — generous, since a chat screen legitimately bursts many images) runs BEFORE any key sanitize/authorize DB work and returns 429 on a flood; honors `RELAY_RATELIMIT_OFF`. `server/qaBatch4.test.ts` (2 source pins). 1426 tests. QA-sweep progress: 16 of 37 confirmed findings fixed (v2.99.22–26).) (v2.99.25 — HEAVY-QA SWEEP FIXES, BATCH 3 (three HIGH findings). **H9 (HIGH) LANDING LOOKUP ERRORS SHOWED "NO RELAY USER"**: the landing hero dialer's `onLookup` wrapper (`Home.tsx` React shell) ended in `.catch(() => null)`, so a `directory.lookup` ERROR (a shared-NAT `directoryGate` 429, a transient 500) resolved as `null` — indistinguishable from a genuine not-a-user — and the landing rendered "NO RELAY USER WITH THIS NUMBER" with CALL disabled for a REAL online user; `runLookup`'s documented fail-open `.catch` (→ `FALLBACK` → arm CALL → let `/i` re-resolve) was unreachable dead code. FIX: drop the `.catch(() => null)` so a real rejection propagates to `runLookup`'s fail-open (a genuine not-found still RESOLVES to null → correct "NO RELAY USER"; only true errors now fail open). **H4 (HIGH) STALE wasRegistration MISROUTED A LOGIN INTO A BROKEN SETUP SCREEN**: `AuthPanel`'s `wasRegistration` was set true by `submitRegister` and never reset, so a user who started a registration, went Back, then signed IN to an existing PIN-less account was routed by `verifyCode` into the "Finish setting up" screen — where `whoami` has no minted number and `setLoginPin`/`updateProfile` 401, stranding them. FIX: `setWasRegistration(false)` at the top of `routeAfterProbe` (the login-routing chokepoint, which always runs before `submitRegister` re-sets it true). **H6 (HIGH) HEARTBEAT RE-MARKED A HIDDEN TAB ONLINE**: `PresenceManager`'s 30s `tick` fired `heartbeat.mutate()` with NO visibility check, so after `onLeave` (visibilitychange→hidden) marked the user offline, the next tick re-marked them online (`becameOnline=true` → false "X is back online" pushes to every watcher) while the tab was still backgrounded. FIX: `tick` returns early when `document.visibilityState === "hidden"` (the visibilitychange handler already re-heartbeats on return to visible, so nothing is lost). `client/src/app/qaBatch3.test.ts` (4 source pins). 1424 tests.) (v2.99.24 — LANDING ROBUSTNESS: CONTROLS WIRED BEFORE THROWABLE INIT (owner: after clearing cache + reopening on a real phone, the landing "dial pad is not active" and the ع/EN language button "is not clickable"). NOT reproducible here — the exact DEPLOYED artifact (the strip-manus-runtime'd `index.html` + the real bundle) passes every headless Chromium check (desktop AND emulated phone with touch): keypad updates, toggle flips `.lp-root` to `dir=rtl` with the Noto Kufi Arabic font, no JS errors, no overlay, loader dismissed — and the sandbox network blocks ALL outbound hosts so the live site can't be loaded from here to diagnose directly. DEFENSIVE FIX regardless (`client/src/pages/Home.tsx` `startLanding`): the symptom (page renders but nothing is clickable) is the signature of the interactive wiring never attaching, so ALL interactive controls — keypad, Clear, Call, AND the language toggle — plus `syncDial()` are now wired FIRST, before any decorative/3D/audio/boot code; that decorative init (`onScroll`/`initReveals`/`initScramble`/`initMatrix`) and the boot/fx block (`fxLoop`/`runLoader`/`bootThree`) are each wrapped in try/catch, and a `dismissLoader()` on the boot-catch guarantees the opaque full-screen loader can never sit over the page swallowing taps if boot throws before it cleared. Previously `$("langBtn")` was wired AFTER `initReveals/Scramble/Matrix`, so a throw in any of those (a stricter/older browser's WebGL/AudioContext/rAF quirk) left the toggle dead. `Home.test.ts` +2 (ordering + try/catch pins). NOTE: this is a robustness hardening, not a confirmed root-cause fix — the live symptom still needs the owner's `https://your-chat.io/api/version` value (distinguishes "server not serving this build" from "browser can't run the JS"); the deployed CODE is verified sound. 1420 tests.) (v2.99.23 — HEAVY-QA SWEEP FIXES, BATCH 2 (ephemeral-message security). **H2 (HIGH) VIEW-ONCE EXTRACTABLE VIA THE MENU**: a still-LOCKED view-once/disappearing received message (`meta.expire` set, not yet tapped-to-reveal, not burned) exposed the plaintext through the received-message context menu — **Copy** wrote `m.body` straight to the clipboard and **Reply** surfaced it in the composer — and NEITHER called `consumeExpiring`, so the card never burned and the secret was re-copyable indefinitely. Compounding it, `replyingTo` omitted `meta`, so `previewOf`'s disappearing-guard was bypassed and the reply bar printed the raw body. FIX (`client/src/pages/app/Messages.tsx`): suppress the whole `!mine` menu while a message is `locked` (`isExpiring && !revealed.has(m.id) && !burned` → `return null`), so Copy/Reply only exist once the recipient has tapped to view (which burns it); and carry `meta` on `replyingTo` (type + setter + draft-reconstruct) so `previewOf` masks it to "⏱ Disappearing message" everywhere. **M3 (MED) GROUP DISAPPEARING BURNED FOR ALL**: a disappearing message in a GROUP is one shared row — the FIRST member to open it burns it for EVERYONE (the rest see "This message has disappeared"). Until per-recipient burn exists, the composer toggle is now gated to 1:1 (`!isGroup`), the convo-switch effect keeps `expire` null on entry, and the send path re-guards (`const exp = isGroup ? null : expire`). **M5 (MED) REPLY TARGET LEAKED ACROSS THREADS**: `replyingTo` wasn't reset on `conversationId` change, so a "Replying to Alice" banner from one thread persisted into the next and posted with the wrong `replyToId`; a `useEffect(..., [conversationId])` now clears it (the draft-reconstruct effect re-hydrates the new thread's own saved reply). `client/src/pages/app/qaBatch2.test.ts` (5 source pins). DEFERRED: M11 (full server-side content-gating-until-consume — needs a consume-to-reveal content-delivery redesign; the menu is now closed and `previewOf` masks, so the casual UI-extraction vector is shut). 1418 tests.) (v2.99.22 — HEAVY-QA SWEEP FIXES, BATCH 1 (the owner "activate the heavy QA" run: 57 agents, 43 candidates, 37 adversarially-verified confirmed; this batch takes the clearest HIGH findings). **H1 (HIGH) GROUP CALL COLLAPSED ON A DECLINE**: a group dial rings A/B/C off ONE room; while everyone rings, the caller's room is still size 1 (nobody accepted). The `reject` handler (`server/relay.ts`) reaped that size-1 non-party-line room via `leaveRoom` the instant ANY invitee declined — but `leaveRoom` doesn't `cancelPendingRings`, so B/C kept ringing with `pendingRings` pointing at a DELETED room and got `error{gone}` on accept, silently killing the whole conference over one decline (distinct from v2.99.19 #46, which was the OFFLINE-first-invitee client path). FIX: only reap when the caller is ringing NOBODY else — added `&& target.ringing.size === 0` (the decliner was already removed from `target.ringing` just above), so an in-progress group dial survives a decline. **H7/H8/M1 (HIGH) BLOCKED-CALLER MISSED-CALL BYPASS**: `onMissedCall` (`server/_core/index.ts`) recorded History + fired a "Missed call from B" push/email with NO block check — so a blocked caller's offline/declined dials were a repeatable push/email/history-injection channel (onInvite/onPageCallee already block-guard the live path; this one was missed). FIX: `if (await isNumberBlockedBy(callee.id, info.callerPin)) return;` at the top of the hook (fail-open on DB error). **H3 (HIGH) DEAD THREAD SEARCH**: the Messages thread-list `useMemo` filtered by `threadSearch` but omitted it from the dep array (`[threads.data, me]`), so typing re-rendered but returned the cached unfiltered list (threads.data is stable via structural sharing) — search did nothing. FIX: add `threadSearch` to the deps. **M8 (MED) PREMATURE SESSION LOGOUT**: the v2.99.19 session reaper's 95-day idle cutoff was shorter than the 365-day (`SESSION_TTL_MS`) default cookie, so a user idle 95–365 days had their session row reaped → `sessionState` read it as revoked → force-logged-out up to ~270 days early. FIX: raise the idle cutoff to 372 days (365 + 7 grace) so a row outlives every cookie it backs (corrects #51(d)). `server/qaBatch1.test.ts` (5 — behavioural relay tests for H1 + source pins). DEFERRED to later batches: H8's missed-call email THROTTLE (needs a cooldown column), and the remaining 33 findings (view-once menu leak, presence re-online, auth approval edges, etc.). 1413 tests.) (v2.99.21 — ARABIC TOGGLE ACTUALLY ACTIVATES (owner: "the Arabic language section, when you click the button, it's not active"). ROOT CAUSE — the v2.99.16 Arabic parity was DEAD ON ARRIVAL: all its CSS (`.lp-root[dir="rtl"] *{font-family:'Noto Kufi Arabic'…}` + the LTR-island monospace override) is scoped to `.lp-root[dir="rtl"]`, but `dir="rtl"` was only ever stamped on the INNER `[data-lp="root"]` div (a CHILD of `.lp-root`, `client/src/pages/Home.tsx` `markup()`), while the OUTER React `.lp-root` div had NO `dir` at all — so `.lp-root[dir="rtl"]` never matched, the Arabic font/sizing never applied, and Arabic rendered in the small fallback system face (also why the #40 "renders smaller" report was never truly fixed). FIX: bind `dir` on the `.lp-root` element itself — `<div className="lp-root" dir={lang === "ar" ? "rtl" : "ltr"}>` — so the toggle flips the whole page to RTL + the correct Arabic font. HEADLESS-VERIFIED in Chromium against the built bundle (desktop AND an emulated phone with touch): clicking the ع/EN toggle flips `.lp-root` `dir` `ltr→rtl`, switches the copy to Arabic, and the heading's COMPUTED `font-family` becomes `Noto Kufi Arabic` (was the fallback before); a hit-test confirmed NO overlay covers the controls and the boot loader is fully dismissed (`display:none`, `pointer-events:none`). The landing DIAL PAD was also verified working in BOTH English (`5 5 5 · · ·`) and Arabic (`7 8 9 · · ·` after the toggle re-renders the markup) — so the keypad + toggle are functional in the current bundle; a live "can't click anything" is a STALE CACHED BUNDLE (hard-refresh). `Home.test.ts` +2 (pins the `.lp-root` dir binding). NOTE: this rebased onto a parallel session's v2.99.20 hardening pass (version bumped to avoid the collision). 1408 tests.) (v2.99.20 — HARDENING PASS 4 (owner: "expose the security bugs... I want full backend and frontend" — the dedicated `claude-security` orchestrator was handed the whole app; every candidate it and a direct manual re-read of the full backend surfaced was independently verified against source before any fix). 11 confirmed and fixed: **(1) HIGH — storage-proxy key-normalization bypass**: `authorizeStorageKey` ran its exact-string DB match against the RAW request key, while `s3PresignGetUrl` silently normalized via `sanitizeS3Key` (collapsing `a//b`→`a/b`, stripping a leading `/`) at presign time — a real private attachment's key with an extra slash inserted missed the exact-match lookup (fell into the fail-open `unknown`/avatar classification) yet still resolved to, and served, the real object with no participant check; fixed by canonicalizing the key ONCE via `sanitizeS3Key` up front in `storageProxy.ts` so authorization and serving can never disagree about which object is being requested. **(2) `openThread`/`createGroup` block bypass** — either endpoint let a caller force a brand-new DM or group thread onto a target who had blocked them (blocking only stopped `messages.send`, not thread/group creation), pointless-ifying the block; both now check `isNumberBlockedBy` before creating a FRESH thread/group (existing threads predating a block are untouched) and respond identically to "not found" so the block itself is never revealed. **(3) `push.unsubscribe` IDOR** — deleted a push-subscription row by `endpoint` alone with no ownership check, so anyone who learned a victim's endpoint string could silently kill their incoming-call/missed-call notifications; now scoped to the caller's own identity via new `deleteOwnPushSubscription` (the unscoped helper stays for `webPush.ts`'s own dead-token cleanup, which has no identity context). **(4) `attachments.register` accepted an arbitrary client `url`** — pointing an attachment row at any external/`javascript:`/`data:` URL that every client (`AttachmentView`/`FileCard`/`MediaLightbox`) then trusted and rendered unchecked (no-interaction tracking beacon / phishing open-redirect); `url` is now always derived server-side from the already-namespace-validated `storageKey`, matching what `/api/v2/upload` produces. **(5) OAuth session secret fail-open** — `getSessionSecret()` silently signed/verified sessions with an empty key when `JWT_SECRET` was unset (the S10/S11 pattern applied to every OTHER secret, missed here); now throws in production, matching the established fail-closed convention. **(6) `cidToPin` memory leak** — a disconnected signaling client whose call room later reaped left its `cidToPin` entry orphaned forever (nothing ever cleared it); a 15-min sweep now purges entries with no live client AND no active-or-held room, deliberately leaving the reconnect-window entries the auto-rejoin logic depends on untouched. **(7) Redis bus event-kind allowlist** — `relay:v2ev` has no message authentication (anything with VPC/SG reach to Redis can publish); `_handleBusV2Event` now drops any envelope whose `kind` isn't one of the known `V2Event` shapes, a cheap backstop against a forged/malformed envelope reaching the browser's SSE handler unfiltered. **(8) SMTP STARTTLS response-injection** (CVE-2011-0411 class) — the plaintext read buffer was shared across the STARTTLS upgrade boundary, so an on-path attacker could pack extra reply lines into the same segment as the genuine "220 go ahead" and have them consumed as the FIRST reply once the encrypted session started reading, forging the perceived outcome of the real dialog; `upgradeTls` now clears the buffer before the TLS handshake. **(9) weak RNG for 6-digit number allocation** — `randomDigits6` used `Math.random()` (recoverable internal state) while every other identifier in the codebase already used a CSPRNG; switched to `crypto.randomInt`. **(10) `appUrl.ts` host-header validation** — `requestOrigin()` fed an unvalidated `X-Forwarded-Host`/`Host` straight into sitemap XML and (narrowing, not eliminating) email-verification links; a `SAFE_HOST_RE` allowlist now rejects malformed/injection-shaped host values. **(11) CI/CD command injection in `aws-ops.yml`'s `ses-ssm` action** — the `SES_EMAIL`/`DOMAIN` `workflow_dispatch` free-text inputs were spliced unescaped into single-quoted command strings executed on production EC2 via SSM `RunShellScript`; a value containing a quote/semicolon could break out and run arbitrary commands under the instance role. Fixed with the same base64-encode-on-runner/decode-on-remote treatment the adjacent `DESC_B64` value already used (the sibling `iam-grant-ses` action was reviewed and found NOT vulnerable — its interpolation runs directly on the runner via safe single-pass substitution, never re-serialized through a second remote shell). New/updated tests: `storageProxy.test.ts` (+3), `awsOps.test.ts` (+3), plus source-pinned coverage for the block-bypass, push IDOR, attachment-URL, OAuth fail-closed, and cidToPin-reaper fixes. Accepted residuals (documented, not fixed this pass): Redis bus still lacks message authentication at the transport level (architectural, VPC-trust-boundary change out of scope for a bug-fix pass), cluster-leader trust of bus-forwarded `__ownedNumber`/`home` (same trust boundary), no per-account password-login lockout (only per-IP; would need new schema), upload-DoS ordering (rate limit runs after body buffering), `/api/v2/offline` has no rate limit, and anonymous `/api/relay/ice` TURN credential minting for arbitrary `who` (bandwidth-freeloading risk, only matters with an operator coturn configured). 1392 tests.) (v2.99.19 — QA-SWEEP FIXES (the heavy multi-agent QA pass #35 surfaced a cluster of real bugs, several v2.99.11 offline-rework regressions). #46 (HIGH) GROUP CALL COLLAPSED IF ANY INVITEE WAS OFFLINE: a group dial rings the FIRST invitee to CREATE the room, then the `room` ack flushes the rest — but v2.99.11 means an OFFLINE first invitee returns `error{offline}` with NO room, so the rest were never rung AND the client's fatal-error branch tore the whole dial down over one offline person. `client/src/lib/relayClient.ts`'s error handler now splits `reachErr` (offline/nonexistent/gone — the INVITEE is unreachable) from `joinErr` (self/full/forbidden — WE couldn't join): during a group-dial BOOTSTRAP (`callIsGroup && outgoingDial && !establishedOnce && !roomId && aloneInCall()`) a reachErr PROMOTES the next `pendingGroupInvites` entry as the new bootstrap (one at a time, never all-at-once → no duplicate-room race) and only `failDial`s once every invitee is exhausted; a reachErr inside an ESTABLISHED parked call (`inParkedCall()`) never ends the call; `joinErr` stays fatal to a peerless joiner (`(reachErr||joinErr) && inCall && aloneInCall()`). #47 (HIGH) VIEW-ONCE MEDIA BROKE FOR THE RECIPIENT: opening burned it (`consumeExpiring` nulls `attachmentId` → the proxy 403s the url) BEFORE it rendered, so a kept server url showed a broken image; `Messages.tsx`'s `revealExpiring` is now async — it `fetch`es the bytes into a local blob object-URL (and drops `thumbUrl`) BEFORE burning, so the reader views a local copy that survives the burn; object URLs are revoked on countdown purge / thread switch / unmount (no leak), with an "Opening…" spinner over the fetch. #48 (MED) OFFLINE RESOLVER FIRED A STALE ERROR: the invite handler's offline `onPageCallee(...).then/.catch` (`server/relay.ts`) only checked the caller still existed, not that `ctxEpoch` was unchanged — a hang-up/takeover during the await could fire a stray `error` + phantom miss into the caller's NEW context; both continuations now re-check `callerNow.ctxEpoch === ctxEpoch` (same discipline as the party-line `settle`). #49 (MED) MULTI-DEVICE HANG-UP LEFT OTHER DEVICES RINGING: the invite fans the ring to EVERY device but `cancelPendingRings` only cancelled the primary socket; it now fans `ring-cancel` to all `reg.devices` mirroring the ring fan-out. #50 (MED) NEW-DEVICE APPROVAL COULD STRAND A LOGIN: the approver device may be closed and never tap Approve — `AuthPanel`'s waiting screen now always offers a "Sign in with your PIN instead" escape (PIN bypasses approval by design) plus, after 35s, an honest "the other device may be offline or closed" note (fails toward the user getting in). #51 (LOW batch): (a) the in-call add-person offline guard accepts BOTH `offline`+`nonexistent`; (b) UNSEND cleared a phantom unread badge — `deleteMessage` (`v2db.ts`) now decrements (floored at 0) every non-sender recipient who hadn't read past the unsent message (`unreadCount` is a stored counter, not id-derived); (c) a FAILED offline-message email used to keep the cooldown watermark (suppressing notifications for the whole window) — `releaseOfflineMessageEmailClaim` rolls it back on failure so the next message retries; (d) `reapStaleSessions` (every 30 min) drops dead pending-approval rows (inflated the approval bell forever) + rows idle past the longest cookie TTL (95 days). `server/qaSweepV29919.test.ts` (14 — behavioural relay tests for #48/#49 + source-pins); two pre-existing pins updated to the refactored error handler. 1400 tests.) (v2.99.18 — FIRST+LAST NAME ON THE DIALER (#43) + ANIMATED AVATARS (#45), both owner asks surfaced in a transcript re-audit. #43: `directory.lookup` now returns `firstName`/`lastName` (from the `identities` row); the landing hero dialer composes "First Last" via `dialLookupName()` (falls back to `displayName`, still `escLp`-escaped before innerHTML). Registered users already had `displayName = "First Last"`, so this makes the "show the name first and last" explicit. #45 (owner: "an animated icon also"): NEW `client/src/lib/animatedAvatar.ts` — a dependency-free GIF89a + LZW encoder (fixed 216-colour web-safe palette, NETSCAPE2.0 loop-forever) renders a picked emoji as a looping GIF (10-frame bounce/pulse). `AvatarPicker.tsx` gains an "Animated ✨" toggle; an animated pick uploads `image/gif` through the SAME `uploadAvatarImage` bare path (bytes pass through untouched — `uploadBare` never canvas-re-encodes, so animation survives), and the v2.99.14 streaming proxy relays `image/gif` so it animates on EVERY surface (thread rows, contacts, in-call tiles, other users' previews) and syncs like a photo. Headless-verified in Chromium: the output is a valid GIF89a, decodes at 160×160, has 10 GCE frames + the NETSCAPE loop block. `animatedAvatar.test.ts` (4, Node encoder checks), `Home.test.ts` XSS pin updated. 1386 tests.) (v2.99.17 — DIALER: A NONEXISTENT NUMBER OFFERS NO ACTIONS (owner screenshot: dialing `888 888` showed "No RELAY user with this number" YET still lit active Voice/Video/Group call buttons AND a "Save 888-888 to contacts" pill — you shouldn't be able to call, group-call, or save a number that isn't a real RELAY user). In `client/src/pages/app/Dialer.tsx` a new `nonexistent` flag = `/^\d{6}$/.test(dialed) && dialed !== myNumber && previewQuery.isSuccess && !previewIdentity` — keyed on `isSuccess` (a SUCCESSFUL public `directory.lookup` resolve to null: not a user, not a party line) so a lookup ERROR or still-loading query FAILS OPEN (actions stay enabled; the dial then surfaces the real error). `callable` gains `&& !nonexistent` (the Voice + Video buttons already `disabled={!callable}`); the Group Call button — previously ALWAYS enabled — gains `disabled={nonexistent}` (still opens the picker on an empty/partial pad, only blocked for a confirmed-dead 6-digit number); the v2.99.8 "Save to contacts" quick-add pill gains `&& !nonexistent`; and `startCallNow` early-returns on `nonexistent` defensively. Existing-but-OFFLINE users and party lines are unaffected (they resolve to a real `previewIdentity`, so `nonexistent` is false — offline users stay callable per v2.99.11's leave-a-message flow). `client/src/pages/app/dialerNonexistent.test.ts` (5). 1382 tests.) (v2.99.16 — LANDING POLISH: ARABIC SIZING PARITY (#40) + LIVE GROUP-CALL GRID (#41), both `client/src/pages/Home.tsx`. #40 (owner: "the Arabic version renders smaller than English"): ROOT CAUSE — every element hardcodes a LATIN face (`'Space Grotesk'`/`'IBM Plex Mono'`) inside its inline `font:` shorthand and `FONTS_HREF` loaded only those, so Arabic glyphs fell back to a smaller SYSTEM Arabic face and the whole RTL layout read as shrunken. FIX — load `Noto+Kufi+Arabic` in `FONTS_HREF` and force it for RTL text: `.lp-root[dir="rtl"] * { font-family:'Noto Kufi Arabic',… !important }` (inline `font:` shorthands are NOT !important, so a stylesheet `font-family !important` overrides ONLY the family — sizes/weights untouched), with a later, higher-specificity `.lp-root[dir="rtl"] [dir="ltr"], … [dir="ltr"] * { font-family:'IBM Plex Mono',… !important }` keeping the dial-display/keypad/percent LTR islands monospace. #41 (owner: "make the talk moving, not fixed images"): the 10-up group-call grid already had subtle per-tile Ken-Burns, but read as static — added a ROTATING ACTIVE-SPEAKER SWEEP: a green ring overlay (its own `<span>`, so it never fights the container's per-tile speaking box-shadow) on every tile running a shared 20s `@keyframes lpActive{0%,13%,100%{opacity:0}3%,10%{opacity:.95}}` at a staggered `-i*2s` delay, so the highlight rotates around the grid; the container also gets a subtle transform-only `lpTalk` scale pulse (COMMA-combined with any existing `spk` speaking anim so both run — a second `animation:` would clobber the first). Headless-verified the ring sweeps (tile0 opacity .95 while tile1 is 0 at the same instant). All motion stays under the existing `@media (prefers-reduced-motion: reduce){ .lp-root *{animation:none} }` gate. CAVEAT: two backticks inside a CSS comment terminated the CSS template literal (build break) — fixed by removing them; keep comments inside the `CSS` template backtick-free. `Home.test.ts` +2. 1377 tests.) (v2.99.15 — FUNCTIONAL LANDING DIALER + GUEST ONLINE-ONLY CALLS (owner: on the main page, type a number → it AUTO-shows the target's name + "he is online" → Dial → asks the visitor's name → gives a random RELAY number → rings the callee to accept — BUT only if online; an offline user can't be called by a guest). The landing hero dialer (`client/src/pages/Home.tsx`, raw-DOM `startLanding`) previously did NOT resolve the dialed number at all — it just armed CALL at 6 digits and, after a 3s loader, navigated to `/i/<n>`. Now the React shell passes an imperative `onLookup` (`utils.directory.lookup.fetch` — the PUBLIC, rate-limited resolver) into `startLanding`; when the 6th digit lands, `runLookup` resolves the owner and `applyLookup` writes a live preview into a new `data-lp="dialPreview"` node — **name + "ONLINE"** (green) for an online user, **name + "OFFLINE — you can't call them"** (CALL disabled) for an offline user, **"NO RELAY USER WITH THIS NUMBER"** for an unknown number, or **"PARTY LINE · N on the line" + JOIN** for a line. The CALL button is gated by a new `dialCallable` flag (armed only for an online user / party line / a lookup that ERRORED — fallback to `/i` which re-resolves); `callNow` also guards `if (!dialCallable) return`. Display names are `escLp`-escaped before `innerHTML` (no XSS). The "DIAL A DEMO NUMBER" gimmick cancels its in-flight lookup and arms in fallback mode so the cinematic still plays. GUEST OFFLINE BLOCK, mirrored in `OnboardingGate.tsx` (the `/i/<pin>` join card): a guest has no persistent thread to leave a message on (unlike a signed-in caller's post-dial voicemail card), so an offline callee (or unknown number) now DISABLES "Join call" (`joinBlocked = numberNotFound || calleeOffline`, party lines exempt, FAILS OPEN on a lookup error) with an honest "They're offline — can't call" + "you can reach them once they're back online" line, replacing the old "offline — we'll try to reach them" allow-through. `directory.lookup` returns only `displayName` (no first/last split) — shown as-is. `Home.test.ts` +3, `callLinkJoin.test.ts` +3. 1375 tests.) (v2.99.14 — MEDIA URL LOCKDOWN (owner: a video-note URL `https://your-chat.io/manus-storage/relay-chat/62/…_video-note_….webm` could be opened + copied OUTSIDE the app; "everything should be encrypted, cannot be traced — the file URL stays in the app"). ROOT CAUSE (per pre-fix recon) was NOT missing authorization — video/voice/file/image are participant-gated attachments and the relative `/manus-storage/<key>` URL 403s a non-participant/anon. It was that the storage proxy (`server/_core/storageProxy.ts`) answered an AUTHORIZED request with a `307` redirect to a session-independent presigned S3/Forge URL (300s TTL); following the redirect surfaced that raw URL in the address bar/devtools, and it was then copyable + replayable by ANYONE for its lifetime (no cookie). FIX: the proxy no longer redirects — it resolves the presigned URL SERVER-SIDE only, `fetch`es it, and STREAMS the bytes back through the cookie-gated route (`Readable.fromWeb(upstream.body).pipe(res)`), Range-aware (forwards `Range`, relays 206/`Content-Range`/`Accept-Ranges` so `<video>`/`<audio>` seeking works), `Cache-Control: private, max-age=60` (never `public`), `X-Content-Type-Options: nosniff`. The browser now only ever sees `/manus-storage/<key>`; no shareable storage URL is ever emitted. Also closed a fail-open: an `unknown` (orphaned/guessed) key is now refused to an anonymous caller (`kind==="unknown" && identityId==null → 403`); avatars stay semi-public (invite previews) but likewise stream (no presigned leak). Attachment + status gating unchanged. TRADEOFF: media bytes now transit the app server (S3→app→client) instead of client→S3 direct — ~2× egress on media, acceptable for RELAY's volume and the necessary cost of "stays in the app"; the 60s presigned-URL cache is kept but used only server-side. `server/_core/storageProxy.test.ts` rewritten (no-redirect, unknown+anon 403, streams), `server/storageProxy.test.ts` updated (legal key streams 200, no Location), new `server/mediaUrlLockdown.test.ts`. 1369 tests.) (v2.99.13 — EMAIL-NOTIFICATION PREFERENCES (owner, offline batch: "if he's a registered user and put his email, he'll get an email for a missed call OR when somebody sends a message while he's offline — but WITHOUT the content, just 'you received a message, log in to see it'. He can enable/disable it"). Two additive nullable `users` booleans `emailNotifyMissedCall`/`emailNotifyMessage` (+`lastMessageEmailAt` cooldown watermark) via the boot migrator; NULL = ENABLED (historical default — the missed-call email always sent), so a user disables by storing false. The existing missed-call email in `server/_core/index.ts` gains a single gate `if (user.emailNotifyMissedCall === false) return;` placed AFTER the push + `recordMissedCall` so History/push stay unconditional. NEW offline-message email: in `v2MessagesRouter.send`, for every OFFLINE recipient (1:1 or group) with a linked account email, a CONTENT-FREE `messageWaitingHtml()` nudge ("You have a new message waiting on RELAY … we don't include message contents") — no body, no sender, no thread. Throttled by `claimOfflineMessageEmail(userId, cooldownMs)` — a SINGLE atomic conditional UPDATE (stamp `lastMessageEmailAt=now` WHERE pref on `(NULL OR true)` AND cooldown elapsed `(NULL OR < cutoff)`), returning true only when `affectedRows>0` — so N messages while away send at most one email per 15-min cooldown, race-free (mirrors the v2.98.4 S1 lockout-race fix) and fail-safe (DB down → returns false → no email, send unaffected). tRPC `otpAuth.getNotificationPrefs` (normalizes NULL→on, reports `hasEmail`) + `otpAuth.setNotificationPrefs` (auth-guarded partial write via `setUserNotificationPrefs`). Profile gains an `EmailNotificationsSection` (two `role="switch"` toggles mirroring `DndSection`, optimistic update + rollback) that returns null for guests / email-less accounts. `server/emailNotifyPrefs.test.ts` (11). 1361 tests.) (v2.99.12 — OFFLINE-RETURN NOTIFICATIONS: UNREAD MESSAGES SURFACE + BLINKING ICON (owner, offline-call batch: "when he logged in again he will see the notification on the main page and also on the icon and to keep blinking if there is a message or there was in the history a missed call"). Missed calls already had the full stack (landing popup + History entry + bell/tab badges + `missedCallsSeenAt` high-water mark); this extends the on-return surfacing to unread MESSAGES and adds a BLINK. The calls-only `MissedCallToast` mount in `AppShell.tsx` is replaced by a new `AwaySummaryToast` (`client/src/app/MissedCalls.tsx`) — a "While you were away" card with a missed-calls row AND an unread-messages row, each a tappable route (→ `history?filter=missed` / → `messages`); it returns null when both are zero (`MissedCallToast` kept exported for back-compat). AppShell computes `latestUnread` (most-recent unread thread; group `title` or `peerDisplayName`/`peerNumber`) to label the messages row. The dismiss watermark keys on the latest-item TIMESTAMP per category (localStorage `relay_away_popup_seen_v2` `{missedAt,msgAt}`), NOT a count — counts are non-monotonic (they fall on History review / reading a thread), so a count high-water mark goes stale-high and would silently hide genuinely-new activity, incl. a fresh next-day login with fewer-but-new items (caught + fixed by the pre-ship adversarial review). The card opens when EITHER category's newest item is newer than what was dismissed; `dismissAway` advances both marks via `Math.max` (never backwards). The banner is a passive `role="region"` (aria-live polite), not a focus-trapping `alertdialog` (also corrected on the retained `MissedCallToast`). BLINK: the bell badge + the Messages/History tab badges (sidebar + mobile) get a `.relay-blink` opacity flash and the bell button a `.relay-blink-glow` halo whenever `missedCount + unreadCount > 0` (pending-device approvals still count in the badge total but don't strobe); the keyframes live in `client/src/index.css` behind `@media (prefers-reduced-motion: no-preference)`, so the class is inert for reduced-motion users. Realtime: the SSE `message` event already invalidates `messages.threads`, so the badge/blink/card update live (+15s poll backstop). The offline-message EMAIL notification + its enable/disable preference is the NEXT item (#34), not this one. `client/src/app/missedCalls.test.ts` +4. 1349 tests.) (v2.99.11 — OFFLINE CALL: NO AUTO-RING + LEAVE A MESSAGE (owner directive, verbatim: "if the user is offline and you try to call him it should NOT ring automatically. It will tell you he's offline but you can keep for him an SMS message or voice message"). The v2.83 PAGING model — keep the caller sitting on "Reaching their phone…" for up to 65s, fire a full-screen incoming-call Web Push to wake the pocketed device, and redeliver the ring the moment the app opens — is RETIRED for a COLD offline dial. Now: the invite handler resolves the callee identity via the (renamed-in-spirit) `onPageCallee` hook — which in `server/_core/index.ts` is now a PURE identity resolver (no `sendPushToIdentity`, no `kind:"incoming-call"` push) — and returns a FAST, honest `error{offline}` naming the callee (real identity) or `error{nonexistent}` (unknown number), with NO `ensureDialRoom`/`pendingRings`/keep-alive. The MISS is recorded immediately (`onMissedCall{reason:"cancelled"}`) so it still surfaces on the callee's History + (pref-gated, #34) missed-call notification/email when they return. The caller's client adds `"nonexistent"` to the fatal-error set and lets `server-error:offline` raise the existing post-dial card (`VoicemailPrompt`) — now offering BOTH a ≤60s voice message AND a quick written SMS (new `sendText` → openThread + `messages.send kind:"text"`), plus the "tell me when they're back online" watch. The retired paging status line (`setCallStatus("ringing","Reaching their phone…")`) is removed; a `ringing` ack now always means a real live ring (the `paging?` wire field is kept for old-server compat but never read). Note: the LIVE-path ring redelivery (`deliverPendingRing` on a mid-ring reload) is UNCHANGED. TRADEOFF FLAGGED TO OWNER: dropping the incoming-call Web Push means a closed/backgrounded browser is no longer woken to ring for an incoming call on the WEB path — this is the explicit "don't auto-ring an offline user" behavior the owner asked for; the native Android app's FCM ring-when-closed path (M4) is untouched and the `incoming-call` push kind + 70s TTL remain in `server/webPush.ts` as generic infra. `server/relayPaging.test.ts` rewritten (8 behavioral), `server/v29911OfflineCall.test.ts` (7 wiring pins), `enumBlockHardening.test.ts` E4 + `callReachability.test.ts` updated to the new behavior. 1345 tests.) (v2.99.10 — BADGE PLACEMENT + PIN EVERYWHERE (owner screenshot + note): the tier badge no longer crowds the header avatar corner (it overlapped the flag/photo) — it moved into the dropdown that opens on tap, beside the name with the PIN right under it. The 6-digit PIN now shows next to the name on every 1:1 surface: the Messages chat header, Messages thread-list rows, plus Contacts (already), Dialer preview (already), History (already), and the profile popup/full-profile (already). `badgePinSurfaces.test.ts`. 1340 tests.) (v2.99.9 — REJOIN A LIVE CALL FROM HISTORY (owner spec): a user who left a call (logout drops room membership) can rejoin from History — a "Live now · N in the call · hosted by X · Join" card. Tapping Join sends a `knock` to the signaling server, which resolves the number's live room via `liveRoomInfo(reg, number, requester)` — authorized ONLY when the requester was PREVIOUSLY in that room (`roomMeta.roster` is add-only, so no enumeration/eavesdrop oracle over the number space) — and alerts the HOST (+ co-hosts) with a `knock`. The host's RelayEngine shows an Approve/Decline prompt; `knock-approve` (moderator-guarded + pending-knock-guarded, so no forged admit) runs `admitToRoom` (join-without-ring: joinRoomMember + `joined` to the knocker + `peer-joined` fan-out + SFU token), `knock-deny` sends the knocker `knock-result{denied}`. tRPC `directory.liveRoom(number)` (caller-roster-gated, names-only — no dialable pins) powers the History card; degrades to null off the signaling node. New engine handle methods knock/approveKnock/denyKnock/setOnKnock; `RoomMeta.knocks` tracks pending. 9 relay behavioral tests + liveRejoin.test.ts. 1335 tests.) (v2.99.8 — IN-CALL UI BATCH pt.1 (owner spec): (1) MINIMIZE — an in-call Minimize control shrinks the live call to a small DRAGGABLE in-DOM box (NOT a browser PiP window) that floats over the app so Messages/History stay usable behind it; the box shows the 2-up + a live people-count (grows a little as more join) with Maximize + hang-up; the engine div is never torn down (only its position class/inline geometry changes), driven by a new `setMinimized` handle forcing the compact layout + the chrome-hide effect now gated `phase!=="idle" && !minimized`. A "Fit" toggle letterboxes video (object-fit:contain). (2) SCREEN-SHARE MAXIMIZE — a per-tile button shown only on `.screen` tiles full-bleeds the shared screen (`#videoGrid.screen-max` hides the thumb filmstrip, spotlight spans all rows); tap again / share-ends restores the grid. (3) PER-TILE ADD-TO-CONTACTS — under each unsaved remote peer's name an "Add" pill (`tile-addc`); tapping upserts via a new `setOnSaveContact` bridge + `setSavedContacts` saved-set push, and the mark disappears (already-saved peers never show it). (4) DIALPAD SAVE — the Dialer's quick-add now offers Save for ANY complete non-self, non-party-line 6-digit number (not just a resolved directory hit), as a prominent pill with a toast + an "In your contacts" confirmation when already saved. (5) END CALL caption fix (owner screenshot: it fell below the viewport) — the pre-connect controls now reserve `padding-bottom:max(60px,safe-area+48px)`. Headless-verified (grid add-pills, screen-max full-bleed + thumbs hidden, caption on-screen). 1321 tests.) (v2.99.7 — NEW-DEVICE LOGIN APPROVAL (owner spec): an email-code sign-in on a NEW device, when the account already has ANOTHER device online (a session active within 12 min), PARKS on a "Waiting for approval" screen until an existing device approves it (WhatsApp linked-device style); the 4-digit login PIN BYPASSES approval; registrations (first device) never wait. FAIL-SAFE against lockout — approval required only when a real approver exists, every gate/poll fails OPEN. Server: additive nullable `sessions.pendingApproval` (boot migrator, NULL=legacy), `sessionState` maps pending→"revoked" so the UNCHANGED createContext gate blocks it, `hasRecentApprovedSession`/`pendingSessionsForUser`/`sessionApprovalBySid`(fail-open)/`approveSession`; verifyOtp parks + emits the new `device_pending` SSE, loginWithPin never gates; procedures `sessionApprovalStatus`(public own-cookie poll)/`pendingSessions`/`approveSession` (deny=revokeSession). Client: AuthPanel "waiting" stage polling every 2.5s (no whoami invalidate while pending), useRealtime device_pending toast, the notification bell counts pending devices, Profile→Devices Approve/Decline rows. 1306 tests.) (v2.99.6 — THREE-TIER ACCOUNT BADGES (owner spec): the verified-only blue badge is superseded by a RoleBadge shown for EVERY user — blue ✓ Guest / green ✓ Registered (email-verified) / yellow ✓ Admin (users.role="admin", granted via SQL) — with the tier name in tiny type right under the mark ("Guest"/"Registered"/"Admin"). Server: `getRolesByIdentityIds()` (one batched identities⟕users query, decoration-only fail-safe) + additive `role` on whoami / directory.lookup (null for party lines) / contacts.list / messages.threads (peerRole). Client: `RoleBadge`+`roleFromFlags` (explicit role wins, null=no badge, old payloads fall back verified→Registered) wired into AppShell, Profile, Contacts, Messages (rows+header), Dialer preview, profile popup + full profile, and the incoming-ring card (tinted seal + tiny caption). 1290 tests.) (v2.99.5 — MULTI-DEVICE (owner report, 2 screenshots: same profile showed number 235-680 in the header but 911 801 on the second device's dialer; calls rang one device only). ROOT CAUSE: the register handler treated an `__ownedNumber`-verified claim to a pin held by the SAME account's other live device exactly like an unverified claim to a taken pin → `genPin()` fresh number; the `reg.devices` ring fan-out existed but was flag-gated OFF. FIX: `MULTI_DEVICE_RING: "1"` baked into ecosystem.config.cjs before the .env spread (v2.94.4 RELAY_CLUSTER pattern — fleet-identical, operator-overridable; registry lives on the cluster leader so it works across instances) + FOUR flag-on gaps closed before enabling: a registering/re-affirming SECONDARY no longer receives the primary's live-call `rejoin` (no drag-into-call), `deliverPendingRing` delivers to the registering channel's own socket (a device opened mid-ring now rings), reject fans `ring-cancel` to the number's other devices (decline on one silences all — mirror of accept), and identity-switch (logout) PROMOTES a surviving device instead of deleting the number's client record. Multi-device `ring-cancel` carries `reason:"answered"|"declined"` → client toasts "Answered/Declined on another device". 7 new relay tests. 1284 tests.) (v2.99.4 — owner batch pt.2: IN-CALL CONTROL BAR REDESIGN — every control is a COLORED icon chip with a text LABEL underneath (Mute/Unmute + Cam off/Cam on swap with the .off class like the icons do; Flip/Share/Quality/Sound/PiP/Filters/Add/Host(crown)/Chat/⋯More each in a distinct hue; full-sentence tooltips; state classes restyle the chip — JS untouched; #qualityTxt span so updateQualityBtn can't wipe the label; hang-up circle + pre-connect halo unchanged with explicit display:grid). NEW ⋯ More menu holds Record (same #recordBtn id, labeled row) + Diagnostics. PHONE SOUND MENU: Sound now opens a real Loudspeaker/Earpiece/Bluetooth route menu (v2.84's blind toggle retired) — loudspeaker = the WebAudio force (native AudioManager in the Android app), earpiece/bluetooth = drop the force so the OS default route carries audio (BT row honestly dimmed when no headset is enumerable); desktop keeps the sink list. IN-CALL CHAT: every message gets a GLASS identity chip (avatar + username + PIN + time, backdrop-blur, mine right/theirs left), chat frames carry the sender `pin` (old clients ignore it), avatars resolve once per PIN via public directory.lookup into a lifetime cache, and the composer gains a lazy-built 48-emoji palette with caret insertion behind a 😊 toggle. Headless-verified (phone+desktop: 13 buttons 0 clipped, 12 distinct colors; menus on-screen; chip alignment; pre-connect intact). 1277 tests.) (v2.99.3 — owner batch pt.1 of the in-call/profile overhaul. (1) PROFILE POPUP AVATAR CLICKABLE: the popup's avatar was inert when the peer had no status (the `clickable` computation went false inside their own popup); now it's always a button — WITH a status → status viewer, WITHOUT → a NEW full-screen profile view (148px avatar, name+verified, PIN, presence, Message/Voice/Video, Add-to-contacts) in `PeerOverlays.tsx`. (2) PRESENCE CONSISTENCY ("Maja is sometimes online, sometimes not") — two root causes: the SSE `presence` handler never invalidated `directory.presenceMany` (History LEDs) or `directory.presence` (profile popup/batch), so those surfaces lagged on their own 30s poll (both now invalidated in `useRealtime.ts`); AND the 60s stale-presence reaper flipped users offline in the DB with NO SSE event, so SSE-fed surfaces kept crashed/closed users green — `reapStalePresence` now returns the reaped identities and the boot sweep broadcasts offline presence to each one's audience (`getPresenceAudienceIds`+`publishPresenceTo`). (3) HOST-PANEL POLISH: verified every host action is real server-side (`case "mod"`: mute/mute-all/unmute-all/pin/cohost/makehost/kick, isModerator-guarded); the panel itself clipped its 5-button rows at 280px — now 320px, rows stack (name over actions) on cards, buttons wrap with per-action color accents (Mute amber/Pin sky/Co-host violet/Make host teal/Remove red), headless-verified 0 clipped. Increment 2 (control-bar redesign + full-featured in-call chat) next. 1262 tests.) (v2.99.2 — (1) PROFILE PHOTO FIX (owner: removing + re-posting a photo showed a broken image everywhere). Since v2.96.1 avatars upload via `?bare=1`, which named the object `status_…`; `authorizeStorageKey` treats every `/status_` key as rich-status media and FAILS CLOSED with no active `statuses` row — so every such avatar 403'd. Fixed: avatars now mint `avatar_…` keys (`?bare=1&avatar=1`, images-only, client sends the flag), AND the status gate rescues a `status_…` key that is some identity's CURRENT avatarUrl (isIdentityAvatarKey) — healing every already-broken photo with no migration (expired/deleted status media still fails closed; laundering-safe via the F2 keyInOwnerNamespace gate). (2) EMOJI/CHARACTER AVATARS: new `client/src/lib/emojiAvatar.ts` (56 smileys/characters/animals + 10 gradient bgs + `renderEmojiAvatar()` → 256px PNG) + `client/src/app/AvatarPicker.tsx` sheet (Upload photo · pick a character on a background · Remove→initials), wired into Profile's avatar hero AND the registration setup step. An emoji avatar is uploaded through the SAME photo path, so it renders on every surface + syncs like a photo — no schema change. Animated avatars scoped out. 1249 tests.) (v2.99.1 — MULTI-DEVICE SESSION LIST + REMOTE LOGOUT (owner): a new revocable-session model powers a Profile "Devices" list showing each signed-in device (label from User-Agent, last-active + added date, "This device" badge) with a confirm-guarded remote log-out. Fail-safe by design: the session cookie gains an OPTIONAL hex `sid` (`userId.exp.sid.hmac`; omitting it = byte-identical legacy token), a `sessions` ledger row is written per login, and createContext consults the ledger ONLY for sid-bearing cookies and FAILS OPEN on any DB error — so legacy cookies (everyone already logged in) are untouched and a DB hiccup can never mass-log-out anyone. `authCrypto.readSession`, `server/deviceLabel.ts`, v2db `recordSession`/`sessionState`/`listSessionsForUser`/`revokeSession`, otpAuth `listSessions`/`revokeSession`, `signOut` drops its own row. The optional app-lock (4-digit passcode OR Face ID/fingerprint platform passkey) already existed; a TRUE cross-device account passkey (server-side WebAuthn) remains a noted follow-up. 1241 tests.) (v2.99.0 — (1) the message ⋮/long-press options menu (Reply/Unsend) no longer clips off the LEFT screen edge on own wide bubbles (voice notes): own rows are justify-end so the ⋮ sits far-left, but the menu used `right-0` — the mine/received→right-0/left-0 mapping was reversed; fixed to open toward the interior (`mine ? "left-0" : "right-0"`, Messages.tsx). (2) REGISTRATION OVERHAUL (owner) in AuthPanel.tsx: email is collected first and shown READ-ONLY on the name step (no second email field); the post-register "Finish setting up" step shows the minted 6-digit RELAY number and makes BOTH a profile photo AND a 4-digit login passcode MANDATORY (the optional "Skip" is gone) — new registrations only, existing users not forced. Note: the optional app-lock (4-digit passcode OR Face ID/fingerprint via a real platform passkey) already exists — passcode.ts + PasscodeGate.tsx + biometric.ts + Profile's PasscodeSection. Still to come per the same owner batch: a multi-device DEVICE LIST with remote logout (needs revocable server sessions) and a true cross-device account PASSKEY (needs server-side WebAuthn). 1186 tests.) (v2.98.6 — HARDENING PASS 3 + call-latency review (owner: "improve security + call latency, no bugs anywhere, ship to main"). CALL LATENCY: reviewed every code-level lever and found the connect path already fully optimized — multi-STUN (Google×2 + Cloudflare), `iceCandidatePoolSize`/`max-bundle`/`rtcp-mux`, TURN over UDP + TCP:443 + TCP:3478 (+TLS when a cert exists), engine dynamically imported AND auto-registered on `/app` entry (first dial waits on neither chunk nor registration), heavy deps code-split — so NO code change was made (the remaining variable is a dedicated operator coturn via `TURN_SECRET`/`TURN_HOST` vs. the free `openrelay` fallback, an ops decision, not code). A fresh cross-surface security/correctness re-check (every tRPC procedure, v2db, Express routes, client rendering) found the "system-wide application" gap where the messages router had missed the throttle/membership discipline applied everywhere else, plus a call-path block gap — 4 fixed: **E1 (MED) `messages.openThread`** was an un-throttled number→identity oracle leaking display name + avatar AND planting an empty DM thread in the target's inbox per probe; added `directoryGate` (the class F5/S3–S5 walled off). **E2 (LOW-MED) `messages.createGroup`** same existence oracle (via `skipped`/BAD_REQUEST) + forced group-inbox write; `directoryGate` added. **E3 (LOW) `messages.typing`** fanned a typing indicator into ANY conversation id without a membership check (missed sibling of the S6 markRead fix); now requires `getConversationParticipantIds().includes(me.id)`. **E4 (LOW) blocking not server-enforced on the call path** — a blocked caller could still fire the callee's desktop `call_offer` notif + full-screen `incoming-call` Web Push (block was client-auto-decline + messages.send only); both notification hooks (`onInvite`/`onPageCallee`) now suppress when `isNumberBlockedBy(callee, caller)`, leaving paging/routing semantics unchanged (block not revealed). Verified-clean this pass: IDOR/ownership across send/list/search/delete/consume/contacts/status/attachments/partyLines, storage-proxy fail-closed, client XSS (names via `escapeHtml`, bodies via `linkify`, bio/socials never exposed to others), signaling authz + moderation, auth/crypto/rate-limits. New `server/enumBlockHardening.test.ts`. 1222 tests.) (v2.98.5 — RELIABILITY PASS (owner: "make it successful" — a focused bug hunt across calling/messaging/onboarding, independently verified against source before any fix): **`getOrCreateDmConversation` could permanently orphan a DM thread** — the conversation-row insert and the participant-row insert were two separate round trips with no transaction (unlike its sibling `createGroupConversation`, which already wraps both in one); a failure or slow participant insert between them left a committed conversation row with NO participants and no recovery path — every later `openThread` call hit the early-return and handed back a thread `listMessages`/`sendMessage`'s membership checks would 403 BOTH users out of, forever. Fixed by running the participant upsert unconditionally on every call (self-healing for already-orphaned rows too, and closes the race window for new ones). **Messages: scrolling back to the bottom never re-fired the read receipt** — only a new message arriving or the tab regaining focus did; catching up by scrolling alone left a thread "unread" (badge lit, sender never got ✓✓) if neither happened again; the scroll handler now debounce-fires `markRead` when the user is back near the bottom. **Call-waiting: a promoted ring didn't mirror the incoming-ring's video button** — `waitingRing` never carried the `video` flag from the original message, and the post-hangup "promote the waiting caller" block (explicitly commented as "a mirror of onRing's presentation") omitted the one line that actually resets the Video-answer button's visibility, so it kept whatever a PRIOR ring left it in; both now carry/apply `video` correctly. **`useRealtime`'s SSE handler could read a stale identity id** — the effect depended on `[enabled]` only while its `onmessage` closure captured `selfId` (used to suppress self-sent-message chimes); added `selfId` to the dependency array so the subscription is never stale. Reviewed-and-left-alone: `PresenceManager`'s instant-offline-on-tab-hide is a *deliberate* trade-off (documented in its own comment) to catch mobile-Safari tab closes that don't fire `pagehide` — a debounce would defeat detection on exactly the browsers it targets, so it's untouched. New `server/reliabilityPass.test.ts` + `client/src/app/reliabilityPass.test.ts` + a pin added to `useRealtime.test.ts`. 1216 tests.) (v2.98.4 — SECURITY SWEEP ROUND 2 (owner: "maximize the bugs, fix it everywhere"): an exhaustive multi-agent audit of 12 attack surfaces, each candidate adversarially verified against source. 7 confirmed + 6 partial survived; ALL meaningful ones fixed (S1–S11), report appended to `SECURITY-AUDIT-2026-07-22.md`. **S1 (HIGH) — PIN lockout brute-force race:** `attemptPinLogin` wrote `stale_read+1`, so N concurrent wrong guesses all saw `attempts=0` and lost increments, defeating the 3-try cap on the 10^4 PIN space; fixed with a single conditional `UPDATE` (increment + lock-on-threshold) guarded on `loginPinLockedAt IS NULL`, verdict derived from the persisted post-value, lock email sent once via `affectedRows`. **S2 (MED) — `signal` relay had no room-membership check:** any registered client could push SDP/ICE to any ONLINE pin and harvest its ICE candidates (IP deanonymization) with no call/ring/consent; fixed to relay only when sender+target share a room (active `pinRoom` OR held `heldRoom`), mirroring accept/reject. **S3–S5 (LOW) — F5 enumeration gaps:** `directory.presence` (also missing guest-privacy), `directory.watchOnline` (name-harvest oracle), and `calls.logStart` (existence oracle + history-row injection) bypassed the F5 `directoryGate`; all three now gated. **S6 (LOW) — `markRead` IDOR:** the peer-message `status:"read"` flip wasn't membership-scoped (unreadCount already was), letting anyone corrupt other conversations' read receipts; `markThreadRead` now checks `conversationParticipants` in-tx, returns membership, and the router only fans out the `read` SSE when a member. **S7 (LOW) — upload DoS:** `POST /api/v2/upload` had no rate limit; added per-IP + per-identity token buckets before `storagePut`. **S8 (LOW) — Web Push SSRF:** unvalidated `endpoint` URL fed to `web-push`'s server-side request; new `isAllowedWebPushEndpoint` (https + known push-service host allowlist) on subscribe + defensively before send. **S9 (LOW) — OTP attempt race:** same lost-update class as S1 in `recordOtpFailure`; made atomic. **S10/S11 (LOW) — signing secrets failed open:** `sessionSecret`/`inboundSecret` fell back to a public constant when env unset (bare-HMAC session ⇒ trivial forgery); now fail CLOSED in production (dev/test keep the fallback), inbound webhook route also rate-limited. New `server/securitySweep.test.ts` (behavioral SSRF-allowlist + source pins) + a behavioral S2 test in `relay.test.ts`. Accepted residuals: no per-caller invite cap / pendingRings reaper (bounded benefit vs. signaling-path risk), inbound webhook signature stays opt-in. 1208 tests.) (v2.98.4 pt.1 — SECURITY AUDIT & REMEDIATION (owner-requested full review of "all app connections, front and back, and the database"; report saved as `SECURITY-AUDIT-2026-07-22.md`). 5 findings confirmed against source and ALL fixed. **F1 (HIGH) — signaling had no identity binding:** `POST /api/relay/send` keyed connections only by a client-minted `cid` and `register` granted ANY free number from the client's `msg.pin`, so an attacker could seize a victim's number while their app was closed and intercept inbound calls / spoof caller-ID. Fix: for `register` messages the handler resolves the caller's own identity number via `createContext` (session/guest cookie) and stamps a SERVER-ONLY `__ownedNumber` (any client value stripped first); the register handler binds the claimed pin to it — resolved ⇒ your own number (also self-heals a stale client post-`regenerateNumber`); null (no cookie / resolve error) ⇒ explicit claim REFUSED, fresh number allocated (fail closed); field absent (direct `handleMessage`/tests) ⇒ legacy behavior. Client now sends `x-relay-device-id` on the signaling POST so cookie-dropped ITP guests still resolve; only `register` pays the async cost (offer/answer/ICE stay fully synchronous). **F2 (MED) — avatar laundering:** `identity.updateProfile` validated only the SHAPE of `avatarUrl`, so a user could point it at another user's private `/manus-storage` attachment key and `authorizeStorageKey`'s avatar-rescue would then serve that key to anyone; fixed with a `keyInOwnerNamespace` gate on write (matches `attachments.register`/`status.post`). **F3 (MED) — view-once media served public after burn:** `consumeExpiringMessage` DELETED the attachments row, which made `authorizeStorageKey` classify the still-present S3 object as `unknown` — served by the proxy WITHOUT auth (burning made it MORE accessible); fixed by keeping the row (message `attachmentId` already nulled ⇒ participants lose access) so the key stays classified `attachment` and fails CLOSED (403) for every non-uploader. **F4 (LOW) — spoofable rate-limit key:** `clientIpOf`/`pickClientIp` trusted the LEFTMOST `X-Forwarded-For` hop (client-appendable behind the appending ALB), defeating every per-IP limiter by header rotation; fixed to trust the hop the proxy APPENDED (`trustedProxyHops()` from the right, default 1; `RELAY_TRUSTED_PROXY_HOPS` for CloudFront→ALB). **F5 (LOW) — free directory enumeration:** `directory.lookup`/`presenceMany` were public with no throttle over the 10^6 number space; added a generous per-IP `directoryGate` (120 burst / ~60/min, honors `RELAY_RATELIMIT_OFF`) — endpoints stay public so the `/i/<pin>` call-link direct-join still resolves callees. New `server/securityAudit.test.ts` (F1 behavioral + F2/F3/F5 wiring pins); `rateLimit.test.ts`/`geoSelf.test.ts`/`peerIdentityBatch.test.ts`/`relayCluster.integration.test.ts` updated. Residual/follow-up: a room-membership check on the signaling `signal` relay. 1196 tests.) (v2.98.3 — the dial screen's hang-up GLYPH is centered (owner screenshot: the white handset sat pinned to the LEFT edge of the red End Call circle) — the v2.74 pre-connect un-hide rule re-showed the button with `display:flex`, which has no justify-items, defeating `.ctrl`'s grid centering; one-word fix flex→grid, measured 1px/42px → 21.5px all around in an isolated headless render. 1181 tests.) (v2.98.2 — loader PERCENT COUNTER now moves with the bar (the v2.98.1 fix covered the fill but the number was still rAF-written textContent — replaced by a 0%–100% odometer strip swept by a compositor `lpPct` translateY animation on the same clock as lpFill, zero-JS default + per-run re-time; rAF only syncs messages/lock) + ENGLISH is the DEFAULT language for first-time visitors (owner directive — `initialLang()` no longer auto-picks Arabic from navigator.language; the ع/EN toggle + persisted choice still work both ways). 1180 tests.) (v2.98.1 — LANDING LOADER + MOBILE NAV (owner reports): (1) the boot loader's bar no longer freezes at 0% on main-thread-saturated devices — the fill is COMPOSITOR-DRIVEN (`lpFill` transform:scaleX keyframes on a full-width `#loadBar`, declared in plain CSS so it runs with zero JS, re-timed per run by runLoader; rAF now only syncs percent/messages/lock; `[dir="rtl"]` grows from the right; the reduced-motion animation-kill re-asserts the zero-JS lpAutoClear watchdog). Root cause verified headlessly: JS width writes ride rAF, which paints nothing while the main thread is blocked — the v2.95.7 watchdog then jumps the visitor past a 0% bar. (2) the AR/EN language toggle "not working" was the MOBILE NAV: `.lp-navlinks{display:none}` lost to the inline display:flex (needed !important), so desktop links wrapped and pushed the ع/EN toggle + Open-App pill off-screen; the ≤760px block now hides links for real and compacts the nav to fit 320px. 1180 tests.) (v2.98.0 — three-part owner batch, delivered from a single garbled voice report resolved by direct investigation rather than guessing: (1) PROFILE AVATAR SAVE RACE — capturing/picking a profile photo could report "done" (camera-badge spinner stops) before it actually saved. `onAvatarPick` uploaded the bytes THEN fired the `updateProfile` mutation fire-and-forget, clearing `uploading` in a `finally` that only awaited the upload step — a save failure (session hiccup, dropped request) left a real uploaded photo in storage with the profile's `avatarUrl` never actually updated, while the UI had already signalled success. Fixed by awaiting `updateProfile.mutateAsync` in the SAME try block as the upload (`client/src/pages/app/Profile.tsx`), so the spinner and the error path cover the whole pipeline, not just the first half. (2) VIDEO RECORDER HONEST LABELING — confirmed live via a headless Chromium capture with a fake camera device: this browser's `MediaRecorder.isTypeSupported("video/mp4")` returns true, but the recorder actually encodes VP9+Opus under that label (`rec.mimeType` only reveals `"video/mp4;codecs=vp9,opus"` once encoding starts — it stays the bare "video/mp4" for tens-to-hundreds of ms after construction/start, so a naive construction-time check never catches it; a real .mp4 that isn't really H.264/AAC risks a strict decoder like Safari's refusing it for the RECIPIENT). `client/src/lib/videoNote.ts`'s `recordFromStream` now forces an immediate `requestData()` flush right after `start()`, which reveals the true codec within ~2ms with no meaningful footage recorded yet, and swaps to an honest `video/webm` recorder when the label lied — verified end-to-end: the resulting blob's first four bytes are the genuine WebM/Matroska magic `0x1A 0x45 0xDF 0xA3`, not just a relabeled file. A SECOND bug was found and closed while building this fix (also reproduced headlessly before the guard existed): a `cancel()` landing in the same tick as construction — before the mislabel check had run — used to ignore the cancellation and start a fresh live recorder that nobody would ever stop again, leaking the recorder and hanging the `done` promise forever; a `cancelled` guard inside the swap branch closes the race. (3) OUTGOING-CALL HANG-UP REDESIGN (owner: "the red one for Hangout... it's not nice") — the pre-connect dial screen's lone red circle now sits on a soft ambient halo (a radial glow behind the button, distinct from the existing tight ripple ring), a richer two-tone gradient, a bigger glyph, and a real "End Call" caption underneath; all new motion is gated behind `prefers-reduced-motion`. A real bug was found and fixed while building this: the mobile media query that lets a CROWDED in-call control bar scroll (`max-height:40vh;overflow-y:auto`) was silently clipping the new halo + caption on the pre-connect screen, where there's only one button and nothing to scroll — fixed with a higher-specificity override, confirmed via a headless render (screenshot evidence). `server/v298ProfileAvatarFix.test.ts` (3 tests), `server/v298VideoMislabelFix.test.ts` (6 tests), `server/v298CallerHangup.test.ts` (6 tests); two stale pre-existing pins (`server/v2962VideoRecorder.test.ts`, `server/v2963Fixes.test.ts`) updated to match the new source shape instead of silently passing on an empty slice. 1178 tests.) (v2.97.2 — `RELAY_OTP_REGISTER_BYPASS` (owner directive, email-outage stopgap): AWS SES (ap-south-1) is sandboxed pending the owner's production-access request (confirmed pending live via the AWS account API), so every registration OTP to a non-pre-verified address is refused by AWS itself (owner screenshot: "couldn't send your code"). New env flag, read per-call, default OFF (byte-identical): when `=1`, `otpAuth.register` skips minting/emailing a code and performs the SAME account/session outcome a real `verifyOtp` would (resolve-or-create user, mark verified, unlock PIN, upgrade guest identity, set session cookie) — a deliberate TEMPORARY trust reduction the owner explicitly accepted (email ownership isn't proven at signup while it's on). Login (`requestOtp`/`verifyOtp`/`loginWithPin`) is completely unaffected — registration only. `AuthPanel.tsx`'s `submitRegister` recognizes the mutation's `bypass:true` and skips straight to the post-registration "setup" step instead of a code screen for a code that was never sent. To restore full verification once SES is approved: unset the flag on both EC2 instances + restart pm2 — no code change. `server/otpRegisterBypass.test.ts` (6 tests, proves the branch is genuinely taken via distinct DB-unavailable error paths). 1163 tests.) (v2.97.1 — CALL-HOLD OVERHAUL (owner: "answering the second call drops the first"): ROOT CAUSE — on the SFU path holding tears down the holder's LiveKit connection and the HELD party's client read the disconnect as "they left" → 1:1 auto-end (peer-hold never consulted + can arrive AFTER the disconnect). FIX: `peersHoldingUs` gates every auto-end (held tile STAYS, marked on-hold), a bare solo-1:1 SFU disconnect arms a 1.6s `armSoloEndGrace` fuse (late peer-hold/rejoin defuses; fires with the v2.94.8 held-promotion), onPeerHold restores an already-removed tile, and a REAL peer-left clears hold state + now routes to the SFU tile (old case only hit the mesh removePeer). Parked party gets light HOLD MUSIC (looped WebAudio motif, nodes stopped on resume) + an `#onHoldBar` banner (1:1 only; stacks under the held-bar). Held-bar gains **End held** → NEW server case `end-held` → `releaseHeldRoom` (members get peer-left; ACTIVE call untouched; `nohold` error when nothing held) — so Swap / Merge / End-held / end-active-resume all exist. `nameOf` falls back to `peerNamesSeen` so SFU peers are named. Behavioral end-held test + 10 pins. 1157 tests.) (v2.97.0 — FLASHY INCOMING-CALL CARD (owner spec, headless-previewed): caller's REAL photo (async public directory.lookup via `presentRingProfile`, stale-guarded, initials fallback) inside a ROTATING conic-gradient orbit + radiating halos; verified badge + flag + PIN + presence line; THREE round glossy animated buttons (Voice green bob+ripple / Video teal — wrapper `acceptVideoWrap` hides on voice dials / Decline red nudge; gloss ::before on every circle; ALL motion behind prefers-reduced-motion); "Send to voicemail" (declines → caller gets the existing voicemail offer) + "Message…" (canned replies + a TYPE-YOUR-OWN box → quick-reply bridge → declines); outgoing dial hang-up gains gloss + bob + ripple. 1146 tests.) (v2.96.3 — BELL PANEL FIT + ONE ROUND HANG-UP (owner screenshots): the notification dropdown is FIXED + viewport-clamped on phones (was right-anchored absolute w-64 off the LEFT edge) and left-anchored on the desktop sidebar; the floating "X End" pill is REMOVED (it duplicated the engine's hang-up on dial + in-call) and `.ctrl.hangup` is a true round red 58px circle (72px on the pre-connect dial screen, glass ctrl-bar shell stripped there — that shell was the reported blob). 1144 tests.) (v2.96.2 — IN-APP VIDEO RECORDER (owner screenshot: iOS "Recording video is not available while on a call" — an OS restriction on the SYSTEM camera during ANY active call, `<input capture>` included; RELAY now records IN-PAGE like WhatsApp): NEW `client/src/lib/videoNote.ts` (mirrors voiceNote — `pickVideoMime` probes video/mp4-first (Safari) then webm, NO "" fallback; `openVideoCapture` + `recordFromStream` with 1s timeslice + bounded bitrates ≈20MB/min under the 40MB cap; preview stream deliberately survives takes) + NEW `client/src/app/VideoRecordSheet.tsx` (full-screen live preview (front mirrored), flip, timer + auto-stop, Retake/Use review, camera released on EVERY exit, honest mid-video-call conflict message). Messages: image button → Record/Library chooser (when supported); clips ride the NORMAL attachment flow (caption + v2.96 expire timer apply), 60s cap. Status: third "Record" composer tab, 30s story cap. 1140 tests.) (v2.96.1 — OWNER FEEDBACK ON v2.96.0: (1) BROKEN-AVATAR ROOT CAUSE — profile photos were uploaded via the ATTACHMENT path, so the v2.95 participant gate 403'd everyone but the uploader (broken-image icons in History/profile popup for OTHER users); fixed at three layers: new uploads go `?bare=1` (`uploadAvatarImage`, no attachments row), `authorizeStorageKey` rescues LEGACY keys that are an identity's CURRENT avatar (`isIdentityAvatarKey`, relative + absolute URL shapes, on the would-be-403 path only), and `PeerAvatar` falls back to initials on img error. (2) AUTO-REFRESH — UpdateChecker reloads silently when IDLE too (dialing/ringing defer); the card is now only the <60s stale-edge loop-guard fallback. (3) IN-CALL CLARITY — mic/cam buttons carry real on/off glyphs (`.ic-on`/`.ic-off` swap on `.off`), the save-contact chip is ICON-ONLY, and the ©/version tag is hidden during calls. (4) IN-CALL CHAT REDESIGN — phones get a bottom SHEET (72dvh, rounded, safe-area composer; End pill stays clear) and every message is an avatar+name+TIME row, mine right / theirs left. (5) FLIP + PiP — stop-old-camera-first universal + 0/300/700ms acquire retries + recovery + muted-track rebind; iOS PiP waits for a decoded frame, VERIFIES the mode flipped (honest failure toast), and voice-only calls get "needs video". 1129 tests.) (v2.96.0 — PEER IDENTITY EVERYWHERE + EPHEMERAL BATCH (owner spec, 3 screenshots): (1) AVATAR PROPAGATION FIX — contacts.list merges the LIVE identity avatar over the frozen saved copy, conferenceHistory participants carry batched live avatarUrls, and the client finally renders peer photos everywhere via NEW `client/src/app/PeerOverlays.tsx`: `PeerAvatar` (photo/initials + STATUS RING: gradient = unseen story, subtle = seen; click opens the status viewer or the profile popup; `fallbackClassName` keeps History's tone tints) wired into Messages thread rows + chat header, Contacts rows, History solo/1:1-conference discs, with name-taps opening the profile from Messages header / History titles / Contacts rows (main tap, supersedes tap-to-voice) / Dialer preview. (2) STATUS REALTIME — `getStatusAudienceIds` (reverse feed rule) + a new `"status"` SSE kind published on status.post/remove; useRealtime invalidates status.feed + quiet toast with a View action; teal unseen-status dot on the Messages tab (both navs). (3) UNIVERSAL PROFILE POPUP — `PeerOverlaysHost` (mounted once in AppShell) hosts the global StatusViewer + profile Dialog (avatar/name/verified/number/presence, one-tap Message/Voice/Video, Add-to-contacts, View status). (4) QUICK-ADD CONTACTS — UserPlus on History rows, in the popup, on the Dialer preview, and IN-CALL via relayClient's new read-only `getRoster()` + the `InCallSaveContacts` chip. (5) MEDIA PREVIEWS — custom dark `VoiceNotePlayer` (seek/clock/download; MediaRecorder Infinity-duration handled) replaces the native `<audio>`; `FileCard` replaces the bare link; broken images fall back to the card; lightbox gains Download. (6) SELF-DESTRUCT — composer Timer toggle (off→1×→5s→10s→30s, per-send, incl. voice notes) → `meta.expire` (closed zod shape); recipient's locked "Tap to view" card burns the row for EVERYONE on open (`messages.consumeExpiring`: participant-only, non-sender, once; attachment ROW deleted so media access is truly revoked) while the reader keeps a local copy for the countdown (view-once: until leaving the thread); expiring content never leaks via thread previews, search, or reply quotes. 1114 tests.) (v2.95.0 — RICH USER STATUS (story-style): WhatsApp-style ephemeral status — text (colored bg), image+caption, video+caption, or audio, visible to the owner + their saved contacts for 24h. `statuses` + `status_views` tables (additive boot migrator); `status` tRPC router (post/feed/mine/remove/markViewed/viewers); a NO-attachment-row media upload mode `POST /api/v2/upload?bare=1` (image/video/audio, 40MB, `BLOCKED_MIME` enforced) so contacts can view status media without the participant-only attachment gate. Client `client/src/pages/app/Status.tsx`: story STRIP atop Messages (my ring + contacts', bright ring = unseen), COMPOSER (text/color or photo/video/audio+caption), full-screen VIEWER (progress bars, auto-advance, tap-nav, seen-by, delete). SECURITY (adversarial-review-gated before ship): status MEDIA is authorized in `authorizeStorageKey` on an ACTIVE `statuses` row + audience — anonymous / non-contact / expired / deleted all 403 (NOT the public avatar path; media is truly ephemeral at the access layer even though the S3 object lingers); the shared audience rule `statusAudienceAuthorized(requester, owner)` = owner OR (requester SAVED owner, non-blocked, AND owner didn't block requester) gates the feed, `markViewed`, and media (blocks hide statuses BOTH ways); `bgColor` is `sanitizeStatusBg`-allowlisted to color/gradient tokens (no `url()` tracking beacon); per-IP `statusGate` rate-limit + per-user 30-active cap + a 10-min expired-row reaper. Media key is ownership-gated on post via `keyInOwnerNamespace` (same as attachments.register). 1085 tests.) (v2.94.8 — MULTI-CALL FIXES from a conference audit (add/swap/hold/conference-10): (1) a HELD call is now RESUMED (via `endActiveLine()`), not dropped, when the far side leaves the active 1:1; (2) `onRingCancel` clears `waitingRing` so a cancelled call-waiting popup can't later kill your live call; (3) `error{full}`/`forbidden` are FATAL to a peerless joiner (guarded by `aloneInCall()`) so a 7th-mesh/11th-SFU accept or a full-party-line dial fails cleanly instead of stranding in a dead in-call screen; (4) the group picker reads the real transport cap via `engine.maxParticipants` (mesh 6 / SFU 10) and `programmaticGroupDial` clamps to it — no more ringing 10 on a 6-cap mesh. NEW behavioral `relayStandardCap.test.ts` covers the previously-untested standard cap at BOTH invite + accept.) (v2.94.7 — LOGIN OVERHAUL pt.2 (registered flow): the native `AuthPanel` is reskinned as a dark glass panel; PIN entry plays a "secure lock engaging" animation (`LockBadge` — ring sweep while verifying, clanks shut GREEN on success, shakes RED on wrong PIN); and a "keep me signed in" control (30/60/90 days, OFF = session-only) is wired end-to-end: `verifyOtp`+`loginWithPin` take a `remember` input (0|30|60|90) and `setSessionCookie(res, userId, ttlMs?)` maps it — 0 ⇒ a browser-SESSION cookie (no maxAge), N days ⇒ persistent, omitted ⇒ the historical 1-year default so every pre-existing caller is byte-identical. `rememberToTtlMs` + `authPanelRemember.test.ts` pin it. NOTE: the spec's "regenerate PIN + auto-sync to all contacts" ALREADY existed — `identity.regenerateNumber` → `planRenumber` propagates the new 6-digit number to every contact that saved the old one, surfaced in Profile with an AlertDialog that says "Everyone who saved you as a contact is updated automatically.") (v2.94.6 — LOGIN OVERHAUL pt.1 (guest): picking a guest name plays a full-screen high-gloss "matrix" ID-reveal — `client/src/app/MatrixReveal.tsx`, a rAF canvas rain of green glyphs behind a glass card whose assigned 6-digit number decodes digit-by-digit and settles, then "Welcome, <name>". Gated BEFORE the identity check in `OnboardingGate` so it outlasts `startGuest` flipping whoami truthy; honors reduced-motion; a CALL-LINK join SKIPS it for speed.) (v2.94.5 — CALL-LINK DIRECT-JOIN (owner: "paramount"): a shared `/i/<pin>` link (→`/app/dialer?to=<pin>`) no longer drops a not-yet-identified clicker on the full marketing/login screen. `OnboardingGate` detects the URL call target (`inviteTargetFromSearch`), resolves the callee via the public `directory.lookup` (name / online / party-line + head count), and shows ONE focused "enter your name to connect" card → straight into the dial. Registered users / active guests skip it. `callLinkJoin.test.ts` pins it.) (v2.94.4 — CROSS-INSTANCE CALLING ACTIVATED on `.io`: the leader-model signaling (v2.94.x, `server/relayCluster.ts`) was deployed but dormant — `RELAY_CLUSTER=1` was never set on the EC2 boxes, so `/api/relay/*` stayed per-process and a caller on instance A couldn't ring a callee on instance B (the in-app "calls may not connect" banner correctly showed). Fix: `RELAY_CLUSTER=1` is now baked into `ecosystem.config.cjs` (the `.io`-only pm2 config the deploy copies onto every server), BEFORE the `/home/relay/.env` spread so an operator can still override. `REDIS_URL` was already present (verified live: `/api/health` → `redisBus:true` + two distinct instance ids). After deploy `/api/health` → `"cluster":true`, the banner suppresses itself, and calls ring across instances with NO ALB pin. `.org` (single Manus instance) never uses this file. 1053 tests.) (v2.92.3 — LANDING-PAGE IMAGES BUNDLED: the marketing landing page (`client/src/pages/Home.tsx`) loaded all 15 of its images (5 app screenshots + 10 group-call avatar tiles) from `/manus-storage/*` (Manus Forge). That proxy only resolves on `.org` (Forge); on the self-hosted `.io` deploy `/manus-storage/*` now presigns against the operator's S3 bucket — where these images were never uploaded — so every landing image 404'd (307→S3→404, verified live). Fix: the 15 JPEGs are downloaded into `client/public/marketing/` and `Home.tsx` references them at `/marketing/*` — they now ship as STATIC build assets (vite copies `client/public`→`dist/public`, served by `serveStatic` on every deploy), independent of ANY object storage. Marketing images are identical for every visitor and belong with the frontend, not in per-request storage; a code comment pins that. Build-verified (all 15 in `dist/public/marketing/`). This is unrelated to user-uploaded attachments, which correctly still use S3/Forge.) (v2.92.2 — ROUND 5 (owner batch): every transactional email is now a COMPLETE, standards-compliant HTML document instead of a bare `<div>` fragment (mail clients — Gmail/Outlook — reflow/restyle fragments and mojibake non-ASCII copy with no charset). New `wrapEmailDocument(inner, title)` in `server/email.ts` adds the envelope — `<!doctype html>`, `<html lang="en">`, UTF-8 charset, a mobile viewport, `<title>`, `color-scheme:light` — around the FOUR templates: `verifyHtml` (`authLocal.ts`), `otpHtml` (`authOtp.ts`), `missedCallHtml` (`_core/index.ts`), `lockEmailHtml` (`authPin.ts`). Deliberate deviation from the spec skeleton: the body stays LIGHT (`#ffffff`), NOT the shown dark `#0b0c10` — every RELAY email is light-themed (near-black copy on light cards) and the inner HTML is emitted VERBATIM, so a dark body would render the copy unreadable; "keep styling exactly as-is" ⇒ light body. `missedCallHtml`'s title is a STATIC string (the user-controlled `callerLabel` must never reach the unescaped `${title}`; it stays escaped in-body). `stripHtml` now strips `<head>` (`\b`-guarded so it never eats a `<header>`) so the new `<title>` never leaks into the text/plain fallback — recipient plain-text is byte-identical to before. `server/emailTemplates.test.ts` verifies all four (the three importable ones behaviorally; `missedCallHtml` via source-read, since the entry self-starts) + `wrapEmailDocument`/`stripHtml` coverage in `email.test.ts`. 1016 tests.) (v2.92.1: the LAST hardcoded deployment domain is gone — `client/index.html`'s canonical is runtime-injected from `location`, `og:url`/JSON-LD `url` deleted, and `/sitemap.xml` + `/robots.txt` are now DYNAMIC routes (`server/seo.ts`, built per-request from `appBaseUrl(req)`; the static `client/public` copies are deleted and `noHardcodedDomains.test.ts` now also forbids `manus.space`/the space slug and pins the dynamic-SEO shape). 1008 tests.) (v2.92: the owner's "Round 3+4" batch — sign-in, mailer, domains, TURN. R3 — the **Manus OAuth sign-in UI is REMOVED entirely** (owner decision): the native AuthPanel (email one-time code + 4-digit PIN, v2.87) is the ONLY sign-in. Earlier versions had already put AuthPanel behind the visible CTAs; v2.92 kills the last reachable OAuth paths — main.tsx's global 401 handler no longer hard-navigates to the portal (it only logs), useAuth's default `redirectPath` is `/`, DashboardLayout's Sign in routes to `/app`, `ManusDialog.tsx` is deleted, and `getLoginUrl` itself is deleted from const.ts (a client-wide test in `verifiedBadge.test.ts` forbids OAuth-portal references outside tests). The server's `GET /api/oauth/callback` is deliberately KEPT (unreachable from UI; existing sessions stay valid). Existing Manus-OAuth users keep their accounts + numbers and sign in via email code at the same address — `server/authOtp.ts findUserByEmailAny` prefers otp/local rows then falls back to ANY user row with that email, so the OTP flow lands on the old OAuth row. R4A — SES mailer compat: **`EMAIL_FROM` is an accepted alias for `SMTP_FROM`** (`SMTP_FROM` > `EMAIL_FROM` > `SMTP_USER`; the SES SMTP username is an `AKIA…` key id SES rejects as a From) and **`MAIL_PROVIDER`** = `smtp` forces the built-in SMTP client (Resend NEVER used, even on send failure) / `resend` forces Resend (SMTP skipped) / unset = auto (SMTP-first, Resend fallback). R4B — **ZERO hardcoded deployment domains** in `server/` `client/src/` `shared/` (owner requirement): new `server/appUrl.ts` `appBaseUrl(req?)` derives `APP_URL` → `DOMAIN` (bare hostname ⇒ https) → request `x-forwarded-proto`/`-host`/Host → null — deliberately NO traffic-derived fallback (an observed-origin ledger was cut in review: x-forwarded-host bursts could poison it and steer ABSOLUTE email links; request-free contexts require env and degrade — the missed-call email omits its button, the VAPID subject stays the neutral mailto — so operators SHOULD set `APP_URL` on both deployments); converted: missed-call email links (`_core/index.ts`), authLocal verification links, the VAPID subject (`VAPID_SUBJECT` → own https origin → neutral mailto, computed per-call), and the marketing/legal pages via `client/src/lib/siteHost.ts` (Technology eyebrow/contact/©-footer, PrivacyPolicy) — `server/noHardcodedDomains.test.ts` walks the tree and forbids the literals (mobile/, scripts/, docs, tests are the deliberate exceptions). R4C — `TURN_TTL` (default 3600; NaN/non-positive now guarded) and `TURN_TCP_HOST` (`turn:<host>:443|3478?transport=tcp`, same HMAC creds) are contractually pinned by new relay tests. 1003 tests.) (v2.91: horizontal-scale building blocks for `.io`, all env-gated dormant (unset ⇒ byte-identical to v2.90; `.org` untouched). ZERO-DEP NATIVE S3 DRIVER — `server/s3.ts` implements SigV4 with `node:crypto` only (smtp.ts/fcm.ts pattern; unit-tested byte-for-byte against the official AWS `examplebucket` GET/PUT/presign vectors): when `S3_BUCKET/S3_REGION/S3_ACCESS_KEY/S3_SECRET` are set (+optional `S3_ENDPOINT`/`S3_FORCE_PATH_STYLE` for R2/MinIO, `S3_PREFIX` default `relay-chat/`, ensure-style so upload keys never double-prefix), `storagePut` PUTs straight to the bucket and returns the SAME `/manus-storage/{key}` URL shape, and the storage proxy presigns GETs locally (60s cache, 307, 300s expiry) — Forge stays the fallback (`.org`). REDIS EVENT BUS — `server/redisBus.ts` (ioredis as a regular dep, pub+sub connections), gated on `REDIS_URL`: v2events delivers SSE events locally FIRST then publishes `{targets, ev}` on `relay:v2ev` with a per-boot `INSTANCE_ID` (subscribers drop their own envelopes), so `/api/v2/events` can stay LOAD-BALANCED across instances; the signaling node mirrors busy-line + party-line counts into `relay:busypins`/`relay:plcounts` (diff-syncs from the join/leave/reap funnels coalesced per tick + 30s full re-sync + 90s TTL + API-tier write-guard), and `pinsInCallAsync`/`partyLineLiveCountsAsync` (now used by directory.lookup/presenceMany/contacts.list/partyLines.list) read the mirror only on API-tier instances (bus on, zero local relay clients). CROSS-INSTANCE RELAY ROOMS EXPLICITLY OUT OF SCOPE — the registry's call-state transitions are transactional per-process (phase 2); ALL `/api/relay/*` must hit ONE node: `docs-aws-scale-out.md` is the runbook (ALB with `relay-signaling` TG holding instance A only + priority-10 `/api/relay/*` rule above the both-instance default, idle timeout 300s, ElastiCache Redis cluster-mode-OFF one small node same VPC + SG 6379-from-app-SG, `REDIS_URL` into `/home/relay/.env` AFTER the rule — single-writer order — then `S3_*`), and `.github/workflows/aws-ops.yml` (`workflow_dispatch`-ONLY; graceful no-secrets failure) automates `verify` (read-only status) / `cloudfront` (us-east-1 ACM cert with printed validation CNAMEs → create-or-update distribution: ALB origin https-only+AllViewer, CachingDisabled default, `/assets/*` CachingOptimized, SSE paths CachingDisabled with 60s origin timeout, http2and3 — prints the DNS change, never touches DNS) / `alb-tune` (idle 300s + TG + rule, idempotent, prints everything). 965 tests incl. a real spawned-redis-server integration pair that auto-skips off-box.) (v2.90: AWS/`.io` deploy scaffolding, repo-side only — a `GET /api/health` liveness route, a **dormant** (`workflow_dispatch`-only) `.github/workflows/deploy.yml` that builds `main` → S3 → SSM rolling-deploys to an EC2 fleet serving `your-chat.io` in AWS Mumbai (a parallel perf-test deploy of the same branch Manus serves as `.org`), a repo-root `ecosystem.config.cjs` correcting the real esbuild entry to `dist/index.js` with `instances:1` (the signaling registry is in-memory/per-process), and `docs-aws-io-deploy.md`. Deliberately NO domain find-replace: the app is already env/Host-driven (`APP_URL` ‖ Host header ‖ `window.location.origin`), so `.io` is set by env+DNS and `.org`/Manus + the `.org` APK stay untouched; the AWS side is blocked on the owner adding a scoped IAM key. v2.89: three audit-backlog features. PHOTO DOWNSCALE + THUMBNAILS — Messages photos are canvas-downscaled pre-upload (cap 2048px, webp q≈0.85/jpeg fallback, original kept if smaller, gifs skipped) with a ≤512px thumb uploaded first (`/api/v2/upload?thumb=1`, no row) and referenced via `?thumbKey=` (owner-prefix-validated, thumbUrl derived server-side; new nullable `attachments.thumbKey/thumbUrl` via boot migrator); bubbles render the thumb with explicit width/height (no layout shift), tap opens the full url; base64 (mobile) route untouched. PARTY LINES, the signature feature — `party_lines` table shares the identity number space (both allocators check both tables); `attachRelay`'s new async `onResolveDial(pin)` hook is consulted in the invite path BEFORE identity/paging (the identity flow now lives in `runIdentityInvite()`, still fully sync without the hook), so dialing a line NEVER rings anyone: standard `room` ack + `joined` into the persistent DERIVED room `pl-<number>` (reaping an empty room never kills the line — re-dialable forever), peer-joined merges like add-person, standard 6/10 caps, conference history logs with the line's title in History; directory.lookup resolves lines first (title + live `partyLineLiveCounts`), Dialer preview shows "Party line · N on the line" with a Join button, GroupCallScreen manages lines (tRPC `partyLines` create/list/remove, guests allowed, rate-limited, 10/owner, share via /i/<number>); client `joined{partyLine}` mid-dial = the answer (callIsGroup, empty line ⇒ established immediately). INSTANT OFFLINE — PresenceManager sendBeacons `POST /api/v2/offline` (scoped text parser, createContext + body-deviceId fallback, goOffline path) on pagehide/tab-hide, heartbeats on return-to-visible; 2-min reaper stays the backstop. 876 tests.) (v2.88: a 16-item six-specialist audit batch. Correctness/scale: sign-out expires ALL session cookies via a shared `useSignOut()`, `/api/v2/upload` resolves identity through `createContext` (email-OTP members no longer 401) and gains a RAW binary path (base64 stays for old/native clients, 10MB cap) with per-route body parsers, listThreads is a groupwise-max with new hot indexes via the boot migrator, polling demotes while SSE is up, the unauthenticated SSE/ICE endpoints are rate/concurrency-capped, the storage proxy caches signed URLs, the call engine lazy-loads with stable vendor chunks, and the call screen finally loads its real fonts. UX: voice-first everywhere incl. `/i/<pin>` invite links + group calls, AlertDialog/toast instead of native confirm/alert, theme-paired History tones, grey (not red) offline LEDs. Three phone-grade features ship: a carrier-style BUSY LINE ("on a call" amber LEDs from the in-memory registry via `pinsInCall`), VOICEMAIL on no-answer/declined/offline (≤60s MediaRecorder → chat audio message with `meta:{voicemail:true}` + "Voicemail from X" push), and CALL-BACK ALERTS (`directory.watchOnline` + `online_watches` table → push + SSE toast when they're back online).) (v2.87.1: sign-out truly ends the session — device-id rotation, lands on /app. v2.87: 4-digit PIN sign-in — probe-first login screen, 3 wrong tries warn / the 4th LOCKS + emails the owner / an email-code sign-in unlocks; a BUILT-IN zero-dep SMTP mailer, SMTP_HOST/PORT/SECURE/USER/PASS/FROM, takes priority over the Resend fallback whose unverified-domain TEST MODE was why registration codes never arrived.) (Phone-app shell with Dialer/**History**/Messages/Contacts tabs, guest identities with 30-day cookies, persistent messaging with attachments, presence). The **native rewrite** (owner mandate: WhatsApp/Telegram-class, no webview) is underway at `mobile/native` — React Native 0.86 + TS, typed client to the unmodified tRPC API via device-id identity, native tab shell/screens (M1 ✅), full messaging + contacts write-parity (M2 ✅), and the CALL ENGINE PORT (M3 ✅ core): the production signaling protocol verbatim, mesh WebRTC with the v2.80/81 m-line discipline + LiveKit SFU on one @livekit/react-native-webrtc stack, staged progress incl. v2.83 paging + stale-ring rules, mutual-consent video, SFU watchdog, 1:1 auto-end, native earpiece/speaker via incall-manager (speaker default ON), full in-call UI wired to Dialer/Contacts/History — 8 interop bugs found+fixed by an adversarial review agent pre-merge; M3.5 defers hold/swap/merge, groups, rejoin (CI: native-rn.yml → RELAY-RN-debug-apk); and M4 ✅ Android rings-when-closed: the v2.86 ring layer ported to `com.relaynative` (RelayFcmService → fullScreenIntent IncomingCallActivity over the lock screen, CallService FGS at establish/hang-up, CallNativeModule.kt RN bridge, engine registers the FCM token via push.subscribe kind:"fcm"), conditional google-services apply so CI builds Firebase-less, debug id org.yourchat.relay.next ⇒ register BOTH packages in Firebase (iOS CallKit/PushKit deferred to a Mac pass); and M3.5 ✅: CALL WAITING (waiting card + v2.83 replace rules; "End call & answer" AWAITS a retried leave before accept — kills the v2.50 switch race; promote-on-death; peer-hold handled so a held SFU call parks instead of auto-ending), GROUP CALLS (dialGroup room-flush on the `room` ack, in-call ➕ add-person with the 6s offline-guard, consent bypass, aloneInCall teardown gates), REJOIN-AFTER-RESTART (28s AsyncStorage snapshot read BEFORE register, snapshot-pin register, ghost-roster backstop, shared-device hijack guard), and messaging GROUP THREADS (createGroup + routed kind) + VOICE NOTES (m4a via audio-recorder-player + rn-fs, absUrl fix for ALL attachments); and M5 ✅ Android-core: SCREEN SHARE (mesh replaceTrack hot-swap / SFU setScreenShareEnabled, `screen`→`peer-screen` spotlight, mediaProjection FGS type added only post-grant per Android 14), NATIVE PiP (onUserLeaveHint → enterPictureInPictureMode, engine-gated pipEligible), RECORDING controls (start/stop-recording + ● REC chip; server unchanged) — filters consciously descoped to the device-verified pass (no CI-checkable GPU path); and M6 ✅: QA-TEST-PLAN §F (9 RN device scenarios), native-rn.yml emits RELAY-RN-release-aab-SIGNED (same 4 secrets), versionName 3.0.0/versionCode 2 — the store swap is uploading that AAB to the EXISTING listing (same applicationId); Android track M1–M6 COMPLETE, remaining: [YOU] Firebase/QA/secrets + the device-verified pass (filters, hold/swap UI) + iOS on a Mac; plan in mobile/README.md §1. v2.86 ships the **NATIVE Android app** (user: "the TWA is an iframe — go straight to the native app"): mobile/app (renamed from mobile/ios, one shared Capacitor project) gains an Android platform + native Java call layer — full-screen lock-screen incoming ring with the app CLOSED (RelayFcmService FCM data → fullScreenIntent → IncomingCallActivity; Answer opens the app and v2.83's deliverPendingRing hands over the ring), real OS speakerphone (CallAudioPlugin/AudioManager — the engine prefers it over the WebAudio force in-app), an ongoing-call foreground service (CallService — background calls never freeze), and FCM end-to-end (CallNativePlugin token → nativeBridge → push.subscribe kind:"fcm" → push_subscriptions.kind → server/fcm.ts zero-dep FCM v1 with FIREBASE_SERVICE_ACCOUNT_JSON beside Web Push); toolchain AGP 8.9.2/Gradle 8.11.1/SDK 35; CI `native` job emits RELAY-NATIVE-debug-apk + RELAY-NATIVE-release-aab(-SIGNED); builds TODAY without Firebase (google-services conditional), rings-when-closed lights up after the runbook's 10-min Firebase [YOU] steps. v2.85 ships the **mobile store shells**: a complete Android **Trusted Web Activity** project (mobile/android — real Chrome, package org.yourchat.relay) built to an installable APK + Play AAB by `.github/workflows/android-apk.yml`, an env-driven `/.well-known/assetlinks.json` (server/wellKnown.ts, TWA_SHA256_FINGERPRINTS) for full-screen, a checked-in **Capacitor 6 iOS** Xcode project (mobile/ios — camera/mic purpose strings + background audio preset) loading the live app, and mobile/README.md + QA-TEST-PLAN.md (strategy ADR: wrappers over rewrite — parity is structural; store runbooks with the owner-only [YOU] steps; 22-scenario device QA). v2.84 fixes **mobile call audio**: the audio button is a speakerphone TOGGLE on Android+iOS (the setSinkId sink menu was an empty dead end on Android — the reported "speaker won't enable on answered calls"), phones DEFAULT speaker ON via a persisted pref primed in the Answer/dial gesture and applied at establishment (iOS's earpiece routing while the mic is live was heard as "iPhone never hears Android" one-way audio), the hang-up icon is a drawn Material call_end (the rotated-pickup transform rendered UNROTATED on some WebViews = an ANSWER icon on End), and a signature medium-loud ringtone ships as ONE shared spec (shared/ringtone.ts, peak 0.28) played by the engine and Profile's new Test-ringtone button; Profile's Notifications section now also registers Web Push ("Call alerts on") with the iOS install note. v2.83 makes calls **reach phones**: dials to a real-but-unreachable number now PAGE the callee (server keeps the dial alive with a `ringing{paging}` ack — "Reaching their phone…" — fires a **Web Push** to wake the device, and `deliverPendingRing` hands the ring over the moment the app opens; reload-mid-ring is also redelivered), the two instant pre-ring drop bugs are fixed (a stale callee `pendingRing`/`waitingRing` no longer blind-rejects the next call — same-caller/expired rings REPLACE; `sendRejoinIfInRoom` no longer reaps a LIVE dial room on mid-dial re-registers), pre-establishment SFU `Disconnected` retries instead of hanging up, failed dials show an honest reason card (~2s) before teardown, History rows carry batched presence LEDs (`directory.presenceMany`), every remaining video-default dial site is voice-first, and iOS finally RINGS: the ringtone AudioContext is gesture-unlocked at boot (was born suspended inside the SSE handler = silent), rings vibrate + flash the tab title + raise a SW notification when hidden, foregrounding reconnects the SSE instantly, and **Web Push infrastructure** ships end-to-end (push_subscriptions table, push router, VAPID derived from JWT_SECRET, sw.js push/notificationclick, incoming-call + missed-call pushes, one-time enable banner; iOS requires Add-to-Home-Screen per Apple). v2.81 ships **fast startup** (route-level code splitting — the entry chunk fell 1,941→723 kB, the docs markdown stack loads only on /docs; hashed /assets now get `immutable, max-age=1y` and index.html no-cache), the **mutual-consent video protocol** (1:1 calls are voice-first: rings for voice dials hide the video answer; camera transmits ONLY after both parties agree — video dial answered with the Video button = consent, mid-call camera tap sends `video-request` → in-call prompt → accept turns BOTH cameras on; groups bypass; answerer flips offered video m-lines to sendrecv pre-answer so consent needs no renegotiation; 1:1 calls auto-END when the peer leaves), and **guest-first login** (name + "Enter as guest" primary, passwordless email login/register secondary). v2.80 is a **multi-party A/V reliability batch** (19 fixes from a 6-participant QA report + 6-dimension audit + an empirical 6-browser matrix test): camera-less mesh peers now negotiate a null-track sendrecv video m-line (their inbound video was silently dead forever) with msid-less ontrack MERGED not wiped; remote video going quiet (mute/congestion/adaptiveStream pause) flips tiles to the avatar on BOTH paths instead of freezing; mesh camera reacquire exists now (+ honest failure states); local track death self-heals (mic reacquired + re-fed everywhere); SFU autoplay unlock is cross-platform; loudspeaker taps use fresh wrapper streams (one-participant-on-earpiece fix); `sendWS` retries dropped signaling (a lost offer was pair-fatal); mesh encoders scale with party size; the server excludes ghost members from rosters. Deferred items listed in the v2.80 changelog. v2.79 turns the **incoming-call overlay into a rich caller card** (circular avatar, name + national flag, caller PIN, split **Voice/Video answer** — voice keeps the camera off, no SFU video publication — full-width red Decline, and a **quick-reply** fold-out that sends a real chat message via `setOnQuickReply` → `messages.openThread`+`send` and declines) and makes **every missed-call notification navigate to the Missed log** (History deep-linked with `?filter=missed`: landing toast, bell panel, and the Dialer banner — now a working link). v2.78.1 fixed **calls dying seconds after dial**: `sendRejoinIfInRoom` refuses ghost rooms (zombie calls that swallowed real rings as auto-declined call-waiting), `peer-joined` now drives the caller's answer transition on both media paths, outgoing dials get a 65s no-answer backstop, and a dying call PROMOTES a waiting second caller instead of auto-declining. v2.78 makes **long chats & call logs scrollable again** on phones: `<main>` needed **`max-md:flex-none`** (a `flex-1`/basis-0 item's height property is ignored on the main axis and its CONTENT contribution inflates the auto-height root column — any page taller than the viewport blew the shell to content height, shoving the tab bar/composer below the fold with the v2.76 scroll lock removing the escape hatch), and the mobile shell is now sized by the **measured viewport** (`--relay-vh = window.innerHeight`, refreshed on resize/orientation/visualViewport; `100svh` is only the first-paint fallback) because CSS viewport units mis-reported on a real iPhone (v2.76 dvh). Both pinned in `appShellNav.test.ts`. v2.77 turns the Dialer's actions into a **trio of round icon buttons** (Voice = blue `PhoneCall`, Video = green `Video`, Group Call = purple `Users` — the "Create Group Call" text bar is gone). v2.76 locks the **document** while the app shell is mounted (`html/body.relay-app-lock` — all scrolling is internal; kills the iOS keyboard scroll-into-view shoving the app past its own end) and adds a **one-time iOS tip** after the first camera/mic grant pointing at Safari's permanent per-site Allow (aA → Website Settings) — the getUserMedia popup itself is browser policy no site can suppress. v2.75 overhauls the **History tab**: All/Dialed/Missed filter tabs with count badges, a confirm-guarded **Clear History** (per-user soft clear via an additive `identities.historyClearedAt` high-water mark — `calls.clearHistory` mutation; both history queries filter `startedAt > clearedAt`; the other party keeps their log), color-coded rows (missed **red** / dialed **green** / received **blue**), full date + precise-time-with-seconds stamps (`formatFullWhen`), durations, dialed PINs, and unanswered outgoing dials now listed ("No answer"; a conference's direction comes from roster order — the caller seeds the roster). v2.74 adds **staged caller progress** — a dedicated pre-connect dial screen (`#dialCard`, only End Call visible) walking "Calling…" (invite sent) → "Ringing…" (new server `ringing` delivery ack, carries the callee's name) → "Connecting…" (on answer) → full in-call UI ONLY on real media establishment; fixes the SFU caller showing "Connected" while still ringing (caller joins the room alone at dial — establishment now waits for the first remote `TrackSubscribed`); voice dials stay camera-off with a "Voice call" chip, video dials connect camera-live with a "Video call" chip. v2.73 **docks the mobile bottom tab bar in-flow** (no `position:fixed`, no `pb-28` clearance — page content ends exactly at the bar's top edge and the Messages composer sits pixel-0 above it) and gives each tab a **distinct active accent** (Calls green / History sky / Messages orange / Contacts purple; gradient squircle + glow, inline styles because runtime-composed Tailwind class names don't JIT). Key layout invariant since v2.73: pages fill the shell with `flex-1`, NOT `h-full` — `height:100%` does not resolve against the scroll container's flex-derived height and silently collapses short pages (pinned in `client/src/app/appShellNav.test.ts`). v2.50 makes **PiP show video on iPhone** (iOS Safari can't render a `canvas.captureStream` source in PiP, so on iOS the PiP `<video>` is fed a **real remote stream** via `webkitSetPresentationMode` — a single active speaker, not the Android 2-up composite) and folds two adversarial-review call-routing fixes (call-waiting `switchCall` no longer races a redundant `leave` against the room you just joined; auto-inviting an offline number from the in-call "+" no longer tears your call down via the primary-dial offline-teardown path). v2.49 gives the in-call "+" add-person window an **on-screen keypad that auto-invites on the 6th digit** (online → rings them in; offline/nonexistent → "That number doesn't exist or is offline."; pad self-closes) and makes **call waiting actually work** (a second caller to someone mid-call now rings through to a call-waiting popup instead of getting a "busy" bounce). v2.48 adds a **self-checking auto-updater** (`shared/version.ts` is the single source of truth, served at `GET /api/version`; a mounted-once `UpdateChecker` polls every 30s and either reloads silently mid-call — auto-rejoin keeps the call — or shows a centered reappear-on-dismiss refresh prompt when idle) and **"enable once" auto Picture-in-Picture** (persisted `relay_auto_pip`; primes the compositor at call start so the browser auto-opens a 2-up active-speaker PiP whenever the app is backgrounded mid-call). Recent batches added a **multi-device ring** (flag-gated), **persistent call membership + auto-rejoin** (refresh/step-away keeps the call; only an explicit hang-up or logout locks you out — hardened against a cross-user hijack on shared browsers), a **multi-party camera fix + mobile screen-share**, **conference call history** (the History tab: dialed number, party count, each party's name + PIN, duration, per room), and an **active-speaker / spotlight view** (the in-call grid follows the talker, auto-focuses a shared screen, tap-a-tile-to-spotlight, and collapses to a 2-up when minimized — pure decision logic in `client/src/lib/callLayout.ts`). The legacy single-screen calling UI is still reachable at `/app/call` for the actual in-call experience.> Versions v2.1–v2.30 layered on a lot since this doc's v2.0 baseline — LiveKit SFU calls, Resend missed-call email, Do-Not-Disturb, invite links, offline auto-reply, call waiting, device **passcode app-lock** + **biometric (WebAuthn) unlock**, a call-engine reliability batch (raw-track publishing to kill mobile heat, a 10s reconnect window, live top-bar status), **screen sharing**, **call recording (LiveKit Egress → S3)**, **group messaging**, an installable **PWA**, **rich contact fields** (+ an additive boot-migrator), **inbound email reply-to-thread**, **voice calls + voice↔video**, message **unsend** / **reply** / **per-conversation mute**, and **typing indicators**. See `todo.md` for the per-version changelog; treat it as the source of truth for what shipped.

---

## What's been built (high-level feature inventory)

| Area | Status | Where |
|---|---|---|
| Marketing landing page at `/` | shipped | `client/src/pages/Home.tsx` |
| In-browser calling app at `/app` | shipped | `client/src/pages/Relay.tsx` + `client/src/lib/relayClient.ts` |
| Docs page at `/docs` | shipped | `client/src/pages/Docs.tsx` (renders `client/public/RELAY-README.md`) |
| Signaling server (SSE + POST, not WebSocket — see below) | shipped | `server/relay.ts`, mounted via `attachRelay(app)` in `server/_core/index.ts` |
| 6-digit number assignment, ringing, accept/reject, mesh room management, busy detection | shipped | `server/relay.ts` (`handleMessage`) |
| Short-lived HMAC-SHA1 TURN credentials when `TURN_SECRET` + `TURN_HOST` are set | shipped | `server/relay.ts` (`iceServers()`) |
| Free public TURN fallback (`openrelay.metered.ca`) layered on top of Google STUN | shipped | `server/relay.ts` |
| WebRTC mesh client (max 6, glare-free — only newcomer sends offers) | shipped | `client/src/lib/relayClient.ts` |
| In-call text chat over WebRTC data channels | shipped | `client/src/lib/relayClient.ts` |
| Per-peer connection-state indicators, auto-ICE-restart on failure | shipped | `client/src/lib/relayClient.ts` |
| Diagnostics overlay (`?` key) with per-peer ICE/conn/gather/signaling state + rolling event log + Copy button | shipped | `client/src/lib/relayClient.ts`, markup in `Relay.tsx` |
| Front/back camera flip (`facingMode: user` ↔ `environment`) with `RTCRtpSender.replaceTrack` (no renegotiation) | shipped | `client/src/lib/relayClient.ts` (`flipCamera`) |
| Snapchat-style live filter dock (color filters via canvas filter, background blur via MediaPipe Selfie Segmentation, face overlays via MediaPipe Face Detector) | shipped | `client/src/lib/mediaPipeline.ts` |
| Glassmorphic redesigned in-call control bar | shipped | `client/src/pages/Relay.tsx` (CSS in template literal) |
| Vitest suite (signaling, paging/redelivery, party lines, persistent-rejoin, conference-history, active-speaker layout, filters, auth, v2 routers, DND, passcode, biometric, geo, recording, inbound-email, contacts, groups, mute, typing, auto-update, auto-PiP, image downscale, offline beacon, S3 SigV4 vectors, Redis bus, aws-ops pins, …) | **965 passing / 1 skipped** | `server/**/*.test.ts`, `client/src/lib/**/*.test.ts`, `client/src/app/**/*.test.ts`, `client/src/pages/**/*.test.ts` |

### v2.0 phone-app surface (added 2026-05-30)

| Area | Status | Where |
|---|---|---|
| Drizzle schema: `identities`, `presence`, `contacts`, `conversations`, `conversation_participants`, `messages`, `attachments`, `call_history`, `signaling` | shipped | `drizzle/schema.ts` |
| Unified context resolver (OAuth user OR guest cookie → `ctx.identity`) | shipped | `server/_core/context.ts` |
| Guest sessions: SESSION-scoped HttpOnly `relay_guest` cookie (v2.95 — the DB row keeps a 30-day TTL, the cookie does not), 6-digit number allocation, and an explicit Adopt-and-Retire recovery key so a browser close no longer strands the identity (v2.99.68) | shipped | `server/v2routers.ts` (`v2AuthRouter`), `server/guestRecovery.ts` |
| Directory: lookup by number, presence heartbeat, go-offline beacon | shipped | `server/v2routers.ts` (`v2DirectoryRouter`) |
| Contacts CRUD + favorites | shipped | `server/v2routers.ts` (`v2ContactsRouter`) |
| SMS-style messaging: threads, messages, sendText, sendAttachment, markRead, **reply/quote**, **unsend**, **typing indicators**, **per-conversation mute**, **group threads**. Realtime via SSE push (`/api/v2/events`) + polling fallback. | shipped | `server/v2routers.ts` (`v2MessagesRouter`), `server/v2events.ts` |
| Attachment upload `POST /api/v2/upload` (base64 body, 40 MB cap, writes via `storagePut`) | shipped | `server/v2upload.ts` |
| Call history (start/end) — independent of the legacy in-memory signaling | shipped | `server/v2routers.ts` (`v2CallsRouter`) |
| Stale-presence reaper (60s sweep, 2 min timeout) | shipped | `server/_core/index.ts` (setInterval) |
| DB helpers (Drizzle queries) | shipped | `server/v2db.ts` |
| App shell: mobile bottom-nav, desktop sidebar, sticky header with number + Upgrade button | shipped | `client/src/app/AppShell.tsx` |
| Onboarding gate: name-entry form before entering `/app/*` | shipped | `client/src/app/OnboardingGate.tsx` |
| Identity hook: whoami + 30s heartbeat + pagehide go-offline | shipped | `client/src/app/useIdentity.ts` |
| Dialer tab: fluid responsive 3×4 keypad + dialed echo + presence preview + recent calls | shipped | `client/src/pages/app/Dialer.tsx` |
| Messages tab: thread list, conversation view, emoji picker, image/video/audio/file attachments, voice-note recording, read receipts | shipped | `client/src/pages/app/Messages.tsx` |
| Contacts tab: search, sort, presence dots, last-seen, favorite, edit/delete, inline message+call | shipped | `client/src/pages/app/Contacts.tsx` |
| Design spec (OKLCH dark palette + responsive sizing rules) | shipped | `design/v2-spec.md`, generated by `scripts/gen-design-spec.mjs` via `gemini-flash-latest` |

**Known gaps in v2.0 (carry into v2.1):**
- The legacy `/app/call` Relay component does **not** read the guest cookie — users still have to type a name when entering the actual call session. Wiring the cookie into the embedded HTML/JS is a deferred port.
- Messaging is **polling-based**, not WebSocket push. Typing indicators are not implemented. The `signaling` table is created for the eventual DB-backed signaling mailbox but not yet used.
- Avatar upload UI is a placeholder (DB column exists).
- The OAuth upgrade flow migrates the guest's number into the user row, but no end-to-end vitest covers it yet (only shape/validation tests cover v2 routers).
- No vitest coverage yet for message send/read/unread, attachment roundtrip, or presence transitions. The 8 new tests in `server/v2routers.test.ts` cover the pair-key helper, validation, and whoami shape only.

The full history of features and bugs lives in `todo.md` at the repo root; check it before you start a change so you don't redo work or relitigate a closed bug.

---

## Architectural decisions (and why)

These are decisions that look unusual but were made deliberately. Don't undo them without reading the rationale.

### 1. Signaling uses Server-Sent Events + HTTP POST, not WebSocket

The original RELAY zip the user uploaded used a `ws` WebSocket server. That works on a VPS but **does not work behind the Manus production gateway (Cloudflare + Cloud Run)** for arbitrary paths — WebSocket upgrade requests get downgraded and our server returns the SPA's HTML instead. We replaced the transport with **GET `/api/relay/stream` (SSE) for server → client** and **POST `/api/relay/send` for client → server**. Both are standard HTTP and route through the gateway cleanly. The protocol semantics (`register`, `invite`, `accept`, `reject`, `signal`, `leave`, `room`, `peer-joined`, `peer-left`, `bye`, `busy`) are unchanged.

> Don't switch back to WebSocket. If you need bidirectional, extend the SSE+POST envelope.

### 2. The calling UI is a React route, not standalone HTML

We originally served the unmodified `index.html` / `app.js` from `client/public/relay/`. In production, Manus's Space Editor injects a `<manus-content-root>` Web Component and a Space Editor script that interferes with the global scope of plain HTML pages — `connectWS` ended up `undefined` on the global object, so the page stayed stuck on "Connecting…". The fix was to port the markup + CSS + behavior into a real React component (`client/src/pages/Relay.tsx` mounts the markup via `dangerouslySetInnerHTML` once; `client/src/lib/relayClient.ts` runs the WebRTC logic via `useEffect`). The Space Editor leaves React routes alone.

> If you need to add a new page, make it a React component under `client/src/pages/` and register the route in `client/src/App.tsx`. Don't add raw HTML to `client/public/`.

### 3. The local camera goes through a canvas processing pipeline

`client/src/lib/mediaPipeline.ts` takes the raw `getUserMedia` track, paints each frame into a hidden `<canvas>` (applying CSS-filter color grading, MediaPipe-segmented background blur, and MediaPipe-detected face overlays), and republishes the canvas as a `MediaStream` via `canvas.captureStream(30)`. The processed stream is what's sent to peers; we hot-swap the track on every peer via `RTCRtpSender.replaceTrack` whenever the user picks a different filter or flips the camera — **no SDP renegotiation needed**, so it's silent on the wire.

MediaPipe models (selfie segmenter, face detector) are **lazy-loaded** — only fetched the first time a filter that needs them is picked. Don't preload them at app boot.

### 4. Outgoing video is never mirrored; local self-preview is mirrored only on the front camera

The outgoing canvas stream is the source of truth and is never flipped. CSS mirrors the **local self-tile** only when the front camera is active (so movements feel natural to you); the back camera and the remote tiles always show the true orientation.

### 5. In-memory state in `server/relay.ts` only survives within one Cloud Run instance

Channels (`channels: Map<cid, RelaySocket>`) and peer registry (`peers`, `rooms`) live in memory. Cloud Run scales to zero with `min-instances=0` and can spin up additional instances at low traffic. We've seen this work in practice because RELAY's traffic is so low that one instance is typically warm. **If you add a feature that needs multi-instance coordination, externalize the state to MySQL (already wired) or a Redis-like store.** Don't pretend the in-memory map is durable.

---

## Where things live

```
relay-chat-video/
├── client/
│   ├── public/                      ← favicon, robots.txt, RELAY-README.md (docs page source). NO source code.
│   └── src/
│       ├── App.tsx                  ← Wouter routes: /, /app, /docs, /404
│       ├── main.tsx                 ← Providers; don't edit
│       ├── index.css                ← Tailwind + global tokens
│       ├── pages/
│       │   ├── Home.tsx             ← landing page
│       │   ├── Relay.tsx            ← calling UI (markup + scoped CSS template literal)
│       │   ├── Docs.tsx             ← markdown-rendered README
│       │   └── NotFound.tsx
│       ├── lib/
│       │   ├── trpc.ts              ← tRPC client (don't edit)
│       │   ├── relayClient.ts       ← WebRTC mesh client, SSE transport, diag overlay
│       │   ├── mediaPipeline.ts     ← canvas filter pipeline (color / blur / face overlays)
│       │   └── mediaPipeline.test.ts
│       └── _core/, components/, hooks/, contexts/  ← framework plumbing; avoid editing
├── server/
│   ├── _core/                       ← framework plumbing; avoid editing
│   ├── relay.ts                     ← signaling server (HTTP transport, peer registry, TURN creds)
│   ├── relay.test.ts                ← vitest for protocol
│   ├── routers.ts                   ← tRPC procedures (auth + features). Currently only auth + system.
│   ├── db.ts                        ← Drizzle helpers
│   ├── storage.ts                   ← S3 helpers
│   └── auth.logout.test.ts          ← scaffold test
├── drizzle/
│   ├── schema.ts                    ← MySQL tables (users only; extend as needed)
│   └── migrations/
├── shared/                          ← constants/types shared between client and server
├── todo.md                          ← living feature + bug log; update on every change
├── README.md                        ← template README (extensive — read once)
├── CLAUDE.md                        ← this file
└── package.json
```

---

## Build & run loop

```bash
pnpm install              # install
pnpm dev                  # NODE_ENV=development tsx watch server/_core/index.ts (serves React + tRPC + relay on port 3000)
pnpm test                 # vitest run (453 tests)
pnpm check                # tsc --noEmit
pnpm format               # prettier --write .
pnpm db:push              # drizzle-kit generate && drizzle-kit migrate
pnpm build                # vite build (client) + esbuild bundle (server) → dist/
pnpm start                # NODE_ENV=production node dist/index.js
```

**Cloud Run constraints**: Node-only build image, 1 vCPU, 512 MiB RAM, 180s request timeout, min-instances=0 (cold starts). No Python, no native binaries beyond what npm ships. The signaling SSE connection is long-lived; Cloud Run is fine with this within the 180s window (and we send a ping every 25s).

---

## Environment variables (all injected by Manus, do NOT commit `.env`)

`DATABASE_URL`, `JWT_SECRET`, `VITE_APP_ID`, `OAUTH_SERVER_URL`, `VITE_OAUTH_PORTAL_URL`, `OWNER_OPEN_ID`, `OWNER_NAME`, `BUILT_IN_FORGE_API_URL`, `BUILT_IN_FORGE_API_KEY`, `VITE_FRONTEND_FORGE_API_KEY`, `VITE_FRONTEND_FORGE_API_URL`, `VITE_APP_TITLE`, `VITE_APP_LOGO`, `VITE_ANALYTICS_ENDPOINT`, `VITE_ANALYTICS_WEBSITE_ID`.

**RELAY-specific** (optional; set when operator has their own coturn):
- `TURN_SECRET` — same `static-auth-secret` as the coturn instance
- `TURN_HOST` — public hostname/IP of coturn (port 3478). SINGLE-relay spelling.
- `TURN_HOSTS` (v2.99.61) — comma/whitespace separated LIST of relay hosts, one per availability zone
  (e.g. `13.232.119.83,13.204.23.58`). Takes precedence over `TURN_HOST`; each relay is advertised with
  every transport (UDP, TCP:443, TCP:3478, and `turns:` when `TURN_TLS=1`) and they SHARE one minted
  credential, because coturn in `use-auth-secret` mode validates the HMAC against its own
  static-auth-secret — so every relay configured with the same `TURN_SECRET` accepts it. Clients gather
  relay candidates from all of them, which is what makes a zone loss survivable without renegotiation.
  Duplicates are dropped. With a list, `TURN_TCP_HOST` is IGNORED (each relay uses its own address);
  it still applies in the single-relay case it was written for.
- `TURN_TCP_HOST` (optional, v2.92-pinned) — separate host/IP for the TCP/TLS candidates when the UDP and TCP listeners sit behind different public IPs (two L4 LBs). Falls back to `TURN_HOST`. Adds `turn:<host>:443?transport=tcp` + `turn:<host>:3478?transport=tcp` with the SAME minted HMAC credentials.
- `TURN_TTL` (optional, v2.92-pinned) — credential lifetime in seconds; default `3600`. Garbage/non-positive values fall back to 3600 (never `NaN:` usernames). The anonymous `/api/relay/ice` probe always mints 300s creds regardless.
- (also read: `TURN_PORT` default 3478, `TURN_TCP_ALT_PORT` default 443, `TURN_TLS=1` + `TURN_TLS_PORT` default 5349 to advertise `turns:`.)

These are read **per-call** in `iceServers()` so they can be added without restarting the process.

**Public base URL** (v2.92 R4B — there are ZERO hardcoded deployment domains in `server/`, `client/src/`, `client/public/`, `shared/`; pinned by `server/noHardcodedDomains.test.ts`). Server-side URLs (email links, VAPID subject) derive via `appBaseUrl(req?)` in `server/appUrl.ts`: `APP_URL` → `DOMAIN` → the request's `x-forwarded-proto`/`-host`/Host → null (degrade). There is deliberately NO traffic-derived fallback — a "most-observed request origin" ledger was removed in security review (D1): `x-forwarded-host` is attacker-controlled and the value fed absolute links in missed-call emails. Client marketing/legal pages use `window.location` (`client/src/lib/siteHost.ts`):
- `APP_URL` — explicit full origin (e.g. `https://example.org`); wins outright.
- `DOMAIN` — bare-hostname convenience for self-host `.env`s (e.g. `example.org` ⇒ `https://example.org`); second priority.
- Operators SHOULD set `APP_URL` on BOTH deployments (org: the canonical https origin; io already has `DOMAIN`) — absolute links in missed-call emails now REQUIRE one of the two; without them the email sends without its "Open RELAY" button and the VAPID subject stays the neutral mailto fallback.

**Signaling abuse backstop** (optional):
- `RELAY_RATELIMIT_OFF` — `1` to DISABLE the per-IP token-bucket rate limiter on `POST /api/relay/send`
  (`server/rateLimit.ts`, wired in `server/_core/index.ts`). The limiter is ON by default and tuned
  generously (~200 msg/s sustained, 1000 burst per IP) so only a true flood is ever throttled; set this
  only if you suspect it's interfering. The signaling handler also caps a single `message` at 256 KB.

**Multi-device ring** (optional, **experimental**; one number rings all signed-in devices):
- `MULTI_DEVICE_RING` — `1`/`true` to enable. **Default OFF**, and the off-path is byte-identical to the historical single-device behavior (a 2nd device on the same number evicts the 1st). When ON, multiple devices share a number, an incoming call rings every idle device, the first to accept wins (the rest get an "answered elsewhere" `ring-cancel`), and in-call signaling routes to the accepting device. Needs real 2-device testing before relying on it. Read per-call from `multiDeviceEnabled()` in `server/relay.ts`.

**Native S3 storage** (optional, v2.91; first-party attachment storage for self-hosted deploys — the `.io` fleet has no Manus Forge). When all four are set, `storagePut` uploads via the zero-dep SigV4 driver (`server/s3.ts`) and `/manus-storage/*` reads presign locally; when unset, the Forge flow runs unchanged (that's `.org`). Returned URLs keep the `/manus-storage/{key}` shape either way, so DB rows and clients are storage-agnostic. Read per-call like `TURN_*`:
- `S3_BUCKET`, `S3_REGION`, `S3_ACCESS_KEY`, `S3_SECRET` — all four required to enable. Keep the bucket private; browsers only ever get 300s presigned GETs via 307 redirect.
- `S3_ENDPOINT` (optional) — custom endpoint for non-AWS S3 (Cloudflare R2, MinIO, …).
- `S3_FORCE_PATH_STYLE` (optional) — `1`/`true` for R2/MinIO path-style (`endpoint/bucket/key`).
- `S3_PREFIX` (optional) — key prefix, default `relay-chat/`. Ensure-style: keys already under the prefix (all `/api/v2/upload` keys) are NOT double-prefixed, so the default produces byte-identical keys to the Forge backend.

**Round 11 (v2.99.66) — durable room registry.** In clustered mode the elected leader shadows every room into Redis (`relay:room:<id>` + the `relay:rooms` index, 6h TTL, fenced by `relay:leader:epoch`) and the NEXT leader hydrates from it before serving signaling, so losing the leader no longer ends every call fleet-wide. Fully dormant outside clustered mode (writes are gated on a fence epoch, which only leadership grants), needs no new env var, and every key self-expires. Design + the manual failover test: `docs-cross-instance-signaling.md` § Round 11. Two rules for anyone editing it: **the pin→room index is DERIVED from membership** (a member's `held` flag), never stored as its own key, so it rides one atomic write; and **`rejoin-recreate` must never trust a client-asserted roomId or role** — it is authorized by a server-minted capability (`server/roomCapability.ts`), because trusting the client there reopens the class closed by v2.99.43/M45 and v2.99.57/R-GENPIN.

**Redis event bus** (optional, v2.91; REQUIRED before running more than one instance of the app). Gated entirely on one var — absent ⇒ every bus call is a no-op and behavior is byte-identical to the single-instance build:
- `REDIS_URL` — e.g. `redis://relay-bus.xxxxx.cache.amazonaws.com:6379` (`rediss://` + auth supported via the URL). When set: v2 SSE events fan out cross-instance on `relay:v2ev` (so `/api/v2/events` can stay load-balanced), and busy-line/party-line state is mirrored into `relay:busypins`/`relay:plcounts` (90s TTL) by the signaling node for API-tier reads. NOTE the invariant: this does NOT make `/api/relay/*` scalable — signaling must still be pinned to ONE instance (ALB rule; see `docs-aws-scale-out.md`, and set `REDIS_URL` only after that rule is live).

**LiveKit SFU** (optional; enables professional 10-way calls + recording-ready media). When all three are set, call media routes through the LiveKit SFU instead of the WebRTC mesh; when unset, RELAY falls back to the mesh (≤6 comfortably). Read per-call like `TURN_*`:
- `LIVEKIT_URL` — `wss://` project URL of the LiveKit SFU
- `LIVEKIT_API_KEY` — LiveKit project API key
- `LIVEKIT_API_SECRET` — LiveKit project API secret (server-only; HMAC key for all grants; **never** sent to the browser — only short-TTL join JWTs are)

**Recording** (optional; LiveKit Egress → S3. Requires LiveKit above AND all four S3 vars. Records a room-composite grid MP4 straight to the operator's bucket — bytes never touch this server. Read per-call like `LIVEKIT_*`):
- `RECORDING_S3_BUCKET`, `RECORDING_S3_REGION`, `RECORDING_S3_ACCESS_KEY`, `RECORDING_S3_SECRET` — all four required to enable. When set, the in-call **Record** button appears and a "● REC" indicator shows for every participant.
- `RECORDING_S3_ENDPOINT` (optional) — custom endpoint for non-AWS S3 (Cloudflare R2, MinIO, …).
- `RECORDING_S3_PREFIX` (optional) — key prefix, default `recordings/`.
- `RECORDING_S3_FORCE_PATH_STYLE` (optional) — `1`/`true` for R2/MinIO.

**Email** (optional; OTP sign-in codes + missed-call notifications). Two senders — the v2.87 built-in SMTP client (`server/smtp.ts`, zero-dep, works with SES/any mailbox) and the Resend API fallback (`server/email.ts`):
- `SMTP_HOST` / `SMTP_PORT` / `SMTP_SECURE` / `SMTP_USER` / `SMTP_PASS` — enable + configure the built-in SMTP client (587 STARTTLS default; `SMTP_SECURE=1` for 465).
- `SMTP_FROM` — SMTP sender, e.g. `RELAY <no-reply@example.org>`.
- `EMAIL_FROM` (v2.92) — accepted ALIAS for `SMTP_FROM` (`SMTP_FROM` > `EMAIL_FROM` > `SMTP_USER`). SES `.env`s commonly use this name; without either From var the fallback is `SMTP_USER`, which on SES is an `AKIA…` key id that SES rejects as a sender.
- `MAIL_PROVIDER` (v2.92) — `smtp` forces the SMTP client (Resend never used, even if `RESEND_API_KEY` is set and even when an SMTP send fails); `resend` forces Resend (SMTP ignored); unset/other = auto (SMTP first when configured, Resend fallback).
- `RESEND_API_KEY` — enables Resend. Without a DNS-verified sending domain, Resend test mode only delivers FROM `onboarding@resend.dev` TO the account owner's address.
- `RESEND_FROM` — Resend sender; defaults to `onboarding@resend.dev`. Set to a verified-domain address to email arbitrary registered users.
- Email links use `appBaseUrl()` (see "Public base URL" above) — set `APP_URL` (or `DOMAIN`) on EVERY deploy; there is no hardcoded default domain and no traffic-derived fallback, so a missed-call email on an env-less deploy ships without its absolute "Open RELAY" button.

**Inbound email** (optional; reply to a RELAY notification from your inbox and it posts into the thread). When set, missed-call emails get a SIGNED `Reply-To` (`relay+{convId}.{identityId}.{hmac}@domain`); the operator points an inbound provider (Resend Inbound) at `POST /api/email/inbound`. Replies are bound to the mailbox owner (the email `From` must match the signed identity's registered address):
- `INBOUND_EMAIL_DOMAIN` — enables the feature, e.g. `inbound.your-chat.org`. Add the provider's MX records for this domain + a route/webhook to `https://<app>/api/email/inbound`.
- `INBOUND_EMAIL_LOCALPART` (optional) — default `relay` (the part before `+`).
- `INBOUND_EMAIL_SECRET` (optional) — HMAC key for the reply-address signature; falls back to `JWT_SECRET`.
- `INBOUND_EMAIL_WEBHOOK_SECRET` (optional, recommended) — the provider's Svix signing secret (`whsec_…`) to verify webhook authenticity.

---

## Coding conventions

1. **tRPC-first** for any new HTTP feature. Don't introduce `axios` or `fetch` wrappers in `client/src/` — use `trpc.feature.useQuery()` / `useMutation()`. Exception: the SSE/POST relay endpoints, which are deliberately raw HTTP because tRPC doesn't model long-lived streams well.
2. **Optimistic updates** for list ops, toggles, profile edits (`onMutate` → `onError` rollback). Use `invalidate` only for critical operations (payments, auth).
3. **shadcn/ui** components for any new UI primitive (`@/components/ui/*`); compose with Tailwind utilities. Avoid bespoke CSS.
4. **Stable query inputs** — never pass `new Date()` or fresh array literals as tRPC query inputs. Use `useState(() => new Date())` or `useMemo`. (See `README.md` "Common Pitfalls" → "Infinite loading loops from unstable references".)
5. **File storage** — use `storagePut(key, bytes, mime)` in `server/storage.ts`. Save the returned `key` in MySQL; don't store bytes in the DB.
6. **Animation discipline** — 100–300ms, ease-out for entering UI, ease-in-out for moving. Never animate `width/height/padding/margin/top/left`; only `transform` and `opacity`. Add `@media (prefers-reduced-motion: no-preference)` gates around non-essential motion.
7. **Auth** — use `useAuth()` for current user state; never read cookies directly. v2.92 (R3, owner decision): the Manus OAuth sign-in UI is REMOVED — `getLoginUrl` no longer exists; every sign-in affordance opens the native `AuthPanel` (email one-time code + 4-digit PIN). Do NOT re-add an OAuth sign-in path (a client-wide test in `verifiedBadge.test.ts` forbids it); the server's `/api/oauth/callback` remains only for pre-existing sessions.
8. **Origins/domains** — always use `window.location.origin` client-side and `appBaseUrl(req?)` (`server/appUrl.ts`) server-side. Never hardcode `manus.space` or deployment domains anywhere in `server/`, `client/src/`, `shared/` (pinned by `server/noHardcodedDomains.test.ts`; `mobile/` and `scripts/` are the deliberate exceptions).
9. **Datetime** — store as UTC Unix ms in the DB; convert in React via `new Date(ts).toLocaleString()`.
10. **Logs** — `.manus-logs/devserver.log`, `browserConsole.log`, `networkRequests.log`, `sessionReplay.log`. Don't read these as text files; use grep/tail.

---

## Testing

Vitest runs Node-environment tests under `server/**/*.test.ts` and `client/src/lib/**/*.test.ts` (see `vitest.config.ts`). New features must come with tests — focus on protocol shapes, business logic, and pure functions. Browser DOM tests are not currently configured; UI is verified via manual preview.

> Run `pnpm test` before every checkpoint / PR. All 453 must stay green.

---

## Deploying

**Every push to `main` auto-deploys to `.io`** (the only live deployment): `.github/workflows/deploy.yml` builds, uploads to S3, and rolling-deploys via SSM to the EC2 fleet (one server at a time, `/api/health` gated, ~60–90s end to end). Verify with `curl https://your-chat.io/api/version`.

The former Manus/`.org` deployment was **emptied and retired by the owner (2026-07-21)** — the Publish-button flow no longer applies. Do not point anything (docs, mobile clients, emails) at `.org` or `manus.space`. There is **no Vercel, Netlify, Railway, or Render involved**.

---

## What to be careful about

1. **Don't refactor `server/relay.ts`'s in-memory maps into a database** unless you also implement durable cross-instance routing — the simple design relies on single-instance affinity that Cloud Run usually provides.
2. **Don't add SSR / Next.js** — this is a Vite SPA + Express server. Different framework.
3. **Don't add WebSocket** anywhere user-facing — production gateway won't pass the upgrade. Use SSE+POST or long-polling.
4. **Don't preload MediaPipe models** — they're ~3 MB each and most users won't use filters.
5. **Don't mirror the outgoing video** — only mirror the local self-preview on front camera, never the published stream.
6. **Don't bypass `RTCRtpSender.replaceTrack`** when changing the local track — replacing the stream object entirely forces a renegotiation that drops media for ~1s.
7. **Don't commit secrets** — `.env*` is in `.gitignore`. Use `webdev_request_secrets` in Manus or `Settings → Secrets` in the Management UI.
8. **Database changes** are not auto-reversible. Use `pnpm db:push` only after reviewing the generated SQL in `drizzle/migrations/`.

---

## Pending work / future ideas

These are noted but not yet implemented. If you pick one up, mark it in `todo.md` first.

- Persist a user's assigned 6-digit number + recents in `localStorage` so refreshes keep identity.
- Add a "Mic / Camera test" panel before registration so users confirm permissions before joining.
- Add a real-device test matrix (Safari iOS, Chrome Android, Firefox desktop) for the camera-flip + filter pipeline.
- Consider replacing the free TURN fallback with a self-hosted coturn once usage grows beyond what `openrelay.metered.ca` rate-limits comfortably.
- Add screen sharing via `getDisplayMedia` (no signaling changes needed; same `addTrack` flow).

---

## Working with the user

The user prefers:
- **Concise, honest progress updates** — no exaggeration ("perfect", "fully bug-free"); just specific, testable claims.
- **Versioned footer on every deploy** — bump the version string in `Relay.tsx` (`RELAY · v1.2.0`) on every shipped change.
- **Direct deployment to the live server** when possible — keep iteration cycles tight.
- **System-wide application** — when fixing a bug or adding a feature, apply it consistently across the codebase, not just where the bug surfaced.

---

*This file is the contract between the codebase and any agent contributing to it. Keep it accurate. When you ship a change, update the relevant section here and in `todo.md` in the same commit.*
