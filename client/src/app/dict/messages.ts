/** Strings owned by the messages surface. See ./core.ts for why each area has its own file. */
import type { Entry } from "./types";

export const MESSAGES = {} as const satisfies Record<string, Entry>;
