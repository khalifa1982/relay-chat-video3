/** Strings owned by the profile surface. See ./core.ts for why each area has its own file. */
import type { Entry } from "./types";

export const PROFILE = {} as const satisfies Record<string, Entry>;
