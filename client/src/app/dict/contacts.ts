/** Strings owned by the contacts surface. See ./core.ts for why each area has its own file. */
import type { Entry } from "./types";

export const CONTACTS = {} as const satisfies Record<string, Entry>;
