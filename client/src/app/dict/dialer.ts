/** Strings owned by the dialer surface. See ./core.ts for why each area has its own file. */
import type { Entry } from "./types";

export const DIALER = {} as const satisfies Record<string, Entry>;
